

## 2026-07-11 ledger compaction — archived completed units
- **PHYS-TRUTH-BRIDGE-001 — Accepted explicit-contact adapter.** `src/duel_physics.rs` now reduces exactly two consecutive 120 Hz shared-physics steps from one action tick to a caller-specified `truth::PhysicalContactBatch`. It preserves a measured no-contact interval as `None`; any swept collision becomes the sole in-range contact evidence. It rejects nonconsecutive physics ticks and mismatched action ticks rather than synthesizing a result. The adapter creates no weapon/body proxy and is deliberately not wired to the renderer's static pose or generated motion. Evidence: `cargo test --bin just-dodge duel_physics::tests` 5/5 and `cargo test --bin just-dodge truth::tests` 5/5; `rustfmt --check src/duel_physics.rs` and diff checks pass.
- **BONES-SEED-INGEST-005 — Accepted provenance intake.** Completed the authorized Hugging Face checkout on `/run/media/vdubrov/NVMe-Storage1/Just Dodge/bones-seed`; direct Hugging Face tree comparison verified every one of 541 remote files exists locally at the exact advertised size. `tools/audit_bones_seed.py` now understands v004’s empty per-motion path columns, records archive-container provenance, and can fail closed against a streamed exact G1 tar-member index. `tools/index_bones_seed_g1.py` streams `g1.tar.gz` without extraction. For the immutable metadata/license hashes `71d09422034946d17dbb0db027d569408c486545bdb12f7979d55e6ffd9bbc39` / `eb9b70b084e0a5dcda8465a4be2ba2f5b81e3c3800305c727777a3176defbc5f`, lexical selection matched 2,830 motions; 2,426 have an exact G1 archive member and were admitted; 404 are excluded because no corresponding G1 member exists. Audit/index remain inside ignored gated-data storage; no raw data was extracted or redistributed, and no model/runtime artifact was promoted.
- **PHYS-120-001 — Accepted shared-contact reducer.** Added `src/duel_physics.rs`: a single-writer 120 Hz reducer that consumes both fighters' prior/current weapon proxies and current body proxies in the same step, validates finite physical input before tick advancement, evaluates both attack directions before selection, and canonically orders every bilateral candidate by time of impact and stable identity. `src/hitbox.rs` now exposes ordered `swept_contacts`, preserving simultaneous proxy-pair contacts rather than discarding them in an earliest-hit helper. This is a contact reducer only, not a rigid/articulated solver and not yet wired to renderer pose input or `PhysicalContactBatch`. Evidence: `cargo test --bin just-dodge duel_physics::tests` 3/3 and `cargo test --bin just-dodge hitbox::tests` 7/7 pass; touched files pass rustfmt and `git diff --check`.
- **COMBAT-MOTION-SOURCE-RESEARCH-005 — Accepted.** Added `docs/research/COMBAT_MOTION_DATA_SOURCING_2026-07-10.md` with current primary-source license findings. Confirmed: Kyokushin optical mocap is CC0 (Figshare article `12315629`, DOI `10.6084/m9.figshare.12315629.v1`); BONES-SEED is gated under its own license, not unrestricted open commercial data; Mixamo/Rokoko/ActorCore public pages establish commercial project use but not model-training rights; DuoBox has gated access with no public commercial license. Added `tools/audit_bones_seed.py`, which creates a non-redistributable internal audit from an authorized checkout and requires an opaque commercial-license reference; added `data/bones-seed/` to `.gitignore`.
- **COMBAT-CONDITION-DATA-004 — Accepted.** `src/combat.rs` now carries explicit inter-agent data needed for a future combat-trained MotionBricks condition stream: `OpponentRelativeRoot`, contact state (`Guard|Strike|Parry|Grab|Impact|Knockdown`), paired semantic sockets, deep-anatomy query, hard/soft binding, and force/direction labels. Hard strike/parry/grab/impact bindings are structurally rejected unless their 30 Hz frame maps inside the immutable 60 Hz active window; zero-force/zero-direction impacts and empty anatomy queries are rejected. Six focused combat tests pass, including an out-of-window paired-impact rejection. The fields are metadata only: truth still owns collision, injury, and outcomes; the released navigation checkpoint does not consume them.
- **MB-OFFICIAL-KEYFRAME-003 — Rejected; no target adapter retained.** After the accepted episode reset, a real `Thrust/Longsword/Top` four-frame source primitive could be fed through the official full-agent loop byte-stably (SHA-256 `87685da7fe2f363341a4ddae61e4d2ac622dcb69f1144a374ec553ac834f272a`), but raw G1 output failed the first mechanical gate: 44 frames, root path `62.8937m`, maximum joint step `6.7531m`, pelvis range `-15.858..6.674m`. Its measured source was finite and bounded: 4 frames, root path `0.0305m`, pelvis `0.736..0.737m`. The target-injection code was rolled back. This directly falsifies “keyframe injection alone turns the released navigation checkpoint into martial motion.” The required next path is combat-specific data/condition schema and fine-tuned model validation, while deterministic truth remains external.
- **MB-OFFICIAL-EPISODE-RESET-002 — Accepted.** Added `_reset_official_episode()` to reset the full agent, base controller state, and `random_controller`'s retained `_control`/`_time_since_prev_control` before each request. Same-process official navigation now reproduces byte-identically at seed 17: SHA-256 `e3318a5639996ea0e81a62c38463d4fc12477ff08e44d5bb6abceb24ed5bead1` twice. This repairs request reproducibility only; it does not add combat semantics.
- **MB-OFFICIAL-KEYFRAME-001 — Rejected at Strike 2; no adapter retained.** The thin source-target adapter successfully proved shape compatibility between decoded real primitive frames and the official full-agent target interface: source/inverse `[1,4,34,3]` positions + `[1,4,34,3,3]` rotations; agent target inputs have the same shapes plus `[1,4,3]` roots. But same-process deterministic output failed twice after resetting Torch/CUDA/NumPy, agent, and controller base state. The provisional adapter was fully rolled back. Fresh-process control (official navigation, seed 17) reproduced SHA-256 `e3318a5639996ea0e81a62c38463d4fc12477ff08e44d5bb6abceb24ed5bead1` twice, falsifying checkpoint/GPU randomness as the primary cause. The next falsifier is controller episode state: upstream `random_controller` retains `_control` and `_time_since_prev_control` because inherited reset clears only `_prev_qpos`. No combat runtime capability is claimed.
- **COMBAT-PRIMITIVE-001 — Deterministic deep-simulation primitive contract.** Replaced hand-authored float action profiles in `src/combat.rs` with a validated T1/T2/T3 intent schema: millimetre/milliradian root constraints, semantic body/weapon anchors, hard/soft pose constraints, multiple 60 Hz contact windows, and anatomy-layer queries spanning armor through skin/fascia/muscle/tendon/ligament/bone/vessel/nerve/organ. Broad hit locations are explicitly acceleration hints, never injury authority. The schema rejects non-token durations, out-of-range constraints, empty anatomy queries, and a contact phase without an in-window hard pose constraint. Focused binary test: 5/5 pass; `rustfmt --check` and `git diff --check` pass. This establishes the intent/truth schema, not a claim that the released navigation checkpoint already produces martial motion.
- **C0-RETARGET-FRAME-CAL-001 — Accepted structural frame calibration; generated source falsified.** Added causal world-frame G1→C0 transfer with cumulative one-to-many chain distribution for 163-bone C0 limbs/spine. RED 90-degree upper-arm test proved the two target bones receive 45/90-degree cumulative world deltas. The corrected ANM reference plus measured four-frame strike primitive preserves source segment lengths within `0.000000209 m`, target local lengths exactly, determinant `0.999996662–1.000000834`, and axis scale `0.999993682–1.000006676`; visual QA at `qa/frame_calibration_001/c0_contact.png` passes connected, non-exploding anatomy. Source-only inspection falsified the neural generation path before retargeting: its 32-frame G1 skeleton is already severely contorted (`qa/rejected_reference_delta_001/g1_generated_source_contact.png`). Added `load_primitive_clip` only as an explicit measured calibration path, never as a runtime fallback. Default runtime remains unchanged pending MB-GENERATION-SOURCE-001.
- **MESHY-RUNTIME-C0-001 — Cook/reference infrastructure accepted; direct G1 local-delta retarget rejected at Strike 2.** The SKM/ANM cooker was corrected to serialize root/world, parent-relative pose and exact inverse-bind matrices consistently. Corrected C0 cook verifies `14,517` vertices, `80,268` indices, `163` bones, zero bad top-eight weight sums, coherent bind and coherent reference-pose CPU skinning. `asset.rs` now loads dynamic ANM1 hierarchies and computes arbitrary-bone reference-pose skin matrices; its real C0 regression passes. The attempted name-level G1 local rotation-delta transfer was rejected: Repair 1 fixed the cooker but generated strike frames remained anatomically contorted; Repair 2 anchored deltas to strike frame 0 and still produced extreme spine/limb looping and self-intersection. Determinants (`0.999875..1.000002`) and axis scales (`0.999942..1.000003`) proved that matrix-validity checks do not prove anatomical validity. Failed retarget code/probe was removed. Rejection evidence: `assets/source/meshy/c0_base_fighter/pose_carrier_001/qa/rejected_reference_delta_001/temporal_contact.png` SHA-256 `de7854984f33db7ff37e38cef03571104b3a80a8b86cceb6ea61878295428de1`. Next falsifier requires measured source/target joint-frame calibration including bone-roll bases and one-to-many chain distribution, not another direct local-delta map.
- **C0-POSE-CARRIER-001 — Accepted preserve-bind male C0 reference-pose carrier.** Generated an MPFB male/muscular body with fixed macros (`gender=1.0`, `muscle=0.72`, `weight=0.52`, `height=0.62`), retained one closed 13,380-vertex/13,378-face body and the validated 163-bone/top-eight bind, transferred Meshy extremity detail with the existing 4 mm bounded topology-preserving process, then authored `C0_REFERENCE_POSE` as direction-only rotations for 33 major bones. Bind vertices, topology and weights remain exactly unchanged. Reference direction error is 0°, measured pose is 1.958757 m high ×1.920826 m arm span, and deterministic scale `0.9189499701711632` yields 1.8 m. Simultaneous 60-degree fingers/toes remain coherent. FBX re-import preserves one closed manifold, 163 bones, zero unbound vertices and at most six influences; `c0_reference_pose.json` reruns byte-identically. Artifacts: `assets/source/meshy/c0_base_fighter/pose_carrier_001/`; tool: `tools/create_c0_reference_pose.py`. Not runtime-installed.
- **C0-BASETOPO-001 — Accepted full-articulation topology carrier and bounded Meshy extremity transfer.** MPFB2 2.0.17 commit `26c811d307b57e2f9f3d743b92d69681b1704e85` supplies CC0 base/rig assets. Helper removal yielded one closed manifold 13,380-vertex/13,378-face body with 163 bones, 38 finger bones, 28 toe bones and zero unbound vertices. Simultaneous 60-degree hand/toe articulation passed. Top-eight pruning affected 920 vertices but measured only 0.142 mm maximum deformation error and zero vertices over 1 mm; FBX re-import preserved 163 bones and at most six influences. Bounded symmetric Meshy hand/foot detail transfer changed 5,326 vertices by at most 4 mm while preserving topology and weights exactly. Deterministic rerun had zero vertex difference. Preserved under `assets/source/reference_humans/mpfb2_base_001/` and `assets/source/meshy/c0_base_fighter/basetopo_001/`; not runtime-installed.
- **MESHY-SKIN8-001 — Accepted eight-influence SKM/runtime path.** Added a 96-byte vertex format carrying two `u32x4` joint vectors and two `f32x4` weight vectors, eight-term WGSL LBS, top-eight SKM extraction/loading, legacy four-influence zero-tail compatibility, and verifier support. RED layout test failed on the former four-element arrays; new layout and synthetic serialized-eight loader regressions pass. Fixed a discovered FBX cooker defect by baking imported mesh/armature world transforms before axis conversion; C0 cooked height changed from erroneous 1.47 m to measured 1.80 m. C0 cook: 97,305 vertices, 581,820 indices, 24 bones, zero bad weight sums/>8. Top-eight versus full-nine across 16 walk/run samples: 79 dropped assignments, ≤0.00016465 m max error, ≤0.000000938 m RMS; top-four had 0.0279 m max and remains rejected. Full `cargo test --all-targets --locked` passed 97 executions; `cargo check --all-targets --locked`, legacy male/female SKM verification and wgpu shot shader smoke passed. Cooked outputs under `rigged_001/cooked/`: mesh `415e64b1acbf22dd6d207961fcab2bc30db32c67ff5150a0ed4f147be0478036`, walk `11b2a8d27c5b27eff5fd9524689abb8bea2cc90dee7a7467f432f92f21468ef2`, run `7c54f3d8f7578ee1a3377b197fd4d7855f622e6fd09e82f8b51387baa74ba3ef`. Global fmt remains blocked only by untouched `src/combat.rs:206`; touched Rust files pass rustfmt.
- **MESHY-RIG-001 — Accepted Meshy body-rig source.** Verified temporary HTTPS intake of C0 retopo and completed Meshy rig task `019f4c2a-73ff-7bd1-950d-f3f5cee8a2dc` for 5 credits. Output: one 24-bone armature, 96,822 vertices/193,940 faces, zero unbound vertices, normalized weights, included 32-frame walk and 20-frame run at 30 fps. Bind/walk/run cardinal vision passed without explosion, collapse or graft pumping. Rig has no finger bones and up to nine influences; naive top-four pruning was rejected after 27.9 mm max sampled deformation error. Artifacts `assets/source/meshy/c0_base_fighter/rigged_001/`; rig FBX `81fd608d4c8b53fd29651e16541d3b37ff8cc08a1ccd36b71cdf772d08bb489f`; walk `06174fe350a984c51c2f1db8a50b89c4eb2b4e218f2acc17c1569e9d2c726c3c`; run `34efb951eb9f221f28fa8aba65e7fc0d427e1bdfb2a01bf369ca06a3d7a2f354`; manifests 21/21 pass.
- **C0-RETOPO-003 — Accepted rig-input outer-body topology.** Measured actual graft cross-sections and found the foot ankle y-center offset by 76 mm and hand stump radius only ~one-third of the forearm. Translated/inset foot stumps and tapered hand stumps over x=0.705–0.780 m before independent pre-simplification and exact boolean union. Final mesh: one closed manifold component, 146,356 vertices, 290,325 faces, zero boundary/nonmanifold edges, 1.789993×0.294004×1.800000 m. All five digits per extremity passed close vision; wrist/ankle cuffs and rings are absent. FBX topology and GLB dimension/material parity pass; deterministic rerun matched source, simplification, graft measurements and topology. Artifacts `assets/source/meshy/c0_base_fighter/retopo_001/`; tool `14fd4afdeb625ed3bd905132eac49ca113ea76f90a1f390770b99b63441dfa03`; FBX `fcd0fe6530facd305f20b4c265edd439accad70f58bcf21d5ef287c26e51ec6d`; GLB `050447efb8368432e347a179226b365e7103bd64983a5ba756b4bad7dc94ebb4`; manifests 20/20 pass.
- **C0-ASM-001 — Accepted complete outer-body source assembly.** Regenerated foot candidate 002 with coherent five-toe references and Meshy-6 task `019f4bef-03fa-7e89-beb5-747f41519a96` (20 credits), yielding a 75,132-vertex/150,280-polygon closed manifold source. Normalized candidate 003 to 1.80 m; cut/capped mitten hands and toeless feet; grafted mirrored accepted five-digit hands and corrected five-toe feet as five separate closed-manifold components. Final dimensions 1.790000×0.338886×1.800000 m; symmetric T-pose; explicit 15 mm wrist and 60 mm ankle overlaps. FBX/GLB preserve five object names, dimensions and neutral material parity. Determinism rerun matched source hashes, interfaces, dimensions and all component statistics. Cardinal/extremity vision passed conditionally for unified retopology/weights and seam smoothing before rigging. Artifacts `assets/source/meshy/c0_base_fighter/assembled_001/`; assembler `1032719994a38be8cecbfccc9e32a6d02442974ccf536068f02a61df0523438a`; FBX `9c4dd4a5bceb15490eac61dea62022daea4372299ea757218fa20bb18e479a35`; GLB `6063b8fa9382437c323bc1d8807476bda616c57aa81df202c6fbf86788be8ddc`; manifests 19/19 pass.
- **E0-ASM-001 — Accepted Meshy-derived modular arena threshold.** Reconstructed the accepted monolithic gateway silhouette as 115 named, independently addressable closed-manifold components with structural, impact, brace, foot, cap, socket, plate, and visual-fastener roles. Final dimensions 3.620000×0.668000×3.000000 m; clear opening 2.35×2.48 m. FBX re-import preserves 115 manifold objects, four material IDs, names and dimensions; GLB preserves object/material/dimension parity. Determinism rerun matched source hash, dimensions, clearance and all component statistics. Strict cardinal vision passed after fastener/socket-plate detail repair. Hidden raw Meshy shell remains in BLEND provenance only. Artifacts `assets/source/meshy/e0_arena_threshold/assembled_001/`; assembler `e59167b9d6b080b87773c941cdff10a351d8bc2aa89403616cab0234529352da`; FBX `d81977868564f4c9682a57da0141646e909e861c12edd7c36613553ff3f23ee5`; GLB `faf1fb3bc4822361fc8cb4cd156e022635abe6ae8e03b7076b26dd394678c0de`; manifest schema 17/17 pass.
- **W0-ASM-002 — Accepted component-first W0 source weapon.** Baked each FBX `matrix_world` into mesh coordinates before direct metric scaling; assembled nine independent components with explicit material IDs and mechanical interfaces. Final dimensions 0.280000 × 0.050000 × 1.549825 m. FBX re-import: nine named closed-manifold components, zero boundary/nonmanifold edges, material and dimension parity. GLB re-import: nine object/material/dimension parity (seam-split topology is non-authoritative). Determinism rerun matched source hashes, dimensions and complete component statistics. Strict cardinal and hilt vision passed after collar seating repair. Artifacts: `assets/source/meshy/w0_sword/assembled_001/`; assembler SHA-256 `bc79cfe34858ed66453d7304d8b5f393c7e5b1d88264bec2d64a52a737e1ebff`; FBX `01705aabb18c55686e701eeb0d57db30aba3021a8d05514834993a5fef723cba`; GLB `5ab0ba05dc321fc759c4ea02100ec843d85cd5b36faa1fb1a0a3ef46252053e4`; manifest schema 16/16 pass.
- **MESHY-R1 — Delegated-doc cleanup.** Reconciled the late five-agent Meshy batch against direct measured trials and current official docs. Removed duplicated playbooks containing wrong remesh cost, seed-reuse claims, unsafe printability/Creative-Lab recommendations, fallback/model-degradation strategies, and the unexercised rig validator. Deleted all five files from unauthorized commit `66e9b03` in the working tree, including guessed `meshy_to_canonical_bones.json`; retained the verified `meshy-api-integration` skill, repo pipeline/schema, and measured candidate manifests.
- **ANAT-R1 — Frontier anatomy/control research reconciliation.** Preserved the combat-control source inventory as non-canonical research leads; replaced conflicting anatomy-source drafts with `docs/research/ANATOMICAL_ASSET_SOURCE_STRATEGY_2026.md`; verified current official BodyParts3D CC BY 4.0 (2025-02-27), NLM Visible Human public-domain status, task-specific TotalSegmentator usage terms, Z-Anatomy/Open3DModel share-alike status, and SKEL non-commercial status. Rewrote `just-dodge-deterministic-injury-architecture` v2.0 to enforce the locked 500–1,000-structure full-deformable truth, sparse activation, multirate solvers, provenance and no dependency/fallback shortcuts. Deleted the delegated 6–8-region architecture and stale licensing reports.
- **MESHY-002 — First measured Meshy-6 component batch.** Cached/reconciled 31 official docs (489,785 characters); corrected `meshy-api-integration`; added repo pipeline/schema; generated 15 manifested Meshy tasks (280 task-reported credits) plus one rejected 9-credit 2D task; validated FBX files with Blender/bmesh and strict vision. Accepted geometry: right hand, W0 blade, W0 guard, E0 threshold (conditional component split). Rejected/conditional evidence is preserved for bodies, foot, whole swords, grip, pommel, and two E0 retextures. No current runtime asset was replaced.
- **JD-001 — Deterministic fixed-step truth timing.** Added exact `CombatTruth::tick()`, integer fixed-step accumulator with retained bounded catch-up, per-tick replay recording, renderer-start clock reset, and 30/60/144/240 Hz hash-equivalence regression. Focused tests passed; `cargo check --all-targets --locked` and full `cargo test --all-targets --locked` passed (89 tests). Runtime smoke initialized renderer/UI and emitted 20,295 telemetry rows without panic. Pre-existing global `cargo fmt --check` and `clippy -D warnings` debt remained unchanged in the touched scope.
  - *Validation Executed:* RED `/tmp/jd001_red.log`; fixed-step 2/2 and truth 4/4 focused tests; `cargo check --all-targets --locked` pass; `cargo test --all-targets --locked` 89 executions/0 failures; debug build and Xvfb runtime pass; cross-rate ordered hashes identical at 30/60/144/240 Hz. Pre-existing format/clippy debt unchanged (`FMT_RC=1`, `CLIPPY_RC=101`).
  - *Independent review reconciliation:* startup-delay complaint rejected—the explicit renderer-start reset intentionally discards non-simulation initialization wall time and prevents machine-speed-dependent starting frames. Public `tick()` contract concern is API hardening, not a demonstrated determinism bug. Catch-up arithmetic retains debt and is safe in realistic durations. One valid pre-existing defect remains: multiple catch-up ticks can advance and record after `match_over`; tracked as JD-001A.


## 2026-07-11 ledger compaction — archived completed units
- **PHYS-WORLD-001 — Accepted deterministic external-target world boundary.** Added `src/duel_world.rs` and registered it in `src/main.rs`. `DuelWorld` accepts only explicit world-space weapon transforms plus externally supplied physical body proxies, retains prior weapon state for CCD, validates all transforms/proxies before a substep advances, evaluates exactly two 120 Hz targets as one 60 Hz interval, and emits the existing `PhysicalContactBatch`. It does not generate target motion or accept renderer skinning/generated animation as physics evidence. Tests prove tunnelling contact, explicit whiff, non-finite-input rejection without clock advancement, target determinism, and actual `CombatTruth` admission during Resolve. Evidence: `cargo test --bin just-dodge duel_world::tests` 5/5; `duel_physics::tests` 5/5; `truth::tests` 5/5; targeted rustfmt and diff checks pass. One initial test assertion used `Result` equality against a non-`PartialEq` success type; repaired once by asserting the error directly.

- Ledger compaction: archived 23 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.


## 2026-07-11 ledger compaction — archived completed units
- **PHYS-SEMANTICS-002 — Accepted role-tagged physical classification.** `HitboxProxy` now carries stable `Body|WeaponEdge|WeaponGuard` roles. `DuelWorld` accepts externally supplied guard proxies; `SharedDuelPhysics` validates them, retains attacker/defender roles on every bilateral candidate, and makes same-time guard contact outrank body contact when producing a truth packet. `truth::ContactGeometry` preserves measured attacker side and `Body|Guard` surface; `action_matrix::resolve` derives Whiff from absent contact, Hit from body contact, and Beat from guard contact, with the winner from the measured attacker/defender rather than a table label. Action-pair data remains only force/location configuration. Evidence: `duel_physics::tests` 4/4, `action_matrix::tests` 6/6, `duel_world::tests` 5/5, `truth::tests` 5/5, `hitbox::tests` 7/7; `rustfmt --check` for rewritten/new semantic modules and `git diff --check` pass. Initial test compilation exposed 11 schema-propagation errors after adding role/contact fields; repaired once by updating all explicit fixtures and target constructors.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.


## 2026-07-11 ledger compaction — archived completed units
- **SLICE-CONTRACT-003 — Accepted locked first playable action contract.** `src/combat.rs` now owns the single typed `Longsword / Top / {Thrust, Block, Dodge}` registry. Plan-phase truth rejects out-of-slice actions and stances; player input maps `1/2/3` and left-click to Thrust/Block/Dodge; UI presents exactly those actions and a Top-stance prompt; AI selects only those actions and Top. Broader `Strike`/`Grab` and side-stance source/motion data remains retained as non-selectable evidence. Tests: `combat::tests` 7/7, `input::tests` 3/3, `ai::tests` 5/5, `truth::tests` 6/6, and fixed-step render-rate hash gate 1/1. `rustfmt --check` passed for authored slice files and `git diff --check` passed. Existing warnings in `motion.rs`/`retarget.rs` are pre-existing debt.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.


## 2026-07-11 ledger compaction — archived completed units
- **CLEANBOX-RUNTIME-004 — Accepted real runtime physical submission.** Added `src/cleanbox.rs`, a deterministic kinematic collision target producer for the locked `Longsword / Top / {Thrust, Block, Dodge}` slice. It supplies only weapon-edge transforms, Block weapon-guard proxies, and torso body proxies; it never reads C0 skinning, renderer matrices, or generated motion. `App::combat_update` now invokes it immediately before every unresolved Resolve tick, where it clears only prior-exchange weapon history, evaluates exactly two 120 Hz `DuelWorld` substeps, and submits the resulting physical packet before `CombatTruth::tick()`. Direct tests prove Thrust→Block produces a guard/Beat packet, Thrust→Dodge produces an explicit Whiff, duplicate inputs reproduce packet and truth hash, and the runtime-order render-rate hash gate remains stable at 30/60/144/240 Hz. Evidence: `cargo check --bin just-dodge`; cleanbox 3/3, `duel_world` 5/5, `duel_physics` 4/4, truth 6/6, fixed-step 1/1; `rustfmt --check src/cleanbox.rs`; `git diff --check`. One compile repair changed the pure dodge-offset helper from `const fn` because derived `PartialEq` is not const-callable. New-module cargo-check warnings: none.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.

## Archived after FIRST-PERSON-CAMERA-008
- **EPISODE-EVIDENCE-005 — Accepted compact resolved-packet replay evidence.** `src/replay.rs` now appends a backward-compatible `ResolvePacket` event containing pre-Resolve selected actions, the exact two consecutive 120 Hz physics ticks and shared action tick, every canonical role-tagged contact/TOI, explicit Whiff/Guard/Body outcome, quantized health/stamina deltas, and the post-Resolve truth hash. `App::combat_update` emits this event only after `CombatTruth::tick()` admits the same packet, before recording the matching frame hash. A runtime-order replay simulation proves exactly one receipt at every deterministic 3-second Thrust-vs-Block exchange and equal receipts/hashes at 30/60/144/240 Hz. Evidence: `cargo check --bin just-dodge`; replay 6/6; fixed-step 1/1; cleanbox 3/3; truth 6/6; `rustfmt --check src/replay.rs`; `git diff --check`. Initial test used a non-existent `Vec::single`; repaired once with explicit cardinality plus indexed event matching. New-module cargo-check warnings: none.

## Ledger compaction 2026-07-11
- **EPISODE-REDUCER-006 — Accepted independent replay receipt reducer.** Added the read-only `just-dodge --reduce-replay <path.jdrp>` path. It bypasses WGPU/assets, loads the native container, validates snapshots and every `ResolvePacket`, and prints a stable result table plus `verdict=PASS`. It rejects missing/duplicate snapshot or Resolve frames, frame/hash mismatches, nonconsecutive physics ticks, mismatched action ticks, contacts outside the two recorded ticks, non-finite/out-of-range TOI, empty actions, and Whiff/Guard/Body role contradictions. No replay bytes or truth state are modified. Evidence: reducer Guard/Whiff + malformed-invariant tests, replay 8/8, fixed-step 1/1; actual binary reduced `/tmp/just_dodge_resolve_receipt_a.jdrp` to one Guard packet with `verdict=PASS`; `rustfmt --check src/replay.rs`, `git diff --check`. No new dependencies or reducer-source cargo-check warnings.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.

- **EPISODE-CAPTURE-007 — Accepted reducer-driven Resolve contact sheet.** `src/replay.rs` now emits canonical `contact frame action_tick physics_tick toi attacker_role defender_role` rows after the validated packet table. New stdlib-only `tools/qa/render_replay_contact_sheet.py` invokes the native reducer, rejects invalid/mismatched row counts, renders a timeline SVG, and writes a provenance receipt containing the replay/report/SVG SHA-256 values. The sheet explicitly states that markers are reducer receipt evidence, not rendered poses. Evidence: replay 8/8, fixed-step 1/1, Python compile, hash/count receipt verification; fresh source replay hash `f3cd3cc1bc0059ee5589bd25951f5cb4f6f1bcb478a0374fccb0838120392ec6`; repaired sheet at `qa_runs/episode_capture_20260711T233234Z_repaired/contact_sheet.png`, receipt hash `908c6891488dd73376dec20b5031ccdd7b2b474b7de13abe0ff2d3f800a42528`; vision inspection passed after expanding the card and adding the caveat (all labels/markers/roles readable, no clipping/overlap, no rendered-pose implication). `rustfmt --check src/replay.rs` and `git diff --check` passed. One verification-only threshold was corrected because the valid 312-byte text report was intentionally smaller than the initial arbitrary 1 KB threshold; no source behavior changed.
- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.
