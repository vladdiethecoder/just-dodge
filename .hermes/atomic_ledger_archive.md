

## 2026-07-11 ledger compaction — archived completed units
- **PHYS-TRUTH-BRIDGE-001 — Accepted explicit-contact adapter.** `src/duel_physics.rs` now reduces exactly two consecutive 120 Hz shared-physics steps from one action tick to a caller-specified `truth::PhysicalContactBatch`. It preserves a measured no-contact interval as `None`; any swept collision becomes the sole in-range contact evidence. It rejects nonconsecutive physics ticks and mismatched action ticks rather than synthesizing a result. The adapter creates no weapon/body proxy and is deliberately not wired to the renderer's static pose or generated motion. Evidence: `cargo test --bin just-dodge duel_physics::tests` 5/5 and `cargo test --bin just-dodge truth::tests` 5/5; `rustfmt --check src/duel_physics.rs` and diff checks pass.
- **BONES-SEED-INGEST-005 — Accepted provenance intake.** Completed the authorized Hugging Face checkout on `/run/media/vdubrov/NVMe-Storage1/Just Dodge/bones-seed`; direct Hugging Face tree comparison verified every one of 541 remote files exists locally at the exact advertised size. `tools/audit_bones_seed.py` now understands v004’s empty per-motion path columns, records archive-container provenance, and can fail closed against a streamed exact G1 tar-member index. `tools/index_bones_seed_g1.py` streams `g1.tar.gz` without extraction. For the immutable metadata/license hashes `71d09422034946d17dbb0db027d569408c486545bdb12f7979d55e6ffd9bbc39` / `eb9b70b084e0a5dcda8465a4be2ba2f5b81e3c3800305c727777a3176defbc5f`, lexical selection matched 2,830 motions; 2,426 have an exact G1 archive member and were admitted; 404 are excluded because no corresponding G1 member exists. Audit/index remain inside ignored gated-data storage; no raw data was extracted or redistributed, and no model/runtime artifact was promoted.
- **PHYS-120-001 — Accepted shared-contact reducer.** Added `src/duel_physics.rs`: a single-writer 120 Hz reducer that consumes both fighters' prior/current weapon proxies and current body proxies in the same step, validates finite physical input before tick advancement, evaluates both attack directions before selection, and canonically orders every bilateral candidate by time of impact and stable identity. `src/hitbox.rs` now exposes ordered `swept_contacts`, preserving simultaneous proxy-pair contacts rather than discarding them in an earliest-hit helper. This is a contact reducer only, not a rigid/articulated solver and not yet wired to renderer pose input or `PhysicalContactBatch`. Evidence: `cargo test --bin just-dodge duel_physics::tests` 3/3 and `cargo test --bin just-dodge hitbox::tests` 7/7 pass; touched files pass rustfmt and `git diff --check`.
- **COMBAT-MOTION-SOURCE-RESEARCH-005 — Accepted.** Added `docs/research/COMBAT_MOTION_DATA_SOURCING_2026-07-10.md` with current primary-source license findings. Confirmed: Kyokushin optical mocap is CC0 (Figshare article `12315629`, DOI `10.6084/m9.figshare.12315629.v1`); BONES-SEED is gated under its own license, not unrestricted open commercial data; Mixamo/Rokoko/ActorCore public pages establish commercial project use but not model-training rights; DuoBox has gated access with no public commercial license. Added `tools/audit_bones_seed.py`, which creates a non-redistributable internal audit from an authorized checkout and requires an opaque commercial-license reference; added `data/bones-seed/` to `.gitignore`.
- **COMBAT-CONDITION-DATA-004 — Accepted.** `src/combat.rs` now carries explicit inter-agent data needed for a future combat-trained MotionBricks condition stream: `OpponentRelativeRoot`, contact state (`Guard|Strike|Parry|Grab|Impact|Knockdown`), paired semantic sockets, deep-anatomy query, hard/soft binding, and force/direction labels. Hard strike/parry/grab/impact bindings are structurally rejected unless their 30 Hz frame maps inside the immutable 60 Hz active window; zero-force/zero-direction impacts and empty anatomy queries are rejected. Six focused combat tests pass, including an out-of-window paired-impact rejection. The fields are metadata only: truth still owns collision, injury, and outcomes; the released navigation checkpoint does not consume them.
- **MB-OFFICIAL-KEYFRAME-003 — Rejected; no target adapter retained.** After the accepted episode reset, a real `Thrust/Longsword/Top` four-frame source primitive could be fed through the official full-agent loop byte-stably (SHA-256 `87685da7fe2f363341a4ddae61e4d2ac622dcb69f1144a374ec553ac834f272a`), but raw G1 output failed the first mechanical gate: 44 frames, root path `62.8937m`, maximum joint step `6.7531m`, pelvis range `-15.858..6.674m`. Its measured source was finite and bounded: 4 frames, root path `0.0305m`, pelvis `0.736..0.737m`. The target-injection code was rolled back. This directly falsifies “keyframe injection alone turns the released navigation checkpoint into martial motion.” The required next path is combat-specific data/condition schema and fine-tuned model validation, while deterministic truth remains external.
- **MB-OFFICIAL-EPISODE-RESET-002 — Accepted.** Added `_reset_official_episode()` to reset the full agent, base controller state, and `random_controller`'s retained `_control`/`_time_since_prev_control` before each request. Same-process official navigation now reproduces byte-identically at seed 17: SHA-256 `e3318a5639996ea0e81a62c38463d4fc12477ff08e44d5bb6abceb24ed5bead1` twice. This repairs request reproducibility only; it does not add combat semantics.
- **MB-OFFICIAL-KEYFRAME-001 — Rejected at Strike 2; no adapter retained.** The thin source-target adapter successfully proved shape compatibility between decoded real primitive frames and the official full-agent target interface: source/inverse `[1,4,34,3]` positions + `[1,4,34,3,3]` rotations; agent target inputs have the same shapes plus `[1,4,3]` roots. But same-process deterministic output failed twice after resetting Torch/CUDA/NumPy, agent, and controller base state. The provisional adapter was fully rolled back. Fresh-process control (official navigation, seed 17) reproduced SHA-256 `e3318a5639996ea0e81a62c38463d4fc12477ff08e44d5bb6abceb24ed5bead1` twice, falsifying checkpoint/GPU randomness as the primary cause. The next falsifier is controller episode state: upstream `random_controller` retains `_control` and `_time_since_prev_control` because inherited reset clears only `_prev_qpos`. No combat runtime capability is claimed.
- **COMBAT-PRIMITIVE-001 — Deterministic deep-simulation primitive contract.** Replaced hand-authored float action profiles in `src/combat.rs` with a validated T1/T2/T3 intent schema: millimetre/milliradian root constraints, semantic body/weapon anchors, hard/soft pose constraints, multiple 60 Hz contact windows, and anatomy-layer queries spanning armor through skin/fascia/muscle/tendon/ligament/bone/vessel/nerve/organ. Broad hit locations are explicitly acceleration hints, never injury authority. The schema rejects non-token durations, out-of-range constraints, empty anatomy queries, and a contact phase without an in-window hard pose constraint. Focused binary test: 5/5 pass; `rustfmt --check` and `git diff --check` pass. This establishes the intent/truth schema, not a claim that the released navigation checkpoint already produces martial motion.
- **C0-RETARGET-FRAME-CAL-001 — Accepted structural frame calibration; generated source falsified.** Added causal world-frame G1→C0 transfer with cumulative one-to-many chain distribution for 163-bone C0 limbs/spine. RED 90-degree upper-arm test proved the two target bones receive 45/90-degree cumulative world deltas. The corrected ANM reference plus measured four-frame strike primitive preserves source segment lengths within `0.000000209 m`, target local lengths exactly, determinant `0.999996662–1.000000834`, and axis scale `0.999993682–1.000006676`; visual QA at `qa/frame_calibration_001/c0_contact.png` passes connected, non-exploding anatomy. Source-only inspection falsified the neural generation path before retargeting: its 32-frame G1 skeleton is already severely contorted (`qa/rejected_reference_delta_001/g1_generated_source_contact.png`). Added `load_primitive_clip` only as an explicit measured calibration path, never as a runtime fallback. Default runtime remains unchanged pending MB-GENERATION-SOURCE-001.
- **MESHY-RUNTIME-C0-001 — Cook/reference infrastructure accepted; direct G1 local-delta retarget rejected at Strike 2.** The SKM/ANM cooker was corrected to serialize root/world, parent-relative pose and exact inverse-bind matrices consistently. Corrected C0 cook verifies `14,517` vertices, `80,268` indices, `163` bones, zero bad top-eight weight sums, coherent bind and coherent reference-pose CPU skinning. `asset.rs` now loads dynamic ANM1 hierarchies and computes arbitrary-bone reference-pose skin matrices; its real C0 regression passes. The attempted name-level G1 local rotation-delta transfer was rejected: Repair 1 fixed the cooker but generated strike frames remained anatomically contorted; Repair 2 anchored deltas to strike frame 0 and still produced extreme spine/limb looping and self-intersection. Determinants (`0.999875..1.000002`) and axis scales (`0.999942..1.000003`) proved that matrix-validity checks do not prove anatomical validity. Failed retarget code/probe was removed. Rejection evidence: `assets/source/meshy/c0_base_fighter/pose_carrier_001/qa/rejected_reference_delta_001/temporal_contact.png` SHA-256 `de7854984f33db7ff37e38cef03571104b3a80a8b86cceb6ea61878295428de1`. Next falsifier requires measured source/target joint-frame calibration including bone-roll bases and one-to-many chain distribution, not another direct local-delta map.
- **C0-POSE-CARRIER-001 — Accepted preserve-bind male C0 reference-pose carrier.** Generated an MPFB male/muscular body with fixed macros (`gender=1.0`, `muscle=0.72`, `weight=0.52`, `height=0.62`), retained one closed 13,380-vertex/13,378-face body and the validated 163-bone/top-eight bind, transferred Meshy extremity detail with the existing 4 mm bounded topology-preserving process, then authored `C0_REFERENCE_POSE` as direction-only rotations for 33 major bones. Bind vertices, topology and weights remain exactly unchanged. Reference direction error is 0°, measured pose is 1.958757 m high ×1.920826 m arm span, and deterministic scale `0.9189499701711632` yields 1.8 m. Simultaneous 60-degree fingers/toes remain coherent. FBX re-import preserves one closed manifold, 163 bones, zero unbound vertices and at most six influences; `c0_reference_pose.json` reruns byte-identically. Artifacts: `assets/source/meshy/c0_base_fighter/pose_carrier_001/`; tool: `tools/create_c0_reference_pose.py`. Not runtime-installed.
- **C0-BASETOPO-001 — Accepted full-articulation topology carrier and bounded Meshy extremity transfer.** MPFB2 2.0.17 commit `26c811d307b57e2f9f3d743b92d69681b1704e85` supplies CC0 base/rig assets. Helper removal yielded one closed manifold 13,380-vertex/13,378-face body with 163 bones, 38 finger bones, 28 toe bones and zero unbound vertices. Simultaneous 60-degree hand/toe articulation passed. Top-eight pruning affected 920 vertices but measured only 0.142 mm maximum deformation error and zero vertices over 1 mm; FBX re-import preserved 163 bones and at most six influences. Bounded symmetric Meshy hand/foot detail transfer changed 5,326 vertices by at most 4 mm while preserving topology and weights exactly. Deterministic rerun had zero vertex difference. Preserved under `assets/source/reference_humans/mpfb2_base_001/` and `assets/source/meshy/c0_base_fighter/basetopo_001/`; not runtime-installed.
- **MESHY-SKIN8-001 — Accepted eight-influence SKM/runtime path.** Added a 96-byte vertex format carrying two `u32x4` joint vectors and two `f32x4` weight vectors, eight-term WGSL LBS, top-eight SKM extraction/loading, legacy four-influence zero-tail compatibility, and verifier support. RED layout test failed on the former four-element arrays; new layout and synthetic serialized-eight loader regressions pass. Fixed a discovered FBX cooker defect by baking imported mesh/armature world transforms before axis conversion; C0 cooked height changed from erroneous 1.47 m to measured 1.80 m. C0 cook: 97,305 vertices, 581,820 indices, 24 bones, zero bad weight sums/>8. Top-eight versus full-nine across 16 walk/run samples: 79 dropped assignments, ≤0.00016465 m max error, ≤0.000000938 m RMS; top-four had 0.0279 m max and remains rejected. Full `cargo test --all-targets --locked` passed 97 executions; `cargo check --all-targets --locked`, legacy male/female SKM verification and wgpu shot shader smoke passed. Cooked outputs under `rigged_001/cooked/`: mesh `415e64b1acbf22dd6d207961fcab2bc30db32c67ff5150a0ed4f147be0478036`, walk `11b2a8d27c5b27eff5fd9524689abb8bea2cc90dee7a7467f432f92f21468ef2`, run `7c54f3d8f7578ee1a3377b197fd4d7855f622e6fd09e82f8b51387baa74ba3ef`. Global fmt remains blocked only by untouched `src/combat.rs:206`; touched Rust files pass rustfmt.
- **MESHY-RIG-001 — Accepted Meshy body-rig source.** Verified temporary HTTPS intake of C0 retopo and completed Meshy rig task `019f4c2a-73ff-7bd1-950d-f3f5cee8a2dc` for 5 credits. Output: one 24-bone armature, 96,822 vertices/193,940 faces, zero unbound vertices, normalized weights, included 32-frame walk and 20-frame run at 30 fps. Bind/walk/run cardinal vision passed without explosion, collapse or graft pumping. Rig has no finger bones and up to nine influences; naive top-four pruning was rejected after 27.9 mm max sampled deformation error. Artifacts `assets/source/meshy/c0_base_fighter/rigged_001/`; rig FBX `81fd608d4c8b53fd29651e16541d3b37ff8cc08a1ccd36b71cdf772d08bb489f`; walk `06174fe350a984c51c2f1db8a50b89c4eb2b4e218f2acc17c1569e9d2c726c3c`; run `34efb951eb9f221f28fa8aba65e7fc0d427e1bdfb2a01bf369ca06a3d7a2f354`; manifests 21/21 pass.
- **C0-RETOPO-003 — Accepted rig-input outer-body topology.** Measured actual graft cross-sections and found the foot ankle y-center offset by 76 mm and hand stump radius only ~one-third of the forearm. Translated/inset foot stumps and tapered hand stumps over x=0.705–0.780 m before independent pre-simplification and exact boolean union. Final mesh: one closed manifold component, 146,356 vertices, 290,325 faces, zero boundary/nonmanifold edges, 1.789993×0.294004×1.800000 m. All five digits per extremity passed close vision; wrist/ankle cuffs and rings are absent. FBX topology and GLB dimension/material parity pass; deterministic rerun matched source, simplification, graft measurements and topology. Artifacts `assets/source/meshy/c0_base_fighter/retopo_001/`; tool `14fd4afdeb625ed3bd905132eac49ca113ea76f90a1f390770b99b63441dfa03`; FBX `fcd0fe6530facd305f20b4c265edd439accad70f58bcf21d5ef287c26e51ec6d`; GLB `050447efb8368432e347a179226b365e7103bd64983a5ba756b4bad7dc94ebb4`; manifests 20/20 pass.
- **C0-ASM-001 — Accepted complete outer-body source assembly.** Regenerated foot candidate 002 with coherent five-toe references and Meshy-6 task `019f4bef-03fa-7e89-beb5-747f41519a96` (20 credits), yielding a 75,132-vertex/150,280-polygon closed manifold source. Normalized candidate 003 to 1.80 m; cut/capped mitten hands and toeless feet; grafted mirrored accepted five-digit hands and corrected five-toe feet as five separate closed-manifold components. Final dimensions 1.790000×0.338886×1.800000 m; symmetric T-pose; explicit 15 mm wrist and 60 mm ankle overlaps. FBX/GLB preserve five object names, dimensions and neutral material parity. Determinism rerun matched source hashes, interfaces, dimensions and all component statistics. Cardinal/extremity vision passed conditionally for unified retopology/weights and seam smoothing before rigging. Artifacts `assets/source/meshy/c0_base_fighter/assembled_001/`; assembler `1032719994a38be8cecbfccc9e32a6d02442974ccf536068f02a61df0523438a`; FBX `9c4dd4a5bceb15490eac61dea62022daea4372299ea757218fa20bb18e479a35`; GLB `6063b8fa9382437c323bc1d8807476bda616c57aa81df202c6fbf86788be8ddc`; manifests 19/19 pass.
- **E0-ASM-001 — Accepted Meshy-derived modular arena threshold.** Reconstructed the accepted monolithic gateway silhouette as 115 named, independently addressable closed-manifold components with structural, impact, brace, foot, cap, socket, plate, and visual-fastener roles. Final dimensions 3.620000×0.668000×3.000000 m; clear opening 2.35×2.48 m. FBX re-import preserves 115 manifold objects, four material IDs, names and dimensions; GLB preserves object/material/dimension parity. Determinism rerun matched source hash, dimensions, clearance and all component statistics. Strict cardinal vision passed after fastener/socket-plate detail repair. Hidden raw Meshy shell remains in BLEND provenance only. Artifacts `assets/source/meshy/e0_arena_threshold/assembled_001/`; assembler `e59167b9d6b080b87773c941cdff10a351d8bc2aa89403616cab0234529352da`; FBX `d81977868564f4c9682a57da0141646e909e861c12edd7c36613553ff3f23ee5`; GLB `faf1fb3bc4822361fc8cb4cd156e022635abe6ae8e03b7076b26dd394678c0de`; manifest schema 17/17 pass.
- **W0-ASM-002 — Accepted component-first W0 source weapon.** Baked each FBX `matrix_world` into mesh coordinates before direct metric scaling; assembled nine independent components with explicit material IDs and mechanical interfaces. Final dimensions 0.280000 × 0.050000 × 1.549825 m. FBX re-import: nine named closed-manifold components, zero boundary/nonmanifold edges, material and dimension parity. GLB re-import: nine object/material/dimension parity (seam-split topology is non-authoritative). Determinism rerun matched source hashes, dimensions and complete component statistics. Strict cardinal and hilt vision passed after collar seating repair. Artifacts: `assets/source/meshy/w0_sword/assembled_001/`; assembler SHA-256 `bc79cfe34858ed66453d7304d8b5f393c7e5b1d88264bec2d64a52a737e1ebff`; FBX `01705aabb18c55686e701eeb0d57db30aba3021a8d05514834993a5fef723cba`; GLB `5ab0ba05dc321fc759c4ea02100ec843d85cd5b36faa1fb1a0a3ef46252053e4`; manifest schema 16/16 pass.
- **MESHY-R1 — Delegated-doc cleanup.** Reconciled the late five-agent Meshy batch against direct measured trials and current official docs. Removed duplicated playbooks containing wrong remesh cost, seed-reuse claims, unsafe printability/Creative-Lab recommendations, fallback/model-degradation strategies, and the unexercised rig validator. Deleted all five files from unauthorized commit `66e9b03` in the working tree, including guessed `meshy_to_canonical_bones.json`; retained the verified `meshy-api-integration` skill, repo pipeline/schema, and measured candidate manifests.
- **ANAT-R1 — Frontier anatomy/control research reconciliation.** Preserved the combat-control source inventory as non-canonical research leads; replaced conflicting anatomy-source drafts with `docs/research/ANATOMICAL_ASSET_SOURCE_STRATEGY_2026.md`; verified current official BodyParts3D CC BY 4.0 (2025-02-27), NLM Visible Human public-domain status, task-specific TotalSegmentator usage terms, Z-Anatomy/Open3DModel share-alike status, and SKEL non-commercial status. Rewrote `just-dodge-deterministic-injury-architecture` v2.0 to enforce the locked 500–1,000-structure full-deformable truth, sparse activation, multirate solvers, provenance and no dependency/fallback shortcuts. Deleted the delegated 6–8-region architecture and stale licensing reports.
- **MESHY-002 — First measured Meshy-6 component batch.** Cached/reconciled 31 official docs (489,785 characters); corrected `meshy-api-integration`; added repo pipeline/schema; generated 15 manifested Meshy tasks (280 task-reported credits) plus one rejected 9-credit 2D task; validated FBX files with Blender/bmesh and strict vision. Accepted geometry: right hand, W0 blade, W0 guard, E0 threshold (conditional component split). Rejected/conditional evidence is preserved for bodies, foot, whole swords, grip, pommel, and two E0 retextures. No current runtime asset was replaced.
- **JD-001 — Deterministic fixed-step truth timing.** Added exact `CombatTruth::tick()`, integer fixed-step accumulator with retained bounded catch-up, per-tick replay recording, renderer-start clock reset, and 30/60/144/240 Hz hash-equivalence regression. Focused tests passed; `cargo check --all-targets --locked` and full `cargo test --all-targets --locked` passed (89 tests). Runtime smoke initialized renderer/UI and emitted 20,295 telemetry rows without panic. Pre-existing global `cargo fmt --check` and `clippy -D warnings` debt remained unchanged in the touched scope.
  - *Validation Executed:* RED `/tmp/jd001_red.log`; fixed-step 2/2 and truth 4/4 focused tests; `cargo check --all-targets --locked` pass; `cargo test --all-targets --locked` 89 executions/0 failures; debug build and Xvfb runtime pass; cross-rate ordered hashes identical at 30/60/144/240 Hz. Pre-existing format/clippy debt unchanged (`FMT_RC=1`, `CLIPPY_RC=101`).
  - *Independent review reconciliation:* startup-delay complaint rejected—the explicit renderer-start reset intentionally discards non-simulation initialization wall time and prevents machine-speed-dependent starting frames. Public `tick()` contract concern is API hardening, not a demonstrated determinism bug. Catch-up arithmetic retains debt and is safe in realistic durations. One valid pre-existing defect remains: multiple catch-up ticks can advance and record after `match_over`; tracked as JD-001A.


## 2026-07-11 ledger compaction — archived completed units
- **PHYS-WORLD-001 — Accepted deterministic external-target world boundary.** Added `src/duel_world.rs` and registered it in `src/main.rs`. `DuelWorld` accepts only explicit world-space weapon transforms plus externally supplied physical body proxies, retains prior weapon state for CCD, validates all transforms/proxies before a substep advances, evaluates exactly two 120 Hz targets as one 60 Hz interval, and emits the existing `PhysicalContactBatch`. It does not generate target motion or accept renderer skinning/generated animation as physics evidence. Tests prove tunnelling contact, explicit whiff, non-finite-input rejection without clock advancement, target determinism, and actual `CombatTruth` admission during Resolve. Evidence: `cargo test --bin just-dodge duel_world::tests` 5/5; `duel_physics::tests` 5/5; `truth::tests` 5/5; targeted rustfmt and diff checks pass. One initial test assertion used `Result` equality against a non-`PartialEq` success type; repaired once by asserting the error directly.

- Ledger compaction: archived 23 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.


## 2026-07-11 ledger compaction — archived completed units
- **PHYS-SEMANTICS-002 — Accepted role-tagged physical classification.** `HitboxProxy` now carries stable `Body|WeaponEdge|WeaponGuard` roles. `DuelWorld` accepts externally supplied guard proxies; `SharedDuelPhysics` validates them, retains attacker/defender roles on every bilateral candidate, and makes same-time guard contact outrank body contact when producing a truth packet. `truth::ContactGeometry` preserves measured attacker side and `Body|Guard` surface; `action_matrix::resolve` derives Whiff from absent contact, Hit from body contact, and Beat from guard contact, with the winner from the measured attacker/defender rather than a table label. Action-pair data remains only force/location configuration. Evidence: `duel_physics::tests` 4/4, `action_matrix::tests` 6/6, `duel_world::tests` 5/5, `truth::tests` 5/5, `hitbox::tests` 7/7; `rustfmt --check` for rewritten/new semantic modules and `git diff --check` pass. Initial test compilation exposed 11 schema-propagation errors after adding role/contact fields; repaired once by updating all explicit fixtures and target constructors.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.


## 2026-07-11 ledger compaction — archived completed units
- **SLICE-CONTRACT-003 — Accepted locked first playable action contract.** `src/combat.rs` now owns the single typed `Longsword / Top / {Thrust, Block, Dodge}` registry. Plan-phase truth rejects out-of-slice actions and stances; player input maps `1/2/3` and left-click to Thrust/Block/Dodge; UI presents exactly those actions and a Top-stance prompt; AI selects only those actions and Top. Broader `Strike`/`Grab` and side-stance source/motion data remains retained as non-selectable evidence. Tests: `combat::tests` 7/7, `input::tests` 3/3, `ai::tests` 5/5, `truth::tests` 6/6, and fixed-step render-rate hash gate 1/1. `rustfmt --check` passed for authored slice files and `git diff --check` passed. Existing warnings in `motion.rs`/`retarget.rs` are pre-existing debt.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.


## 2026-07-11 ledger compaction — archived completed units
- **CLEANBOX-RUNTIME-004 — Accepted real runtime physical submission.** Added `src/cleanbox.rs`, a deterministic kinematic collision target producer for the locked `Longsword / Top / {Thrust, Block, Dodge}` slice. It supplies only weapon-edge transforms, Block weapon-guard proxies, and torso body proxies; it never reads C0 skinning, renderer matrices, or generated motion. `App::combat_update` now invokes it immediately before every unresolved Resolve tick, where it clears only prior-exchange weapon history, evaluates exactly two 120 Hz `DuelWorld` substeps, and submits the resulting physical packet before `CombatTruth::tick()`. Direct tests prove Thrust→Block produces a guard/Beat packet, Thrust→Dodge produces an explicit Whiff, duplicate inputs reproduce packet and truth hash, and the runtime-order render-rate hash gate remains stable at 30/60/144/240 Hz. Evidence: `cargo check --bin just-dodge`; cleanbox 3/3, `duel_world` 5/5, `duel_physics` 4/4, truth 6/6, fixed-step 1/1; `rustfmt --check src/cleanbox.rs`; `git diff --check`. One compile repair changed the pure dodge-offset helper from `const fn` because derived `PartialEq` is not const-callable. New-module cargo-check warnings: none.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.

## Archived after FIRST-PERSON-CAMERA-008
- **EPISODE-EVIDENCE-005 — Accepted compact resolved-packet replay evidence.** `src/replay.rs` now appends a backward-compatible `ResolvePacket` event containing pre-Resolve selected actions, the exact two consecutive 120 Hz physics ticks and shared action tick, every canonical role-tagged contact/TOI, explicit Whiff/Guard/Body outcome, quantized health/stamina deltas, and the post-Resolve truth hash. `App::combat_update` emits this event only after `CombatTruth::tick()` admits the same packet, before recording the matching frame hash. A runtime-order replay simulation proves exactly one receipt at every deterministic 3-second Thrust-vs-Block exchange and equal receipts/hashes at 30/60/144/240 Hz. Evidence: `cargo check --bin just-dodge`; replay 6/6; fixed-step 1/1; cleanbox 3/3; truth 6/6; `rustfmt --check src/replay.rs`; `git diff --check`. Initial test used a non-existent `Vec::single`; repaired once with explicit cardinality plus indexed event matching. New-module cargo-check warnings: none.

## Ledger compaction 2026-07-11
- **EPISODE-REDUCER-006 — Accepted independent replay receipt reducer.** Added the read-only `just-dodge --reduce-replay <path.jdrp>` path. It bypasses WGPU/assets, loads the native container, validates snapshots and every `ResolvePacket`, and prints a stable result table plus `verdict=PASS`. It rejects missing/duplicate snapshot or Resolve frames, frame/hash mismatches, nonconsecutive physics ticks, mismatched action ticks, contacts outside the two recorded ticks, non-finite/out-of-range TOI, empty actions, and Whiff/Guard/Body role contradictions. No replay bytes or truth state are modified. Evidence: reducer Guard/Whiff + malformed-invariant tests, replay 8/8, fixed-step 1/1; actual binary reduced `/tmp/just_dodge_resolve_receipt_a.jdrp` to one Guard packet with `verdict=PASS`; `rustfmt --check src/replay.rs`, `git diff --check`. No new dependencies or reducer-source cargo-check warnings.

- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.

- **EPISODE-CAPTURE-007 — Accepted reducer-driven Resolve contact sheet.** `src/replay.rs` now emits canonical `contact frame action_tick physics_tick toi attacker_role defender_role` rows after the validated packet table. New stdlib-only `tools/qa/render_replay_contact_sheet.py` invokes the native reducer, rejects invalid/mismatched row counts, renders a timeline SVG, and writes a provenance receipt containing the replay/report/SVG SHA-256 values. The sheet explicitly states that markers are reducer receipt evidence, not rendered poses. Evidence: replay 8/8, fixed-step 1/1, Python compile, hash/count receipt verification; fresh source replay hash `f3cd3cc1bc0059ee5589bd25951f5cb4f6f1bcb478a0374fccb0838120392ec6`; repaired sheet at `qa_runs/episode_capture_20260711T233234Z_repaired/contact_sheet.png`, receipt hash `908c6891488dd73376dec20b5031ccdd7b2b474b7de13abe0ff2d3f800a42528`; vision inspection passed after expanding the card and adding the caveat (all labels/markers/roles readable, no clipping/overlap, no rendered-pose implication). `rustfmt --check src/replay.rs` and `git diff --check` passed. One verification-only threshold was corrected because the valid 312-byte text report was intentionally smaller than the initial arbitrary 1 KB threshold; no source behavior changed.
- Ledger compaction: archived 1 older completed units to `.hermes/atomic_ledger_archive.md`; retained the latest three completed units.

## Archived on auto-compaction (up to **FIRST-PERSON-CAMERA-008 — Accepted deterministic player-head baseline.** Replaced orbital theta/phi/radius with a 70° camera at authoritative player root + 1.62m. Mouse controls presentation-only yaw/pitch, clamps pitch at ±80°, and cannot mutate truth, cleanbox, replay, or movement. Left-click now routes Thrust rather than camera drag; scroll no longer zooms. First-person runtime omits the camera owner carrier to prevent self/head occlusion. `src/bin/shot.rs` now renders the same first-person-duel exclusion; the inspected capture `qa_runs/bind_pose_1783814932/jd_bind_first_person_duel.png` shows the opponent without self-occlusion. It also establishes the concrete remaining visual deficit)

- **M3-HYGIENE-ARMOR-005 — Packet-truth and armored-duelist integration commit.** Committed `9691ecb9bc523ac9d0edb0c9950cf947aa2a2146` (`feat: integrate packet-driven M3 and armored duelist`). The active M3 resolver consumes one measured `PhysicalContactBatch` from `M3CleanboxWorld` per resolve frame, records it in replay v2, and reconstructs a headless match to frame 342 / truth hash `d1a3cc1bfb9c2f67`. Replaced the nude 163-bone carrier with the 24-bone cooked `c0_armored_duelist_001` (82,928 vertices, 309,864 indices); source/cook hashes and source Meshy task IDs are in its manifest. The light-bronze fallback was raised to restore silhouette contrast while PBR remains a separate task. Fresh evidence: `RUSTFLAGS='-Dwarnings' cargo check --locked --all-targets`; repeated `RUSTFLAGS='-Dwarnings' cargo test --locked --all-targets` passed 79 library + 93 game-binary + 1 official-motion + 6 motion-service tests; `m3_match --autoplay 1` plus `--verify`; cooked-mesh verifier; two fresh `shot` frame dumps inspected by vision. QA output is now ignored rather than staged. One initial all-target run had a transient `all_top_primitives_are_present_and_rigid` failure; three focused repetitions plus the final complete rerun passed, so the test remains a tracked flake-investigation item rather than a source regression.
- **M3-CONTACT-CORE-002 — Packet-driven M3 resolution.** Removed the active M3 3×3 action lookup authority. Resolve now holds without a packet, consumes only an admitted packet, derives winner/injury from attacker/body evidence, and records last contact in the truth hash. Guard/whiff packets cause no injury. Replay schema v2 records submitted physical packets and replay refuses missing Resolve evidence. Validation executed: `RUSTFLAGS='-Dwarnings' cargo test --locked --lib milestone3::tests -- --nocapture` passed 8/8; log `qa_runs/m3_contact_truth_001/contact_core_tests.log`.
- **M3-CONTACT-SCHEMA-001 — Physical packet admission boundary.** Added typed `PhysicalContactBatch` / `PhysicalContact` / `ContactSurface` evidence to the active M3 authority. `Match` now accepts exactly one batch for its next Resolve frame and rejects non-Resolve, stale, and duplicate submissions. The packet is deliberately not yet consumed by the existing tactical resolver; that migration is M3-CONTACT-CORE-002. Validation executed: `RUSTFLAGS='-Dwarnings' cargo test --locked --lib milestone3::tests::physical_packet_admission_is_frame_exact_and_single_use -- --nocapture` passed 1/1; log `qa_runs/m3_contact_truth_001/contact_schema_focus.log`.
- **DODGE-PRESENTATION-RUNTIME-012 — Locked active source interval.** The admitted `ib_dodge_back_L_001__A437` stream now fails closed below 271 frames and maps the 80-tick Commit→Consequence timeline only over zero-based source frames `[137,271)`: tick 0 → 137 and terminal tick 79 → 270. Source evidence: 792-frame SHA-256 `d4a840f410d3dbd7db5486f453175eb34b8c7705498a777580d970b479f232b4`; source contact channels `[308:413]` are zero, dominant composite motion energy is at destination frames `138..=258`, and direct toe kinematics span left-airborne `[163,205)` / right-airborne `[191,262)`. Regressions prove exact endpoints, every tick in bounds, clamping, and short-stream rejection; Observe/Plan remain excluded by the existing phase mapper. Static scan confines interval symbols to `src/dodge_presentation.rs`; truth, cleanbox, duel physics/world, replay, and action choice are unchanged. Evidence: `cargo test --bin just-dodge dodge_presentation -- --nocapture` 4/4; `cargo test --bin just-dodge dodge_presentation_phase_tests -- --nocapture` 1/1; `rustfmt --edition 2024 --check src/dodge_presentation.rs`; `cargo fmt --check`; `git diff --check`; source-enabled `cargo run --bin shot` produced `qa_runs/bind_pose_1783858022/jd_bind_first_person_duel.png`. Vision: visible opponent mesh is connected with no skinning explosion or tear; first-person weapon composition partially limits lower-body inspection.
- **G1-C0-DODGE-CAL-011 — Accepted QA-only direct Dodge → C0 calibration.** Extended `tools/qa/validate_bones_seed_g1.py` to emit a deterministic ignored little-endian `[N,413]` frame stream (SHA-256 `d4a840f410d3dbd7db5486f453175eb34b8c7705498a777580d970b479f232b4`) alongside its provenance receipt. Added `src/bin/c0_bones_dodge_calibration_probe.rs`, which decodes that stream, uses only `asset::calibrated_g1_target_locals` and `reference_pose_skin_matrices`, CPU-skins all 792 direct-source frames, and rejects malformed/degenerate rotations, non-finite/inverted matrices, or source/C0 local-length drift. Results: 163 C0 bones, 14,517 vertices, determinant `[0.999995947, 1.000001192]`, axis scale `[0.999992669, 1.000007153]`, C0 local-length error `0m`, source segment drift `1.79e-7m`. Added `tools/qa/render_c0_calibration_sheet.py`; its initial default Blender OBJ axis conversion rendered an upside-down false failure, then a measured import-axis probe showed `forward_axis=Y, up_axis=Z` preserves the already converted `(X,-Z,Y)` data. The corrected rendered evidence `qa_runs/c0_bones_dodge_calibration_20260711/c0_dodge_contact_sheet.png` passed fresh vision: reference plus five ordered samples are upright, connected, non-exploding, non-tearing humanoids with no material shear or catastrophic silhouette deformation. No files under `src/main.rs`, renderer, truth, cleanbox, duel physics/world, injury, replay, or action selection refer to BONES-SEED, the motion ID, or the QA interchange. This proves only direct-source-to-C0 calibration; runtime playback and combat authority remain absent, and it does not prove Thrust/Block motion.
- **BONES-DODGE-SOURCE-010 — Accepted direct source-only dodge evidence.** Selected `ib_dodge_back_L_001__A437` solely from the authorized BONES-SEED provenance audit's explicit `combat/fight/martial` labels and exact indexed member `g1/csv/230731/ib_dodge_back_L_001__A437.csv`. The ignored local source CSV hashes to `ca884af3a5f790cafc1f959fb831c9ec4a0e7e3e95ae682ab5b9fa45e4c02507`; audit/index/license/archive linkage is captured in the ignored validation receipt. Added `tools/qa/validate_bones_seed_g1.py`, which fails closed on metadata/index mismatch, CSV schema/frame discontinuity, non-finite qpos, root scale, G1 discontinuity, or segment drift. It uses NVIDIA's `mujoco_qpos_converter`, not reconstructed FK. Result: 792 frames, root path 1.443492m, maximum root step 0.013962m/frame, maximum joint step 0.033349m/frame, maximum segment drift 1.79e-7m. Vision passed on `bones-seed/derived/ib_dodge_back_L_001__A437.keyframes.png`: stable connected anatomy and readable backward/defensive dodge. It remains ignored local evidence only; no runtime, retarget, collision, cleanbox, truth, injury, replay, or result path reads it. The source does not establish weapon-based Thrust or Block semantics.
- **MB-GENERATION-SOURCE-001 — Accepted official navigation-source boundary.** Re-homed the 2.4 GB canonical MotionBricks checkout from failing Bulk-SSD to `/run/media/vdubrov/NVMe-Storage1/gr00t/motionbricks`; `rsync --checksum` confirmed an exact copy. Critical source/checkpoint hashes matched: `interactive_demo_g1.py` `e04f194d…83d2`, `full_agent.py` `a3d5099a…846d`, and `G1-clip.ckpt` `84afc7c2…8df`. `init_official_service()` imported `motionbricks.motion_backbone.demo.full_agent.full_navigation_agent` and `random_controller` only from NVMe. Removed the adapter's non-upstream root-lock so its 36-frame output retains official root movement: deterministic payload `503f8073304dd6f76b7d6dc74b022f9a5cf1dd875b7c87a3a7f2e7869f77453b`, root path 0.2704m, maximum joint step 0.076645m/frame, maximum segment drift 1.8e-7m. New `tools/qa/visual_verify_official_navigation.py` fails closed on topology/continuity and produced `qa_runs/official_navigation_nvme_20260711/official_navigation_keyframes.png`; vision confirmed a coherent connected G1 locomotion skeleton with no collapse/teleport. The adapter and QA explicitly classify this output as navigation/locomotion only—not Thrust, Block, or Dodge semantics—and nothing is promoted to runtime/truth. Evidence: focused official service integration + C0 calibration tests passed; Python compile, cargo checks, and diff checks passed.
- **FIRST-PERSON-WEAPON-009 — Accepted source-provenance W0 presentation weapon.** Added `tools/cook_w0_sword.py`, which verifies the accepted assembly FBX SHA-256 `01705aabb18c55686e701eeb0d57db30aba3021a8d05514834993a5fef723cba`, requires all nine assembled components, preserves the declared metric bounds, and emits `assets/weapons/w0_sword_assembled.bin`. Two cooks produced identical SHA-256 `e1ba7788cafcb2a156ff2bcdcda4d24f1811c8d50fc2a9a7a5d18ac77919831d`. The renderer draws this source mesh from a pure camera-relative transform; it neither reads nor writes truth, cleanbox, physics, injury, replay, or action state. `shot` uses the exact 70° first-person projection and camera-owner exclusion. Vision passed on `qa_runs/bind_pose_1783816800/jd_bind_first_person_duel.png`: hilt, guard, grip, and blade are visible in lower-right, opponent remains visible, and no self-carrier occlusion. Remaining separate gate: W0 is static presentation, not source-valid action motion or collision authority. Evidence: cooker bounds/component/layout checks, renderer transform test, fixed-step 4/4, cargo checks for both binaries, rustfmt, and diff checks passed.
- **FIRST-PERSON-CAMERA-008 — Accepted deterministic player-head baseline.** Replaced orbital theta/phi/radius with a 70° camera at authoritative player root + 1.62m. Mouse controls presentation-only yaw/pitch, clamps pitch at ±80°, and cannot mutate truth, cleanbox, replay, or movement. Left-click now routes Thrust rather than camera drag; scroll no longer zooms. First-person runtime omits the camera owner carrier to prevent self/head occlusion. `src/bin/shot.rs` now renders the same first-person-duel exclusion; the inspected capture `qa_runs/bind_pose_1783814932/jd_bind_first_person_duel.png` shows the opponent without self-occlusion. It also establishes the concrete remaining visual deficit: the C0 carrier is unarmed and T-pose, so visible weapon/guard and action motion remain a separate source-valid unit. Evidence: focused camera + fixed-step 4/4, both binary checks, formatting, and diff checks passed. One harness-only compile repair used a common slice branch instead of incompatible `Skip`/`Take` iterator branch types.

## Archived on auto-compaction (oldest M3-REPORTS-006)

- **M3-REPORTS-006 — Status reconciliation and final phase-A verifier.** Rewrote the current-state audit, M3 report, build record, asset audit, README, milestone status, changelog, and task list against `9691ecb9bc523ac9d0edb0c9950cf947aa2a2146`; added `E.8 QA-MOTION-FLAKE` after one transient primitive-rigidity failure. The final temporary script `/tmp/hermes-verify-final-pm371l_i.sh` was removed after `AD_HOC_FINAL_PASS`; its retained local log SHA-256 is `21088f7e33bb54b6e9fc3f110c050c1de9dd8256a52d5f387aa410bec75feebb`. It passed warning-denying all-target compilation, 79 library tests, packet-driven autoplay/replay at `d1a3cc1bfb9c2f67`, fresh shot output, formatting, diff check, and report-receipt assertions. M3 remains blocked on action motion, pose-derived contact, real player evidence/media, PBR, and rights closure.

## Archived on auto-compaction (oldest PVP-001-ACTIVE-PATH-RECONCILIATION)

- **PVP-002-CLEAN-CHECKOUT-GATES — Passed on Repair 1 without runtime semantic drift.** Tracked `Cargo.lock`; removed two rustfmt ordering failures and 40 warning-denying clippy diagnostics through mechanical source cleanup; fmt, clippy, check, shellcheck, and diff checks pass. The first true isolated all-target run compiled but failed 1/116 library tests because ignored MotionBricks ONNX files were absent. Repair 1 added `assets/motionbricks_runtime.sha256` and `tools/hydrate_motionbricks_runtime.sh`: 13 source files are verified before copy, staged atomically, and reverified at the destination; no unpinned download or fallback exists. A hydrated isolated checkout passed 116 library + 115 game-binary + two live integrations (233 total). Release autoplay produced 100 replay records; match 0 remained frame 342 / `d1a3cc1bfb9c2f67` and independently verified to 343 states. The preliminary `/tmp` checkout failure was verifier-environment disk pressure (3.1 GB free for a 3.3 GB tracked tree), so the cleanbox was rerun on the project volume. Player-visible change: none. Next: PVP-003 complete runtime flow.
- **PVP-001-ACTIVE-PATH-RECONCILIATION — Passed after one verifier-only repair, with no runtime change.** Reconciled the ledger, README, changelog, implementation plan, current-state audit, development task list, and M3 report against clean `main` revision `2677b4a7dd050e7f4c5ee03881aa16035e413a8b`. Selected and documented `main::App -> milestone3::Session/Match -> m3_cleanbox -> cleanbox/duel_world/duel_physics -> PhysicalContactBatch -> immutable milestone3::Snapshot -> renderer/UI/replay`; preserved supporting and isolated modules rather than deleting unproven scaffolds. Fresh source hashes remained byte-identical (`main.rs` `3f68b075…a4c2`, `milestone3.rs` `6eb905e7…bbc4`, `m3_cleanbox.rs` `a5a6045c…708`). Focused M3 tests passed 8/8 including 100 reconstructions; `git diff --check` and stale-reference/content assertions passed; canonical media failed closed for exactly the three declared missing artifacts. The initial verifier used an overly specific prose substring and continued after its assertion because the wrapper lacked `set -e`; Repair 1 used `set -euo pipefail`, corrected only the assertion, and passed. Evidence log `/tmp/just-dodge-pvp001-verify-repair1.log` SHA-256 `d4f1f16f76b9937f119cc62af3e48b9cb2d50eeddde1389d97c4d1ee3e7d4e9b`. Player-visible change: none; documentation/authority reconciliation only. Rollback: not required. Next: PVP-002 clean-checkout gates.

## Archived on auto-compaction (oldest PVP-003-COMPLETE-RUNTIME-FLOW)

- **PVP-003-COMPLETE-RUNTIME-FLOW — Passed on Repair 2 without combat-truth drift.** Added a truth-isolated `RuntimeFlow` around `milestone3::Session`: Menu and Establishing cannot advance authoritative frames; duel stages retain the existing session; Result releases the cursor; validated Replay reconstructs every recorded frame through the same cleanbox/contact/hash path without mutating the terminal live session; rematch, menu return, and quit are explicit. Repair 1 grouped UI inputs after warning-denying clippy rejected a nine-argument render call. Repair 2 used the actual Result capture to remove Plan/Reveal competition and improve terminal legibility. Warning-denying fmt/clippy and all 237 tests pass (119 library, 116 binary, two integrations). Release autoplay produced 100 matches; match 0 remains frame 342 / `d1a3cc1bfb9c2f67`, and independent verification reconstructs 343 states. Local release captures hash to Menu `5e3b008e…e9379f` and Result `0d899d5e…4b9fb`; runtime logs prove cursor captured in duel and released at Result. Replay-key screenshot automation was unavailable because this Wayland compositor exposes no virtual-keyboard protocol, so real keyboard/mouse evidence remains PVP-004. Player-visible change: complete mechanical outer flow and clearer terminal UI. Next: PVP-004 packaged interaction/cadence proof.

## Archived on auto-compaction (oldest PVP-004-PACKAGED-INTERACTION-CADENCE)

- **W0-GRIP-CAMERA-001 — Passed on Repair 1 as a contract-only unit.** Added `assets/qa/pvp005_w0_grip_camera_v1.json` and `tools/qa/validate_pvp005_w0_grip_camera.py`. Strike/Block own W0 with two active sockets; Grab owns it one-handed in a visible right exterior guard while the left hand is explicitly a grab effector, not a false grip. The validator parses the real rigid mesh, verifies source hash/bounds and 160 mm socket spacing, projects the declared complete actor/weapon envelope through all 16 fixed orbit cameras and the fixed first-person QA camera, preserves zero edge pixels as the crop threshold, and binds the known failed receipt. Repair 1 expanded the envelope and fixed cameras after the first ARDY endpoint falsifier showed the initial envelope omitted a complete forward blade. Output: `PVP005_W0_GRIP_CAMERA=PASS_CONTRACT_ONLY`, `PVP005_CANDIDATE_GENERATION=false`, `PLAYABLE_PROOF=false`; contract SHA-256 `f9b2603b479a8635ab9d8169a0f9ae49e33b91a4c9429562b98a8d879c99d020`. No renderer, truth, motion payload, or candidate output changed.
- **FORGELENS-PHASE-A-001 — Passed as a blocked readiness contract, not visual acceptance.** Added `docs/reports/FORGELENS_PHASE_A_READINESS_CONTRACT.json` and `tools/qa/verify_forgelens_phase_a.py`, pinned to exact subject revision `3a88f4a79b6f8d08941ffe8da22b4985b32e28e1`. The contract binds ForgeLens source hashes and browser authority; preserves ARDY proposal-only and deterministic-physics-only authority; forbids new candidate generation, runtime promotion, and visual acceptance; and records every downstream gate as blocked where evidence is absent or failing. Validator output: `FORGELENS_PHASE_A_CONTRACT=PASS_BLOCKED_PENDING_EVIDENCE`, `PLAYABLE_PROOF=false`. Existing ForgeLens verification passes 22/22 Python tests, Python compilation, Node syntax, and diff checks. No candidate motion or acceptance artifact was generated.
- **CI-RUSTFMT-001 — Passed locally after the second environment repair; remote check pending; PLAYABLE-PROOF remains false.** CI fetches complete history and installs pinned Rust 1.96.0 rustfmt/clippy. Exact baseline `3a88f4a` captured stale feature-baseline prose and missing binary `neural_plan` failures; the source repair preserved feature-baseline `4e481ccd…` and harness `d84950f…` identities and declared the existing neural-plan module. Attached checks then exposed two clean-runner prerequisites: the separately hydrated 13-file MotionBricks artifact bundle, followed by the separately provisioned NumPy/Torch/MotionBricks Python bridge. Generic CI now reports the three positive environment-bound tests as explicit ignores rather than false failures. Canonical `tools/verify_playable_repo.sh` hash-verifies all 13 hydrated files and executes all three exact ignored tests; missing artifacts or bridge failures still fail closed. Local generic suite passed 124 library tests with one ignore, 122 binary tests, two capture tests, and two explicit integration ignores; canonical exact hydrated/runtime/C0/service tests each passed 1/1. PR #1 is `https://github.com/vladdiethecoder/just-dodge/pull/1`.
- **FL-ID-REVIEW-004 — Passed after three RED falsifiers, no repair failure.** Current source had already closed six delayed-review findings: guarded stale archives, strict schema-v1 preservation/ineligibility, stable content version IDs across unrelated HEAD changes, canonical path rejection, stale task-plan HTTP 409, and pre-replace artifact rechecks. Three remaining defects reproduced independently: forged receipt IDs survived when the content digest matched; an asset replacement immediately after the last pre-replace check left a stale review slot; and animated approval accepted an unpersisted or cross-bound neural `pass`. Repairs now require receipt IDs to equal the content-hash prefix, perform artifact/evidence checks immediately before and after review replacement with fail-closed rollback/archive, and require a passing neural audit to resolve to exact content-addressed PNG bytes plus a schema-v2 receipt bound to current asset fingerprint, clip, path, digest, byte count, and review class. Three RED tests failed before repair and passed after; the warning-strict focused set passes 22/22, including forged receipt, post-check replacement, missing/tampered neural receipt, prior archive/schema/path/version/409/auth/replay gates. Python compile/tabnanny, JS syntax, and diff checks pass. Final source SHA-256: server `d0196f243ee5d35c1f35c406f25beb279d8d609c72ce1084326bc8649d681e71`, tests `ba856044ec4b3f7683cf4a7ab69955d379bb2ec6a6a8aec800c5e8a7d7cd1b11`; tool profile `541f9b7847d155b0a8294230e6b1e6806480ff0de6673f3bdb45b2fb9116646f`. Replay truth remains 343 frames / `d1a3cc1bfb9c2f67`; new workflow-bound run ID `14e27b37c03d3f8b4b2d`. Fresh OS-tempfile verifier emitted `AD_HOC_VERIFY=PASS` with 22 focused tests and three explicit delayed-review regressions; log `qa_runs/asset_reviews/verification/20260715T015149Z/ad_hoc_verify.log` SHA-256 `6558652ef656444272e0b85c49c1e229038d901edabc6c3921365f9eb3bc6342`, rc `0`; temporary script SHA-256 `53e88c9db193f827990fae8966aa899d2f288ed79e623ef76e94152ea87ed1f3` and confirmed removed. No dependency, runtime, combat-truth, asset-source, commit, push, or PR change.
- **FL-REPLAY-003 — Passed without repair; no human acceptance claimed.** ForgeLens now accepts `--replay`, exact repository-relative `--replay-verifier`, and repeatable `--capture` inputs and constructs a schema-v1 replay `ReviewRun` bound to replay bytes/repository state, verifier executable bytes/repository state, verifier stdout hash and parsed frame/winner/truth hash, every capture byte/fingerprint, capture-set hash, and the ForgeLens tool-profile hash. Authenticated browser GET/POST endpoints expose and submit this workflow; CSRF and server-derived browser actor remain mandatory. A stale replay, verifier, capture, revision, or workflow changes the run fingerprint and causes HTTP 409; truth-only runs cannot be approved as presentation evidence; immutable receipts are stored per run/receipt without overwriting earlier decisions. The browser Details surface shows run/replay/verifier hashes, 343-frame result, winner, truth hash, capture status and explicit `operator-declared; derivation not machine-proven` lineage, capture image, decision, summary, and receipt. Warning-strict focused tests pass 20/20, including exact run stability, stale rejection, capture-required approval, immutable repeated receipt, authenticated replay endpoints, and all prior identity/auth gates; Python compile/tabnanny, JS syntax, tracked/untracked whitespace checks, and direct `m3_match --verify` pass. Actual run `dc15c5f960c149f8e28d` binds fingerprint `dc15c5f960c149f8e28d8093794775d55338a3c7f123b9a85acc9e199c382617`, replay SHA `9f15f206ba368ff1f828cb63b6f41529df531456ee3a8b23adca7abc8586096a`, verifier SHA `27c10aa064f722a7a223cc88895028c4e75119ca61eeae4c81b26735e76ccfe8`, capture SHA `4d4b881551d48ddca9b007dc89e48fdb70b2c5ccc6decb272cc92ab6454ce877`, and truth hash `d1a3cc1bfb9c2f67`; regular-browser logs prove authenticated replay-run and capture loads. Neural pixel inspection—not human authority—finds the Replay label/HUD and opponent plus first-person sword visible, while the opponent remains in bind/T-pose and the footer labels overlap. No human replay receipt was created and no visual acceptance is claimed. Final tool-profile SHA `1c3a127852d8755b190887711c7c83223a8294a4458542414476cf269caebb35`; source hashes: server `a266b47c654efa3f6900364d6a6eb79cf63e30a155b124491c1f649658a2d362`, app `2307998eea3ba71b75554cc2be80749a20c0a8c8cc920d1f3f835a4c4dad67e8`, index `2448bdd2d0e9f1143b9b8026607481bc50e20217927c1e6c71e3aba578d94ccc`, tests `dadadf346195e49ea3b03eff1e30526a2b9c264f2f095ebc408db4eb438a797b`. No dependency, Cargo/runtime/truth/asset source, commit, push, or PR change.
- **FL-HUMAN-002 — Passed without repair.** ForgeLens now generates an unlogged one-use bootstrap credential, exchanges it only on the bound `127.0.0.1:<port>` Host for a four-hour HttpOnly/SameSite=Strict session cookie, strips the credential from the redirect, redacts it from request logs, exposes CSRF only through the authenticated same-origin session endpoint, and requires exact Host, Origin, cookie, and CSRF on every mutation. The browser transports CSRF through one API wrapper; the server derives comment authors, report submitter, and neural-capture actor, rejects direct unauthenticated access, and strips forged client receipts. Legacy schema-v2 `submittedBy="human"` receipts are archived byte-for-byte and require authenticated resubmission. Warning-strict tests pass 19/19, including bootstrap replay, expiry, wrong CSRF, forged author/receipt stripping, direct POST rejection, nominal-receipt migration, stale 409s, and authenticated report submission; Python compile/tabnanny, JS syntax, and whitespace gates pass. A generated-token launch opened the user's regular browser without printing token material: bootstrap 303, clean page, session/catalog/model/review GETs all succeeded; unauthenticated live probes returned GET 401 and POST 403. A real regular-browser report was then persisted for `c0_base_fighter/assembled_001` with server-derived `human-browser-*` actor, receipt `8ebdda0ec628096096ba`, artifact version `89507ef746ca081104ec`, proving the full authority path. Final tool-profile SHA-256 `8753066bd12bc7a5b8a858a957b2ab90ab23e1fbf6bf731a31dd70eec34bf816`; source hashes: server `b7f231076b0d51bfc7a9a99f20815c79c79a106d13fb7a876f939e8ca2da782d`, app `ba0374616b6661c670433210c77799b6f1ec87cc2714f48357e69fcaa25616fd`, tests `fab2652caf93f531c63bcc29e6192606c1cf4de08e335221979177264adfdf29`. No dependency, Cargo/runtime/truth/asset-source, commit, push, or PR change.
- **FL-ID-001 — Passed after one return-shape repair and concurrent-edit reconciliation.** ForgeLens schema v2 now gives every repository GLB a content-stable `versionId` plus a canonical fingerprint over logical path/id, SHA-256, byte count, reachable HEAD, relevant tracked/staged/untracked path state, exact tool-profile SHA-256, capture profile, and parent evidence hashes. Same bytes retain their version across unrelated reachable heads while the exact review fingerprint changes; same-path changed bytes produce a new pending version. Schema-v1 and changed-version reviews move byte-for-byte under `qa_runs/asset_reviews/superseded/`; stale review/report/report-plan writes return HTTP 409; report receipts and neural evidence bind the claimed artifact; `_write` rechecks identity immediately before replace. Fresh warning-strict focused tests pass 14/14, including staged diff measurement, schema-v1 preservation, stale neural claims, and 409s for all mutation routes. Python compile/tabnanny, JS syntax, tracked and untracked whitespace checks pass. Real catalog probe passed five assets twice at HEAD `3a88f4a79b6f8d08941ffe8da22b4985b32e28e1`, tool-profile SHA-256 `0b67db96dd402ba53cd32726ea190020ba573079c772f816ab3869d78f01b36b`; live loopback returned schema v2 and the responsive ForgeLens surface rendered its populated library, WebGL model, and below-fold inspector without JS errors. Source hashes: `asset_review.py` `f6b4c33bf114442f6f990d2826f7ad39ed94c4db770efba5d23b696a409f5733`, `app.js` `557a51b9b2c83082ac838d50350c0d72908f96a2403f48808308d139acf147d8`, tests `7e38fe4a333e8b98e2bfe8eb6987e7184ba618c47a0c95c3a82ae5c3509459d1`. No Cargo/runtime/truth/asset source, dependency, commit, push, or PR change.
- **PVP-004-PACKAGED-INTERACTION-CADENCE — Passed on Repair 2 for local technical distribution and automated OS input, not public release or human play.** Runtime asset loading now uses the configured package root instead of a compile-time workspace path. `package_release.sh` stages two executables plus eight live assets, explicit boundary docs, launcher/build info, and a complete checksum manifest; `verify_release_package.sh` rejects missing/extra/symlink/unsafe-path payloads and reconstructs terminal replay truth; `verify_playable_repo.sh` runs fmt, warning-denying clippy, all 237 tests, two assemblies, both package verifiers, and byte-for-byte comparison. Both 184,144,387-byte assemblies are identical; the 13-payload manifest hashes to `82042b7d…53944`; aggregate log hashes to `4f21e82e…dbebf`. Launch from `/tmp` reached Result and produced the same `0d899d5e…4b9fb` frame. A temporary user-owned uinput daemon then drove Start, relative mouse motion, action select/confirm, Result, Replay, Rematch, Menu, and Quit through the packaged launcher; app log `9e8d65fe…ba503` records transitions/cursor ownership and the process exits 0. The Replay frame `4d4b8815…ce877` proves replay in-frame and exposes overlapping Replay/Plan footer text plus the known bind-pose opponent; both remain downstream presentation/motion blockers. Repair 1 followed an initial no-op patch context mismatch. Repair 2 moved checksum output outside the enumerated staging tree after shellcheck identified a possible self-read. No credits or network generation used. Next: PVP-005 admitted three-action motion.

## 2026-07-15 ledger compaction — archived completed units
- **ARDY-ACTION-CONSTRAINTS-002 — Passed without repair using the endpoint-first method; design only.** Added `assets/qa/pvp005_ardy_action_endpoints_v4.json` and `tools/qa/validate_pvp005_ardy_action_endpoints_v4.py`. Strike has left support with an explicit right step and diagonal-cut endpoints; Block has stationary root, bilateral support, and horizontal cross-body endpoints; Grab has left support, explicit right step, one-hand low exterior weapon endpoints, and independent left-hand reach. Every root/contact schedule is contiguous and support-feasible; the first eight source frames keep root XZ exact; complete 1.549824834 m W0 pommel/tip endpoints, derived 160 mm grips, and grab effector remain inside the W0 camera envelope; early keyposes are pairwise separated. The current generator does not accept this v4 schema, screen thresholds remain 20 mm/eight frames, and generation fields are absent. Output: `PVP005_ARDY_V4_ARCHITECTURE=PASS_ENDPOINT_FIRST_DESIGN_ONLY`, `PVP005_ARDY_V4_GENERATION=FORBIDDEN`, `PLAYABLE_PROOF=false`; contract SHA-256 `82d3f9a154127dc2e2dcd70450d99a52c4f9d89f13bfaf836ac2675aae0a6010`. No candidate motion, runtime, truth, or acceptance output changed.

## Archived on auto-compaction (oldest GRAB07-SEQ-CONCAT-001)

- SG02-MATCH-LIFECYCLE-DRAW-001: Extended the 120 Hz reducer and M3 contact packet to preserve one canonical contact per attacker, admitted bilateral contacts only in Player/Opponent order, applied both injuries in one 60 Hz Resolve tick, added explicit hashed draw state and mutual-hit evidence, and exposed Draw in Result UI.
  - *Validation Executed:* RED proved bilateral contact and draw state were unrepresentable. A measured cleanbox Strike/Strike path preserved both contacts; three measured bilateral exchanges produced mutual torso incapacitation, one frozen Draw terminal, and exact replay. Fresh ad-hoc verifier passed fmt, clippy all targets `-D warnings`, 415 tests / 3 ignored / 0 failed, no-default-features checks, and seven golden scenarios × 100 runs with Python verification and fail-closed tamper test.
- SG02-MATCH-LIFECYCLE-SESSION-RESTART-001: Special-cased terminal Restart in `Session::apply` so truth and replay reset atomically; the prior seed/events/contacts/hash trace are discarded and Restart is not recorded across match boundaries.
  - *Validation Executed:* RED proved the reset game retained the entire old replay plus a frame-0 Restart event. Fresh ad-hoc verifier passed fmt, clippy all targets `-D warnings`, 408 tests / 3 ignored / 0 failed, no-default-features checks, canonical replay equality/reconstruction, and seven golden scenarios × 100 runs with Python verification and fail-closed tamper test.
- SG02-MATCH-LIFECYCLE-RESET-001: Made rematch a fail-closed state-machine transition admitted only from Result/Replay, removed automatic Result bypass into fight-film, unified initial/reset construction for truth session + 120 Hz cleanbox + seeded AI, and asserted replay flags, fixed clock, input and camera reset at the live reset seam.
  - *Validation Executed:* RED compile regression proved rematch lacked snapshot/state validation. Fresh ad-hoc verifier passed fmt, clippy all targets `-D warnings`, 407 tests / 3 ignored / 0 failed, no-default-features checks, explicit terminal-once/replay/canonical-rematch tests, and seven golden scenarios × 100 runs with Python verification and fail-closed tamper test.
- SG02-TRUTH-HASH-WHIFF-FOLLOWUP-001: Added hidden whiff-cancel follow-up authority to canonical `PlanSnapshot` serialization and `PlanPhase::truth_hash`, eliminating a demonstrated collision between states with different legal future intents.
  - *Validation Executed:* RED regression produced identical `12564167652169898433` hashes despite different `intent_available` behavior. Fresh ad-hoc verifier passed fmt, clippy all targets `-D warnings`, 405 tests / 3 ignored / 0 failed, no-default-features checks, and seven golden scenarios × 100 runs with Python verification and fail-closed tamper test.
- SG02-MATCH-LIFECYCLE-PAUSE-001: Added live deterministic pause/resume as an outer-flow state, bound to `O`, with duel-only admission, released cursor capture, zero truth ticks while paused, exact-state resume, and fail-closed rejection from menu/countdown/result/replay.
  - *Validation Executed:* RED compile regression proved pause API/stage were absent. Fresh ad-hoc verifier passed 5 runtime-flow tests, 7 input tests, fmt, clippy all targets `-D warnings`, 404 tests / 3 ignored / 0 failed, no-default-features `just-dodge` + `m3_match`, and seven golden scenarios × 100 runs with Python verification and fail-closed tamper test.
- SG02-QUANTIZED-GRAB-CONTACT-SAMPLE-001: Replaced secure-grab contact-sample, interval-duration, and admission float authority with integer micrometre/microsecond fields and integer threshold comparisons.
  - *Validation Executed:* RED compile regression proved the old sample lacked `Eq` and integer fields. Fresh ad-hoc verifier passed fmt, clippy all targets `-D warnings`, 403 tests / 3 ignored / 0 failed, no-default-features `just-dodge` + `m3_match`, 3 SG02 reducer tests, schema/static scans, JSON validation, and diff checks. Source diff SHA-256: `934ae32b8437142d67763fb6932bd2f12271bd5d388e06176535d82747a24063`. Commit binding remains pending.
- SG02-QUANTIZED-SUBSTEP-CONTACT-PACKET-001: Replaced `SubstepTruthPacket` floating measurements with integer micrometre/cubic-micrometre fields, integer-derived overlap volume, deterministic invalid/overflow handling, serde truth schema, and derived human-readable capture mirrors.
  - *Validation Executed:* RED compile regression proved the old packet lacked `Eq` and canonical integer fields. Fresh ad-hoc verifier passed fmt, clippy all targets `-D warnings`, 401 tests / 3 ignored / 0 failed, no-default-features `just-dodge` + `m3_match`, 3 SG02 reducer tests, JSON validation, and diff checks. Source diff SHA-256: `a5ade14975526e6d6368a68f07bbbfe35805c98c3d07181e5d36231fe653051a`. Commit binding remains pending.
- SG02-NO-BAKED-RUNTIME-BOUNDARY-001: Isolated the baked provider, constructor, embedded assets, and provider-kind variant to Rust unit-test compilation; removed the integration test's public baked-provider dependency and stale runtime wording.
  - *Validation Executed:* RED baseline found baked-provider symbols in the production rlib. Focused ad-hoc verifier then passed unit/integration provider tests, `RUSTFLAGS=-Dwarnings` no-default-features checks, production rlib symbol scan, release `game_loop` symbol/payload scan, and binary-source scan. Canonical gates passed: fmt, clippy all targets with `-D warnings`, 399 tests passed / 3 ignored / 0 failed, no-default-features `just-dodge` + `m3_match`, and 3 SG02 reducer tests. Source diff SHA-256: `eb5d41241e87fd0b945d4bf41c5cb61f89fcf399d70a5c3880ff1436956d1446`. Commit binding remains pending.
- SG02-CROSS-PLATFORM-PARITY-001-LOCAL-INFRA: Committed receipt/reducer/workflow infrastructure at `a4775aba347dc1f48f1ddb68bda61a1845ec9920`.
  - *Validation Executed:* Clean checkout at that exact commit: fmt, clippy `-D warnings`, `cargo test --locked --all-targets` (236 lib + 159 main + 2 bin + 3 integration; 0 failed), Linux 7-scenario × 100-run receipt `c0fbef31e916e7efd01f9bbf49f3f92070ad10747646179b5d37d6c0ba4e3a2f`, and fail-closed missing-Windows reduction. Durable evidence: `docs/reports/SG02_CROSS_PLATFORM_LINUX_RECEIPT.json`, `docs/reports/SG02_CROSS_PLATFORM_PARITY_STATUS.json`.
- PVP005-UNIT2-EVIDENCE-INTEGRITY-RESET-001-QUARANTINE: Quarantined 11 INVALID_EVIDENCE files (101KB) with SHA-256 preservation.
  - *Validation Executed:* Yes — all 11 files copied to docs/evidence_quarantine/PVP005-UNIT2-EVIDENCE-INTEGRITY-RESET-001/ with SHA-256 hashes. Manifest created with reason, G4/G5 status, and promotion BLOCKED.
- GRAB07-SEQ-CONCAT-002: Train v6 concatenation conditioner on the combined corpus and record the strict full-distribution verdict.
  - *Validation Executed:* Yes — fresh `/tmp/hermes-verify-grab07-concat-_41nt3u3.py` ad-hoc focused verification re-ran `python3 tools/qa/train_grab07_seq_conditioner.py --steps 15000`, structural concat assertions, syntax checking, and receipt assertions. It passed with the expected strict trainer exit 1 / `FAIL` (median 365.10 mm > 15 mm), early stopping at `9.242739179171622e-05`, both training corpora, all 12 paired held-out rows, and median-derived ablation delta 15.77 mm. Temp script cleanup was explicitly verified; evidence log SHA-256 `b45a655ab8d43340914ce671a72ad31efa8a1b561ed39a33ba786bbf59370fa9`.
- GRAB07-SEQ-CONCAT-001: Implement concatenation conditioner, source-aware combined-corpus split, strict median gate, early stop, and distribution receipt.
  - *Validation Executed:* Yes — `python3 -m py_compile ...` and structural forward/Conv/dropout assertions printed `STRUCTURAL_CONCAT_SMOKE=PASS`; one-step live trainer smoke completed with the expected non-pass exit.
