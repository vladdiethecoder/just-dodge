# Atomic Task Ledger

> **CRITICAL AGENT INSTRUCTION:**
> When the "Completed" count exceeds 10, move older completed units to `.hermes/atomic_ledger_archive.md`.
> Do not archive Failed, Blocked, Pending, or Current units. Retain the last 3 Completed units for context.

## Global Context
**Global Goal:** Build a high-fidelity, anime-intense, YOMI-first deterministic duel where all production combat motion is generated or tracked live by the admitted MotionBricks interaction stack and resolved by deterministic articulated physics. No fallback clips, pre-baked animations, or runtime action banks are permitted. Kimodo/ARDY may be integrated as planner/proposal systems where useful, but their outputs must enter runtime only through validated hash-bound plan/packet contracts, never as animation playback. Anime combat clips/images/video, and other media may be used only as offline teachers, constraints, training corpora, or proposal references; they must never become runtime presentation assets. The immediate path is to complete the R6K hero-strike bridge, expand MotionBricks interaction conditioning across combat arts/moves from legally cleared media, and turn the end-of-duel Fight Film into a Demon Slayer-clear cinematic replay while preserving truth isolation, replay determinism, and owner approval.
**Baseline Revision:** published PVP-005 feature baseline `4e481ccd59602c1cb4eda97183c32dec48f9a801` on `pvp-005-readable-live-motion`; public `main` is the ancestor `2677b4a7dd050e7f4c5ee03881aa16035e413a8b`; one worktree.
**Assumptions:**
- PVP-001 began from a clean tracked tree; preserve all unrelated user/project work if it appears during later units.
- The selected live path is `main::App -> milestone3::Session/Match -> m3_cleanbox -> cleanbox/duel_world/duel_physics -> PhysicalContactBatch -> immutable milestone3::Snapshot -> renderer/UI/replay`.
- M3 truth advances at 60 Hz and the current Resolve adapter advances exactly two 120 Hz cleanbox substeps; active-M3 30/60/144 Hz equivalence remains a required future proof.
- ARDY, MotionBricks, motion-plan, G1 articulation, hinge projection, and active-ragdoll modules are isolated foundations until executable wiring proves otherwise.
- Presentation, model proposals, UI, camera, audio, VFX, and capture remain non-authoritative; deterministic shared simulation alone may produce contact, injury, and outcome.
- User amendment: final fighters must be anatomically correct and include vital major organs, major arteries, tissue layers, bones, and joints for deep injury simulation; martial/combat arts must emerge from physical rules rather than scripted outcomes. Meshy is permitted as an offline asset source when validated.
- Clarification Q1 locked by user: use full deformable 3D geometry as both authoritative truth and rendered presentation for required anatomy. Rendering may consume truth geometry but must remain read-only to preserve the canon’s presentation-isolation invariant.
- Clarification Q2 locked by user: include all trauma-relevant macroscopic anatomy, including complete bones/joints and tissue layers, all vital organs, major arterial and venous trees, spinal cord, and major peripheral nerve trunks.
- Clarification Q3 locked by user: use a hierarchical atlas of roughly 500–1,000 separately modeled structures with sparse high-rate activation near contact and propagated injury.
- Clarification Q4 locked by user: external/internal hemorrhage, ischemia, pneumothorax/hemothorax, shock, brain/spinal/peripheral nerve trauma, fractures/dislocations, cartilage/ligament/tendon/muscle failure, organ trauma, pain, and fatigue are distinct and causally coupled.
- Clarification Q5 locked by user: support full physical injury presentation—layer opening, penetration, fracture, severance, exposed anatomy, and persistent fluids—with an optional reduced-gore presentation mode that does not alter truth.
- Clarification Q6 locked by user: support a parametric anatomically registered population across sex, height, skeletal proportions, mass, lean/adipose composition, and organ placement.
- Clarification Q7 locked by user: ship broad physical weapon/contact families—edged, thrusting, blunt, polearm, shield, flexible, unarmed, and grappling—with an eclectic, chaotically varied selection inspired by YOMI Hustle rather than a longsword-only roster.
- Clarification Q8 locked by user: hidden commitments are strategic named intents augmented by directional input, YOMI Hustle-inspired; MotionBricks/whole-body control synthesizes technique and physics/anatomy determine outcome.
- Clarification Q9 locked by user: injury and lethality remain literature-anchored with no artificial durability scaling; duel pacing emerges from armor, spacing, intent cadence, and skill.
- Clarification Q10 locked by user: target RTX 5090 first, then optimize to Steam Deck hardware without degrading anatomy, simulation behavior, fidelity, or evidence standards.
- Material mechanics directive: every weapon, shield, and armor material is physically simulated with mass, inertia/momentum, geometry, thickness, stiffness, strength, toughness, hardness, friction, edge/point condition, deformation, fatigue, and fracture; sufficient force and leverage can shatter or permanently deform swords, shields, and armor.
- Exhaustive coupling directive: player bones can shatter when transmitted force/stress exceeds local structural capacity; block geometry, weapon/limb alignment, support, leverage, armor deformation, joint posture, and prior damage determine the load path. The game is built as deeply nested, extensively coupled simulations rather than isolated lookup systems.
- Combat identity directive: the game is a complex abstract rock-paper-scissors/YOMI-Hustle-like system in first-person 3D. Deep emergent simulation and visual/combat intensity comparable in ambition to For Honor/Elden Ring, with For Honor/Mortal Kombat/Tekken-level combat sophistication, are the differentiators.
- Ability-gate directive: some named/directional intents are available only when hidden deterministic state predicates pass—pose, joint configuration, jump height, balance, momentum/angular momentum, contact/grip, weapon properties/state, armor state, injuries/capability, spacing, environment, and prior exchange state. Gates expose readable affordances without scripting outcomes.
- Process directive: consult Honcho context during development to reduce repeated steering and preserve project intent.
- Art-direction correction: Just Dodge is not a medieval combat game. For Honor/Elden Ring are fidelity/intensity references, not setting references. The setting is currently unchosen; avoid era-specific generation until a provisional timeless/brutalist direction is tested and explicitly accepted.
- Reference-image directive: use the already configured GPT Image 2 generator for concept/turnaround images; Meshy is the 3D generation/processing path.
- Audio directive: use ElevenLabs for Just Dodge audio production; generated audio remains presentation-only.
- Execution directive: licensing/provenance concerns must not halt development. Track provenance and replacement/commercialization work, but continue the strongest technical primary path; distribution compliance remains a release task rather than a development stop condition.
## Unresolved Risks:
- The local 13-file MotionBricks runtime bundle is hash-pinned and clean checkouts can hydrate it from an explicit trusted cache; publishing a durable remote cache remains a release operation.
- Strike Resolve now consumes solved C0 body proxies and the same physically constrained W0 transform used by presentation; Block/Grab/Move still use action-authored cleanbox targets.
- Strike and Move render non-contorted world-space-retargeted motion; idle uses a grounded source frame. First-person W0 remains camera-relative by design, while F2 development cameras expose the solved world-space W0/contact geometry.
- The certificate-gated endpoint-v4 ARDY batch generated six finite proposals, but all six failed structural admission: position-only conditioning left rendered FK hand residuals at `0.479..1.018 m`, planted-foot residuals at `0.055..0.133 m`, and first-eight-frame action tells near-static. This path is halted before runtime promotion; exact receipt: `docs/reports/PVP005_ARDY_V4_GENERATION_RECEIPT.json`.
- Local package/repository verifiers and OS-input interaction evidence exist; canonical gameplay media and five uninterrupted human-match evidence do not.
- Floating-point wall-clock accumulation must not enter authoritative truth or replay serialization.
- Prior O1 blocker: Rust `ort` decoder reshape failure `{1,1024,2} → {1,10,-1}`; Python encoder→decoder succeeds on identical input.
- ANM1 has no matrix-space tag: pose-carrier reference clips are parent-local, but Meshy walk ANM1 is armature-world. Treating the latter as local caused catastrophic mesh explosion despite finite matrices; the active world-space contract is now explicit and tested.

---

## Active Unit
MOTION-FRONTIER-LAB-SHELL-001 — IN PROGRESS. Goal: add a model-independent in-game launch mode that visually falsifies the current R6K motion stack instead of presenting only successful beauty output. Expected behavior: `--motion-frontier-lab` freezes combat truth, starts at an exact requested frame, renders the current MotionBricks target and coupled-tracker output as distinct colored G1 skeleton ghosts, draws target-to-output residuals, reports per-frame target/foot/grip limits, labels requested and ARDY layers unavailable rather than fabricating them, supports exact-frame stepping/play-pause/presentation-off controls, and never writes truth/replay/contact/injury/outcome. Scope: new `src/motion_frontier_lab.rs` plus narrow presentation-only changes in `src/main.rs`, `src/input.rs`, `src/hero_strike.rs`, `src/renderer.rs`, and `src/ui.rs`; no dependency/config/model/asset/schema changes. Baseline evidence: hero-strike 4/4, input 8/8 filtered, renderer 1/1, and `git diff --check` passed before edits. Validation: focused state/metrics/renderer/input/UI tests; warning-denying build/tests; deterministic startup receipt; native GPU launch and actual vision inspection of presentation-off and context views. Rollback: restore exact files from `/tmp/jd-motion-frontier-lab-backup-20260716` and remove only the new module. Assumption: the fixed R6K target is diagnostic evidence of the current limitation, not a newly admitted runtime bank or a claim of open-ended generation. Strike count: 0.

MEDIA-TO-CONSTRAINT-COMPILER-V1 — PASSED (ad-hoc). Built the offline constraint compiler `tools/qa/media_to_constraint_compiler.py` plus packet schema `assets/data/motion_constraint_packet_v1.schema.json`. The compiler consumes a `media-corpus-abi.v1` record plus derived semantic anchors and emits a canonical `motion-constraint-packet.v1`: source/anchor/compiler SHA-256 binding, intent classes, constraints, `outcome_authority=deterministic_physics_only`, `runtime_playback_allowed=false`, and self-hash. Forbidden keys (frames/f413/npz/bvh/fbx/hit/injury/outcome/damage/etc.) are recursively rejected. Ad-hoc gates passed: byte-identical determinism across two runs (packet SHA-256 `012c944676d54f22cbf31140d3ca3a6e4949d5b99367a3fb9097f32afdfcc733`), forbidden-key rejection (`$.hit`), packet contract assertions, `py_compile`, schema parse, and `git diff --check`. This is offline-only tooling; no runtime wiring or motion admission. Strike count: 0.

DYNAMIC-COMBAT-DEMO-001 — PASSED (ad-hoc). Built the interaction-conditioned dynamic combat demo: 162 examples (6 moves × 27 opponent-geometry variants), trained a single MotionBricks interaction extension to track all of them with zero foot/grip/hand error (162/162), and rendered 12 contact-sheet frames for visual QA. The demo proves the model produces different motion for different opponent geometry (feature variance nonzero across 12 dims). Key artifacts: `demo_summary.json`, `rendered_poses.npy`, `rendered_manifest.json`, `frames/*.png`. Ad-hoc verification passed: summary structure, zero-error assertions, pose-motion variance, PNG validity, manifest, py_compile, git diff --check. This is QA evidence only; no runtime assets, no clips, no baked animation. Strike count: 0.

ANIME-COMBAT-MEDIA-INTAKE-001 — IN PROGRESS. Goal: aggressively gather all high-quality combat media that can benefit Just Dodge, including anime/film clips, images, videos, sakuga references, mocap, and user-owned sources, and convert them only into offline teachers, constraints, training corpora, or proposal references for MotionBricks interaction conditioning. Expected behavior: source inventory and conversion/toolchain matrix are built without concern for licensing restrictions during research intake, raw media never becomes runtime animation, and conversion outputs target training/constraint formats compatible with SOMA/G1/C0 or retargetable skeletons. Scope: research, source inventory, toolchain matrix, and integration recommendation only; no runtime admission in this unit. Validation: subagent summaries reconciled against repo docs and prior Hermes sessions; explicit separation between offline training media and runtime MotionBricks-generated/tracked production motion. Rollback: remove only this ledger entry and any new intake notes. Strike count: 0.

PVP005-R6K-EVIDENCE-BASELINE-001 — PASSED AS QUARANTINE, NOT PROMOTION. Exactly 19 stable PNGs were identified: 16 Fight Film frames in `validation_evidence/pvp005-r6-cinematic-replay-20260716T065726Z/frames/ff_{00..15}.png` plus three first-person source-W0 frames in `validation_evidence/pvp005-r6-source-w0-adhoc-20260716T075830Z/frames/f{01..03}.png`. Their exact bytes, sizes, mtimes, and SHA-256 values are recorded in `validation_evidence/pvp005-r6k-baseline-20260716T141300Z/evidence_baseline.json` (canonical manifest-object SHA-256 `957071372e667d4d438679b74a198531831ea10f73a4c8aa0215051387b269fa`). They predate the baseline and have no contemporaneous signed/append-only link to source revision, dirty state, executable, assets/models, seed, or replay. Therefore all 19 are permanently `unpromoted_exploratory`; current state was recorded only as non-binding context. Current context at baseline: exact required HEAD matched; canonical dirty-entry hash `67f63412fb355ea72e4290f11bd58f400e8f0fe9b2abe6b4d6b32c1a98553bec`; freshly rebuilt executable SHA-256 `f4ef8596f2a9dd1b18700fdd38d87a8e83a146a8627454cd000953fd27eec9e5`; replay candidate SHA-256 `c256187e261c90529209e53ca52a2b636de4911150345f505ebd90dd7214ca69`; seed `5562789964732694528`; C0/W0/ARDY/MotionBricks artifacts and existing ARDY model receipts are enumerated. This satisfies the fail-closed baseline rule but proves no R6K motion or visual gate. No Block or Grab was generated. Strike count: 0.

PVP005-R6K-KIMODO-ADMISSION-002 — PASSED AS AN UNPROMOTED OFFLINE TEACHER; RUNTIME ADMISSION REMAINS CLOSED. External worker: `/home/vdubrov/Projects/kimodo-r6k-worker`, isolated Python `3.11.14`, `torch 2.11.0+cu130`; no Cargo/runtime dependency was added. Exact clean Kimodo checkout: commit `1aece8c124d73d255ceff5086d983b844c9f4e94`, tree `3e5f60205d3466b680d982397eb69ce668ffdff8`, Apache-2.0 license SHA-256 `d1a7d615ab8eff4de143b1456f46dabf232f54daf0fcf9a70442bb6f637a9e95`. Exact `nvidia/Kimodo-SOMA-RP-v1.1` revision `6c9233af1180b8151e3c4703477104af5dce9dd5`, checkpoint SHA-256 `ef0a0ca45a6089ab4532dde609785771ae3f38755b4ae6cf314b0213e07cd4a3`, NVIDIA Open Model license SHA-256 `11ccdcb47db87ac30b63765b5e5154403026c4fbb52c19919d99d912ec454e19`. Exact text revisions: LLM2Vec MNTP `31474e395ada192e8ed1586db6be79fb3b70c9c0`, supervised `baa8ebf04a1c2500e61288e7dad65e8ae42601a7`, Meta-Llama-3-8B-Instruct `8afb486c1db24fe5011ec46dfbe5b5dccdb575c2`; official file-hash manifest SHA-256 `d28fe7bf2b54fddf05cf2b0d3692ee136b2433f0cf13cb8718d96e96c885bb1`. Exactly one three-second/90-frame Strike was generated on RTX 5090 with seed `2026071602`, 100 denoising steps, sparse full-body frames `0,32,62,89`, explicit dense stationary root/+Z heading, dense bilateral foot plants, bilateral hand position+rotation targets, and a free-space weapon corridor with no opponent/hit/reaction target. One Kimodo model call produced source SHA-256 `f2d2babb696b620e25921d60eed984e681f1355095639cde9e1e93205a5dcfcb`; zero Block or Grab generation. Disclosed processing: pinned official `post_process_motion` followed by deterministic SOMA77 leg-chain foot-lock refinement, with zero additional model calls. Selected unpromoted teacher SHA-256 `c3412394684264381c8c3b5828f607c69878100d52cba0648cbb84aa75202ece`. Pre-mapping audit: finite; determinant `[0.9999996424,1.0000004768]`; orthogonality max `4.172325e-7`; max hand error `0.004062461 m`; max hand rotation error `0.528787°`; max planted-foot drift `0.009979125 m`; root XZ drift `0`. Evidence: `validation_evidence/pvp005-r6k-kimodo-teacher-20260716T144357Z/package_manifest_v3.json`, manifest SHA-256 `7eef79741c499620ac8555d49086607a2c1f0fb0fe0bed3458ffc3166f0ec407`. No runtime load, ARDY generation, push, promotion, or owner approval occurred. Next gate: deterministic SOMA77→SOMA30→ARDY Core27→C0 bridge; stop before ARDY generation if it fails.

PVP005-R6K-ROTATION-BRIDGE-003 — STOPPED AT STRIKE 2; FAILED PATH ROLLED BACK BEFORE ARDY GENERATION. Official ARDY `CoreSkeleton27` was confirmed at commit `693f74d13b3d04a0a22ce127ee79c929dd89756b` (tree `c430040bbac13d673781f3bce000d82307018727`) and exact SOMA77→SOMA30→Core27 round-trip endpoints were `0.0 m`, with finite, proper rotations and canonicalized quaternions. Both attempted C0 endpoint solvers failed the strict gate because they incorrectly targeted unscaled SOMA world end-effectors: batched differentiable IK plateaued at `0.057054340 m` hand error / `0.041646097 m` foot-target error / `0.010415999 m` foot drift; batched CCD plateaued at `0.057568515 m` / `0.041443444 m` / `0.010398416 m`. The C0/Core semantic segment ratios are only `0.822–0.893` across the arms, falsifying the raw-world-target assumption. The untracked exporter and bridge implementation were removed after preservation. Evidence: `validation_evidence/pvp005-r6k-rotation-strike2-20260716T151500Z/failure_manifest.json`, canonical manifest SHA-256 `4608389478de01ee3c004d060d27a98459ef87d8c017d389c6077b3f16e35bd0`. Next falsifier: map hand and foot effectors as shoulder/hip-relative vectors scaled by exact C0/Core semantic chain lengths, then solve against and gate those explicit scaled targets. ARDY generation remains unauthorized; zero Block or Grab generation.

PVP005-R6K-ROTATION-BRIDGE-004 — SCALED-EFFECTOR PATH STOPPED AT STRIKE 2; EXTERNAL SOURCE ROLLED BACK. The scale-aware bridge falsified the prior raw-world-target failure: exact semantic chain scaling reduced C0 hand error to `0.000002062 m`, grip-span error to `0.000001997 m`, and hand rotation error to `0.046637°`; round-trip FK remained `0.0 m`, with no reflections/non-finite values/joint-limit violations/quaternion discontinuities. Attempt 1 failed mechanically before artifact emission because strict `zip` received unequal iterator lengths. Repair 1 fixed that, but the gate then failed only because right C0 Foot drift was `0.010089594 m`, exceeding `<0.010 m` by `0.000089594 m`; left Foot was `0.009801696 m`. The scaled method still copied Kimodo output foot residuals rather than the explicit stationary planted-foot targets. Evidence: `validation_evidence/pvp005-r6k-rotation-scale-strike2-20260716T151802Z/failure_manifest.json`, canonical manifest SHA-256 `fdcf28e61d8e7cd06c480748e1d44af66f015a4ccde4bb8f8cab28baab2d28d2`. Next falsifier: derive foot/toe anchors from the declared frame-0 planted constraints and repeat them across all 90 frames before scaled C0 CCD. ARDY generation remains unauthorized; zero Block or Grab generation.

PVP005-R6K-ROTATION-BRIDGE-005 — PASSED AND STAGED UNPROMOTED. The third, falsifier-driven method uses official named SOMA77→SOMA30 extraction, a deterministic composition-preserving SOMA30→ARDY Core27 map, and exact C0 asset/runtime coordinate conversion (`+Z`-up centimetre children under root scale `0.01` → right-handed `+Y`-up metres, `+Z` heading). C0 targets use exact semantic chain-length scale ratios; the W0 grip is fixed at `0.160 m`; both declared planted foot/toe anchors repeat their frame-0 world targets across all 90 frames. Golden teacher SHA-256 `c3412394684264381c8c3b5828f607c69878100d52cba0648cbb84aa75202ece` is hard-pinned. Two independent recomputations were byte-identical to bridge SHA-256 `f519764263e87796323c421bb2d7b2df0674d9f9638c763f6a50076ba3d72c72` and report file SHA-256 `7e8f71deb52ead74d84983db5882d68b734457a58e4212ae7e7acc1de57b3e43`. Gates: round-trip FK endpoint max `0.0 m`; C0 hand max `0.000002062 m`; grip max `0.000001997 m`; hand rotation max `0.046637°`; planted-foot drift max `0.008023388 m`; determinants within `[0.9999993375,1.0000005946]`; finite; no reflections; no post-canonical quaternion flips; zero ARDY Core27/C0 joint-limit violations. Fresh atomic verifier exited `0`, emitted `AD_HOC_VERIFY=PASS`, self-deleted, and logged SHA-256 `3945962c25e2e721c8220c7b0ef23e4a05da72819cf2f6f7d9cee82674f447d6`. Evidence: `validation_evidence/pvp005-r6k-rotation-bridge-20260716T151822Z/package_manifest.json`, canonical manifest SHA-256 `2c59ee753504f217c47735f4fcb125c5c4aa82e1c453518f04bef0b69606750e`. Staged assets are explicitly `runtime_admitted=false` and `promoted=false`; ARDY generation remains unauthorized until this gate closure. Zero Block or Grab generation.

PVP005-R6K-ARDY-PROPOSAL-006 — ARDY GENERATED; CUSTOM CONSTRAINT PROJECTION STOPPED AT STRIKE 2 AND ROLLED BACK. Isolated external worker `/home/vdubrov/Projects/ardy-r6k-worker` used clean ARDY commit `693f74d13b3d04a0a22ce127ee79c929dd89756b` / tree `c430040bbac13d673781f3bce000d82307018727`, exact HF revision `abe6c43beb28c867c950acb824b9c4ef3d63fb76`, denoiser SHA-256 `1019d0bf269cf8d1b3e3e9b4a384a58c112672959b071279ddb65814d77660cd`, tokenizer SHA-256 `58a887e299a3a6779b5b4ff361b452c4349d45b513b5957f2a961d393623abcd`, and model-license SHA-256 `11ccdcb47db87ac30b63765b5e5154403026c4fbb52c19919d99d912ec454e19`. Kimodo teacher/bridge supplied only sparse full-body, dense root/heading, sparse bilateral hand, dense bilateral foot, and W0 corridor constraints (`e3408c30b2af09110366f8571ac30c7b8c30c78f5f3a153554149792328c8071`). RTX 5090 attempt 1 failed before denoising because the worker requested 100 steps from a checkpoint fixed at `num_base_steps=10`; no artifact emitted. Repair 1 used the exact model schedule and produced one 40-frame/20 FPS/2.0 s ARDY Core27 proposal, SHA-256 `1ea11146159f5529c0dad8c33e0f00725058fcbc8ae8f30b80091609c7c86e8f`. Raw proposal remains unpromoted because feet drifted up to `0.160121188 m` and grip ranged `0.090325–0.476061 m`. A custom Core27 projector was rejected at Strike 2: first pass `0.112166 m` hand / `0.091807 m` grip / `0.048398 m` foot; repair `0.028390 m` / `0.024339 m` / `0.026733 m`. Its code and outputs were preserved then removed. Next falsifier: pass the raw ARDY proposal plus teacher constraint tracks into MotionBricks, the designated sole runtime motion/stance/retarget/in-between authority, and require MotionBricks—not a parallel custom projector—to close foot/grip targets before Rust physics. Evidence: `validation_evidence/pvp005-r6k-ardy-proposal-20260716T153913Z/package_manifest.json`, canonical manifest SHA-256 `eaee83af96beeddc87fe22f81095aab7503f7c6368c98451738bb4035efb80de`. Runtime admission and promotion remain false. Generated actions: Strike 1, Block 0, Grab 0.

PVP005-R6K-MOTIONBRICKS-BOUNDARY-007 — STOPPED AT STRIKE 2; WRAPPER ROLLED BACK. Direct ARDY Core27→G1 context/target boundary conditioning drove the released MotionBricks checkpoint to root magnitude `134.977524 m` and endpoint residual `10.471282 m`. Repair 1 restored the canonical measured Strike boundary window and injected only bounded ARDY residuals (`0.5×`, `0.20 m`, `60°` caps), but root magnitude still reached `15.535710 m` against a `2.0 m` fail-closed envelope. Exact released checkpoint hashes: VQ-VAE `f12a09d46ad390a8e2eecbe7219b2472fcab6b59df0a13f6a40c35cb6da4d99a`, pose `0223c352b308ba638a499cc5c92104da36cb1d04cea2f8ce61d54a2489f853f1`, root `d7299a9b1f5aca35730c36dfe7ea28075708ac8266bf384fe8e2ef9c9aee69c7`, G1 clip `84afc7c229473351a24b0a7d79fc47be9dbb81bd12774285f2f60a3c0e9028df`. This reproduces the existing official contract probe: the released model is a boundary transition candidate and lacks a compatible interaction/physics-feedback channel. Wrapper source and failed output were preserved then removed. Evidence: `validation_evidence/pvp005-r6k-motionbricks-boundary-strike2-20260716T155819Z/failure_manifest.json`, canonical manifest SHA-256 `dc983d963c964be203679551cfce23a4f7b457bb050e6590d4abfb924e193c95`. Next falsifier: train and verify a compatible MotionBricks VQ-VAE combat adaptation on the exact ARDY-derived G1 Strike target; do not reintroduce boundary wrappers or custom kinematic projectors. Runtime admission/promotion remain false. Generated actions in this failed unit: Strike 0, Block 0, Grab 0.

PVP005-R6K-MOTIONBRICKS-VQVAE-008 — STOPPED AT STRIKE 2; DECODER-ONLY ADAPTATION ROLLED BACK. A 600-step decoder adaptation (`smooth_l1`, task weight 20) failed with max foot drift `0.054402 m`, grip error `0.148803 m`, and hand error `0.135028 m`. Repair 1 used 1,500 steps and direct MSE task weight 2,000; it closed grip to `0.006365 m` but still missed feet (`0.016618 m`) and hands (`0.021792 m`). This falsifies decoder-only codec adaptation as the interaction-conditioning substrate; all emitted checkpoints and motion bytes were preserved as failure evidence then removed. Evidence: `validation_evidence/pvp005-r6k-motionbricks-vqvae-strike2-20260716T160328Z/failure_manifest.json`, canonical manifest SHA-256 `2b69591405ae7ce3ab2ccc8c5a0d2056a91573ce34e99faff1425118083e82be`. Next falsifier: train the MotionBricks pose-backbone interaction extension on the exact ARDY-derived G1 target and validate through its wrapper API before runtime installation. Runtime admission/promotion remain false. Block/Grab generation remains zero.

PVP005-R6K-MOTIONBRICKS-INTERACTION-009 — MECHANICAL PROVIDER GATE PASSED; DEVELOPMENT RUNTIME INTEGRATED, NOT PROMOTED. A trained sparse/dense interaction extension now consumes the exact released MotionBricks Strike prior (`1de671724ca50027ab7b0aa474dac0a8657c555120db41d6a7086a1d956ee985`) plus the exact ARDY Core27 proposal (`1ea11146159f5529c0dad8c33e0f00725058fcbc8ae8f30b80091609c7c86e8f`). Architecture: 1,241-channel base/constraint/mask/phase input, 256-channel dilated temporal residual trunk, 413-channel MotionBricks output, with model-internal hard sparse/full-body and dense root/foot/hand conditioning. Trained target SHA-256 `921cd4c21e83810fd70ab49c36d7500e0c89ea26a55c5e85007a5a0f3da9a7e5`; ONNX SHA-256 `dc67dd498dc50040372144d451b5a455c2f504837c00a3690073912c847605b0`. PyTorch gates: 0 foot drift, `4.47e-8 m` grip error, 0 hand-target error. ONNX Runtime CPU repeated outputs were byte-identical; foot drift 0, grip error `4.47e-8 m`, ONNX/PyTorch max feature delta `0.000786424`. Development runtime now hash-gates and loads exactly 64 MotionBricks interaction frames at 30 Hz, executes four deterministic Rust articulation/PBD substeps per frame at 120 Hz, and derives W0 from solved C0 hand sockets. Focused Rust tests passed in lib and main: finite moving retarget, rigid two-hand W0 < 0.010 m, segment length < 0.005 m, grip span < 0.005 m. No provider accepts opponent/hit/contact/injury/reaction/outcome inputs or authors outcomes; cleanbox remains sole authority. Evidence: `validation_evidence/pvp005-r6k-motionbricks-interaction-20260716T161235Z/package_manifest.json` canonical SHA-256 `9c7abe9cdcf8efb989a78bafe54b8b6aae97d64fef6f1e58761f2970cb473f57`; runtime evidence `validation_evidence/pvp005-r6k-provider-runtime-20260716T161438Z/package_manifest.json` canonical SHA-256 `0820d75315f3eafbd4ffe357a4549d02bf5f43bc959dd921c02602bd89fbd9fa`. First Rust verification failed because `hex` was transitive-only; repaired with a zero-dependency `[u8;32]` digest constant. Repair 1 then encountered the pre-existing libpython dynamic-link environment; Repair 2 set the exact local ARDY venv library path and passed. `runtime_admitted=false`, `promoted=false`, `pushed=false`; owner gate remains open. Generated actions: Strike 1, Block 0, Grab 0.

PVP005-R6K-LIVE-010 — MECHANICAL LIVE GATE PASSED; VISUAL/CAMERA GATE REMAINS OPEN. `current_pose` selects dynamic 64-frame R6K C0 skins during revealed Strike, the game executable no longer calls `renderer::first_person_weapon_model` or contains `action_weapon_presentation`, and both player/opponent W0 transforms derive from solved C0 `LeftHand`/`RightHand` sockets. Ten complete identical runs at seed `5562789964732694528` produced truth hash `d1a3cc1bfb9c2f67` and byte-identical replay SHA-256 `9f15f206ba368ff1f828cb63b6f41529df531456ee3a8b23adca7abc8586096a`. Focused pose/socket/articulation and revealed-Strike-selection tests passed. Release executable SHA-256 `522812af15cbb1edd165ba1ef930d5a91c3027f29b65217ce7a408a4cc237fc9`; 2560×1440 benchmark remained mean `4.183 ms`, p95 `4.274 ms`. Offscreen world captures show non-bind armored poses, but its first-person harness reported 16 crop failures; an attempted 12 s 2560×1440 60 Hz Xvfb capture was black except cursor because x11grab did not observe Vulkan presentation. Both were retained as failed/insufficient visual evidence and are forbidden for owner acceptance. Evidence: `validation_evidence/pvp005-r6k-live-20260716T162544Z/package_manifest.json`, canonical SHA-256 `9818ec58df6331388a0bdffeb285b12f80c04fdda0dc4938bad79c91c8e2a3e2`. `runtime_admitted=false`, `promoted=false`, `pushed=false`; visual/camera, ForgeLens, presentation, continuous native capture, and owner approval gates remain open. Generated actions remain Strike 1, Block 0, Grab 0.

PVP005-R6K-FORGELENS-011 — MOTION LAB FUNCTIONAL GATE PASSED; OWNER OUTCOME AND PHYSICS FOOT GATE REMAIN OPEN. ForgeLens now loads a revision/hash-bound `forgelens.motion-lab/v1` payload with exact Kimodo (`c3412394684264381c8c3b5828f607c69878100d52cba0648cbb84aa75202ece`), ARDY (`1ea11146159f5529c0dad8c33e0f00725058fcbc8ae8f30b80091609c7c86e8f`), MotionBricks (`921cd4c21e83810fd70ab49c36d7500e0c89ea26a55c5e85007a5a0f3da9a7e5`), and Rust physics (`11e96e5f36345cd00a241da2dd1510a84970d77db2ceaf8549caca5eb9d05d67`) lineage. It exposes draggable text/full-body/root/end-effector/contact tracks; synchronized 77/27/34/24-joint skeleton canvases; candidate overlays; FK, foot, COM, grip, and weapon-path plots; and revision/frame/joint/object/world-bound annotations. Motion Lab events are immutable SHA-256-linked receipts; API/Inkling identities can annotate only, while approve/reject/request-change requires an external human event and never transitions ReviewRun admission. The first API annotation truthfully blocks acceptance at frame 16 because measured physics foot drift is `0.151634 m` (> `0.010 m`), while grip error remains `0.000177 m`. Full ForgeLens suite: 40 passed; Rust hero tests: 3 passed; deterministic dataset/export compare passed. Evidence: `validation_evidence/pvp005-r6k-forgelens-motion-lab-20260716T181651Z/package_manifest.json`, SHA-256 `7a2b7c05acdda9d2db9c0ca1c695eb7f1dd4d01464d335145721292ecd83bb9a`. Runtime admission/promotion/push remain false; generated actions remain Strike 1, Block 0, Grab 0.

PVP005-R6-NATIVE-HERO-STRIKE-003 — PASSED after full-stack schema correction and one solver repair. Two C0 transfer attempts were visually falsified and rolled back: treating Meshy walk ANM1 as parent-local produced giant geometry on both pre/post rotation-multiplication variants. Matrix inspection found the actual contract: every walk bone is already armature-world (`0.01` scale and meter translation), while SKM1 carries `0.01` only on the root plus centimeter child locals. Added explicit world-space ANM1 retargeting that preserves target rest scale/translation and converts desired world rotations back to target locals; native first-person and F2 bird's-eye/left/right 3/4 captures show coherent grounded armored bodies with no exploded triangles. Strike now couples the admitted 52-frame ARDY trajectory with a deterministic 32-frame released-checkpoint MotionBricks Strike proposal (SHA-256 `1de671724ca50027ab7b0aa474dac0a8657c555120db41d6a7086a1d956ee985`), then follows it through a 120 Hz bounded motor/PBD articulated solve with parent-child length rows and a closed two-hand W0 row. Initial grip-row test exposed a signed `-0.16 m` span constant; Repair 1 corrected it to `+0.16 m`. Tests prove MotionBricks changes the lower-body targets, every nonzero articulated segment stays within 5 mm of reference length, and W0 hand span stays within 5 mm; existing rigid two-hand W0 tests remain green. Live Strike now renders the solved C0 stream, F2 cycles first-person/bird's-eye/left/right 3/4 cameras, F3 hides HUD for contact inspection, and debug cameras render both world-space W0s. Resolve submits solved body proxies plus the same W0 transforms to cleanbox; replay truth remains authoritative. Visual captures `/tmp/jd-world-retarget-{firstperson,quarter}.png` and `/tmp/jd-coupled-w0-clean-f{0..5}.png` show coherent idle/move/Strike and safe return. Warning-denying all-target Clippy passed; all targets passed (library 136 passed/1 provisioned ignore; main 143 passed; capture ... [truncated]

PVP005-R6-COMBAT-PRESENTATION-004 — IN PROGRESS. Replaced the rainbow/full-width prototype HUD with compact cyan/amber injury rails, a centered phase pill, restrained four-action controls, enlarged radial Move affordance, and non-blocking reveal/result cards. Added deterministic 50 ms contact hold, six-tick camera impulse, truth-region impact burst/border/cross, phase-driven first-person W0 windup/contact/recovery, F2 bird's-eye and three-quarter development cameras, F3 HUD suppression, fixed Space confirmation, and explicit `--resolution`/`--benchmark-frames` profiling. The C0 arm solve now runs 64 deterministic CCD iterations to close both hands to fixed 0.160 m W0 sockets; W0 rendering and cleanbox share those solved C0 transforms. Runtime visual captures prove no geometry explosion and no detached W0 sockets. Real Wayland/RTX 5090 benchmark at 2560x1440 for 300 frames: mean `4.129 ms`, p95 `4.239 ms`; Xvfb/Mesa is an invalid software-present baseline at p95 `79.111 ms`. Warning-denying all-target Clippy passed; all-target tests passed (library 136 passed/1 provisioned ignore, main 143 passed, capture 2 passed, two provisioned integration ignores); release headless autoplay/verify exits 0. Arena presentation now uses model-correct normal lighting with a 0.70 minimum irradiance, keyed/hemi volume, lower-frequency low-contrast stone floor, and material-separated basalt/pillar/rock values; `/tmp/jd-p4-lit-neutral-arena.png` and `/tmp/jd-p4-swing-cross/f{1,2,3}.png` show no black surfaces or atlas streaking and clearer armor/prop volume. This remains an intentional coherent fallback, not final material art: the cooked rigid-mesh format omits source material-run IDs, so its four 6K source atlases cannot be assigned correctly without recooking/format work. The player view-model sweep now crosses lower-right to upper-left through the torso impact instead of becoming a vertical center-screen obstruction. Remaining external blocker: production ElevenLabs audio cannot be produced because `ELEVENLABS_API_KEY` is unset. P4 remains open; no visual-acceptance claim. Strike count: one CCD convergence repair (16 iterations/0.72 damping left a 20.474 mm residual; 64/0.90 passed the 12 mm gate).

PVP005-R6-FIRSTPERSON-W0-VIEW-006 — STOPPED AT STRIKE 2; FAILED PATH ROLLED BACK. The first-person presentation was probed against the admitted world-space W0 instead of accepting the generic camera-space sweep as equivalent to the rotation-conditioned source. Attempt 1 rendered `HeroStrikePresentation::weapon_world` directly from the player actor: `/tmp/jd-p4-source-w0-fp/f01.png` showed the true opponent/source geometry, but `/tmp/jd-p4-source-w0-fp/f03.png` put the player's hilt/guard against the near plane and obscured the lower-left combat view. Attempt 2 applied `weapon_local(0).inverse() * weapon_local(frame)` to a stable first-person anchor with 0.35 translation gain: `/tmp/jd-p4-view-delta-w0/f02.png` moved the player blade mostly below the viewport at contact, leaving the opponent W0—not the player's admitted path—as the dominant readable blade. Both methods failed the human-readable camera gate, so `weapon_view_delta` and the live source-world branch were removed; cleanbox/world W0 authority was never changed. The retained generic view-model sweep is explicitly presentation-only and now crosses lower-right to upper-left through the impact region, but it is not admitted as source-trajectory evidence. Next falsifier requires a new method: derive the first-person eye/view basis from the solved C0 head orientation and render the player's solved forearms/hands with the exact world-space W0, so camera and source carrier share one coordinate frame. Do not retry camera-space delta remapping. Strike count: 2.

PVP005-R6-SOLVED-C0-FIRSTPERSON-007 — STOPPED AT STRIKE 2; FAILED PATH ROLLED BACK. This new method shared one actor coordinate frame among the solved C0 carrier, exact world-space W0, and a Head-derived target-locked first-person camera. Attempt 1 rendered the full player carrier: `/tmp/jd-p4-solved-c0-fp/f01.png` rolled the arena with Head orientation and `/tmp/jd-p4-solved-c0-fp/f03.png` placed torso/helmet and W0 hilt across most of the near plane. Repair 1 locked camera up to world Y and added a C0-specific arm-only skin pipeline that discarded vertices with less than 0.20 combined weight on joints 12–19. It removed the horizon roll and most torso fragments, but `/tmp/jd-p4-solved-c0-arms/f03.png` still had the admitted forearm/hand/hilt filling most of the frame and hiding the rival during follow-through; the source geometry itself crosses the Head camera's near field. This falsifies both full-carrier and arm-only Head-camera variants. Head-index exposure, camera/render branches, arm-only pipeline, and `skin_first_person.wgsl` were removed; release rebuilt and `git diff --check` passed. Truth, replay, cleanbox, solver, source bytes, world-space W0, and retained generic view-model sweep were unchanged. The remaining source-valid first-person path requires a separately authored first-person arm carrier/camera calibration derived from the accepted source—not clipping or remapping the third-person C0 mesh—and is not solvable by another renderer-only branch. Strike count: 2.

PVP005-R6-CALIBRATED-SOURCE-W0-008 — PARTIAL PASS; ARM-CARRIER SUBPATH STOPPED AT STRIKE 2 AND ROLLED BACK. The live first-person Strike now renders the admitted `HeroStrikePresentation::weapon_local(frame)` unchanged under one constant view calibration shared across the whole clip: carrier origin is shifted 0.55 m camera-forward, 0.10 m camera-right, and 0.12 m down, with the original player actor rotation/scale preserved. This is a rigid presentation calibration, not a per-frame trajectory remap; source frame selection, W0 local rotation/translation, cleanbox world W0, replay truth, and contact authority remain unchanged. Native sequence `/tmp/jd-p4-calibrated-source-w0/f{01,02,03}.png` shows the source W0 traverse windup, torso impact, and follow-through with stable horizon, readable rival, no near-plane hilt explosion, and the player's W0 centered on the impact marker; this replaces the generic camera-space sweep during Strike only. A first-person arm-only carrier was then tested under the same rigid calibration. Attempt 1 at 0.20 arm-weight threshold leaked shoulder/torso fragments across the bottom half (`/tmp/jd-p4-calibrated-arms-w0/f{01,02,03}.png`); Repair 1 raised the threshold to 0.65, but the retained third-person forearms still formed a crumpled foreground mass and did not read as attached first-person hands (`/tmp/jd-p4-calibrated-arms65/f{01,02,03}.png`). The arm-only shader, pipeline, render branch, and file were removed; release rebuilt and `git diff --check` passed. W0 calibration remains because it passed its narrower source-trajectory/visibility gate. A separately authored first-person arm mesh remains required for visible hands. Strike count: 0 for W0 calibration; 2 for the rolled-back arm carrier.

PVP005-R6-CINEMATIC-REPLAY-005 — IN PROGRESS, OWNER VISUAL DECISION PENDING. The terminal match now enters Fight Film automatically; playback validates every original truth hash while skipping displayed Observe/Plan gaps and preserving all nine exchanges in order. Replay rendering forces world-space two-fighter/W0 presentation, uses replay-frame roots rather than terminal live roots, animates Block/Grab counter beats from the admitted C0 stream, and directs phase-aware WIDE/TRACK/IMPACT/FOLLOW-THROUGH third-person cuts. A dedicated letterboxed overlay exposes exchange `N/9`, action pairing, cut role, final frame, and film-complete state. Focused tests pass: cinematic cursor displays no idle phases and visits exchanges `[0,1,2,3]` in order; camera/counter test proves finite distinct wide/track transforms and non-static Block frames. Release headless autoplay/verify exits 0 and logs `Player flow: automatic Fight Film`. Native Xvfb sequence `/tmp/jd-fight-film-frames-v2/ff_{00..15}.png` visibly traversed exchanges 1,2,4,5,7 with separated full-body silhouettes and no first-person artifacts; terminal `/tmp/jd-fight-film-final-v2.png` shows `EXCHANGE 9/9`, `FINAL FRAME`, and `FILM COMPLETE`. Stable evidence: `validation_evidence/pvp005-r6-cinematic-replay-20260716T065726Z/` with 17 frame hashes; audit JSON SHA-256 `862dd0a5723520fe7e65929c1c693167efe8924c96c918d2c39b7c0b7883de12`, zero black frames. Vision confirms the requested structural transformation, while still flagging ambiguous block/grab contact poses and the existing graybox arena as visual-quality debt. No replay-byte or truth-authority change. Strike count: 0 implementation repairs; one compile-type correction (`u16` total versus `u32` exchange) after initial check.

M3-RADIAL-MOVE-004 — PASSED with one runtime repair. Added canonical `Action::Move`, replay-serialized signed-Q15 `RadialDi`, commit rejection for zero direction, and deterministic 25 mm-per-Reveal-tick displacement (300 mm for a full-axis action). Keyboard WASD and a direct analog-axis ingress normalize/quantize before truth; key 4 selects Move. Native roots now read replay-stable millimetre displacement, cleanbox receives those roots, and the Plan HUD exposes a four-action row plus an eight-sector radial indicator and live DI marker. Initial 10-match autoplay timed out because adding Move to the seeded AI cycle admitted non-terminal evade loops; Repair 1 restored the existing three-action AI cycle while retaining player/manual Move and deterministic AI radial generation for future policies. Focused Q15/dead-zone/movement/replay tests pass; warning-denying all-target Clippy and all targets pass (library 134 passed/1 separately provisioned ignore; binary 141 passed; remaining target tests pass with only two separately provisioned integration ignores). Ten complete replay-verified matches passed with stable final hashes. Computer-use visual automation was unavailable because cua-driver never reached MCP initialize; native process itself built, opened, initialized C0/UI, and exited cleanly. Strike count: 0 for the implementation; one repaired autoplay-policy failure.

PVP005-VISUAL-BINDING-REFRESH-001 — PASSED with no implementation repair. Refreshed the active visual harness bindings for the hardened ARDY generator (`e6a961ea1b7d89dce8b7e99351d70db8f6baf10199c488cecdb34f0b9eb4e2e7`) and candidate screener (`2e408bf5be88d70006bd59e7c33fbebf57eafd6398ece64bc2c0a25db1899bb4`), yielding visual-contract SHA-256 `595ce5d29f5fd161b5f8ff65a484f24450763c111b26bf6b2188dce86149ee77`. Refreshed the ForgeLens contract's current visual-contract and rebuilt release replay-verifier bindings while preserving the separate historical receipt hash and blocked evidence verdict. Focused visual-contract tests (5/5) and ForgeLens Phase A verifier passed. Clean-worktree `./tools/verify_playable_repo.sh` passed: revision/candidate/visual contracts, warning-denying Clippy, all-target tests, all three separately provisioned MotionBricks gates, two independently assembled byte-identical packages, package hash verification, replay hash `d1a3cc1bfb9c2f67`, and diff check. Files: the two binding contracts plus this ledger. No visual acceptance, historical receipt, runtime, truth, replay, generated motion, or dependency change. Strike count: 0.

PVP005-ARDY-V4-REREVIEW-HARDENING-002 — PASSED with no implementation repair. Absolute and any `..` spec/certificate path now fail before resolution, including paths that would resolve inside the repository; canonical defaults are repository-relative and valid materialize-only remains functional. The pure materializer validates the completed document after arithmetic, so finite-input interpolation overflow cannot return infinity. The validator's primary spec/W0/ledger/certificate/generation-manifest paths use strict JSON. Canonical materialized SHA-256 `06f7bbf2a600ce5068bdc9d73e35eabf9905762859a06fbe22c728bb36337a1c`, version-1 authorization SHA-256 `3fbd63da8501b50b04266643ffa41bb609267e8ee4f0c6be56fa3153459de0a7`, historical generator SHA, and generation-manifest receipts are fixed and asserted; the hardened generator still rejects the old certificate on generator-SHA drift. Fresh targeted ad-hoc evidence `validation_evidence/pvp005-v4-rereview-hardening-20260716T010220Z/manifest.json` records 13 zero-return gates and SHA-256 `3b1d5c00125131c7337c974026e89a8bdbad83302848e674f306710e72b87e56`: Python/Ruff, endpoint adversarial regressions, sparse/normalized conditions with unchanged hashes, revision baseline, formatting, warning-denying Clippy, all-target tests (library `131` passed/one provisioned-artifact ignore, binary `134` passed, capture `2` passed), headless autoplay/verify hash `d1a3cc1bfb9c2f67`, and diff check. Final independent bounded adversarial re-review returned exactly `PASS`. Files: the same three QA modules plus this ledger. No spec, certificate, historical generation manifest, generated candidate, runtime, UI, truth, replay, or dependency changes. Strike count: 0; one malformed inline RED command and two integration/lint corrections were verifier/framework composition, not failed implementation methods.

PVP005-ARDY-V4-REVIEW-HARDENING-001 — PASSED with no implementation repair. The reproduced external/altered W0, false-hash, `last_outcome`, and non-JSON `NaN` bypasses now fail closed. Spec/contract/certificate paths resolve under repository containment and reject absolute, traversal, and symlink escape; source bytes are read once, hashed, then parsed; strict JSON rejects constants, exponent-overflow infinity, and duplicate keys; the pure materializer rejects non-finite direct inputs, exact-whitelists event schemas, recursively forbids combat-result keys, and encodes with `allow_nan=False`. Canonical materialized SHA-256 remains `06f7bbf2a600ce5068bdc9d73e35eabf9905762859a06fbe22c728bb36337a1c`; normalized ARDY condition hashes remain unchanged. Historical certificate/manifest bytes remain immutable, and the hardened generator rejects the consumed certificate on generator-SHA drift. Fresh targeted ad-hoc evidence `validation_evidence/pvp005-v4-review-hardening-20260716T005044Z/manifest.json` records 14 zero-return gates and SHA-256 `50d46d85b98ddf21aa409d30b1863ef81345e728511d57a69d6452e1f14ff9b9`: Python/Ruff, security probes, positive/negative endpoint gate, sparse and normalized conditions, revision baseline, formatting, warning-denying Clippy, all-target tests (library `131` passed/one provisioned-artifact ignore, binary `134` passed, capture `2` passed), headless autoplay/verify hash `d1a3cc1bfb9c2f67`, and diff check. Earlier ad-hoc bundle `.../pvp005-v4-review-hardening-20260716T004853Z/` stopped only because its verifier referenced the historical manifest key as `materialized_sha256` instead of `materialized_constraints_sha256`; source gates had not failed and the corrected fresh bundle passed. Files: the three named QA modules plus this ledger. No spec, certificate, historical generation manifest, runtime, UI, truth, replay, dependency, or generated candidate changes. Strike count: 0.

JD-001A-ACTIVE-M3-TERMINAL-REPLAY-CLOSURE — PASSED with no repair. A RED regression proved eight direct `Session::tick()` calls after `MatchResult` appended eight duplicate terminal replay hashes (`103 -> 111`) while the snapshot/hash remained stable. `Session::tick()` now returns before both game advancement and replay append at terminal. The focused regression passes; all nine M3 tests pass. Fresh targeted ad-hoc evidence at `validation_evidence/jd001a-terminal-replay-20260716T003403Z/manifest.json` records 15 zero-return gates: formatting, warning-denying Clippy, all-target tests (library `131/131` with one provisioned-artifact ignore, binary `134/134`, capture `2/2`), headless autoplay and replay verification at frame `342`/`343` hashes with terminal hash `d1a3cc1bfb9c2f67`, current Python/v4 gates, revision baseline, and diff check. Manifest SHA-256 `76e7e489e7a77c8fd5b36c68406b03ad95311f34269946d1077cccae32c505d3`. Files: `src/milestone3.rs` and this ledger only for this unit. No replay format, contact authority, UI, motion, dependency, or serialization change. Strike count: 0.

PVP005-ARDY-V4-ENDPOINT-GENERATION-001 — HALTED before admission after one content-addressed two-seed-per-action batch produced six finite, provenance-bound ARDY G1 proposals and all six failed the required structural/semantic screen. The exact generator-ready contract, W0 materializer, sparse ARDY constraint builder, and normalized condition hashes pass; default generation remains disabled and the one-shot certificate is consumed by its bound output path. Generated rotation channels did not honor position-only endpoint conditions tightly enough for FK: root XZ residual `0.024..0.106 m`, hand residual `0.479..1.018 m`, and planted-foot residual `0.055..0.133 m`. Structural failures reached `3.0815 rad/frame`, `0.2973 m/frame`, and `0.2437 m` contacted-foot drift; blinded first-eight-frame review found near-static, mutually weak tells. Same-path extra seeds and runtime promotion are forbidden. Next falsifier: rotation-aware/full-body constraints whose compiled mask covers the rotation channels consumed by FK, with sub-centimeter hand/foot residual and declared-contact parity gates before any new visual batch. Receipt: `docs/reports/PVP005_ARDY_V4_GENERATION_RECEIPT.json`; ignored raw evidence: `validation_evidence/pvp005-ardy-v4-generation-20260716T001310Z/` and `validation_evidence/pvp005-ardy-v4-screen-20260716T001310Z/`.

ASSET-REVIEW-STUDIO-001 — PASSED after falsifying and replacing two rendering assumptions. Added dependency-free ForgeLens: a loopback-only Python-stdlib server plus pure WebGL2 Command/Inspect browser surface that indexes five current GLBs, measures metadata, groups pipeline stages, supports smooth damped orbit/pan/dolly, material/clay/normals/wireframe/grid modes, A/B ghost comparison, drag/drop GLB inspection, surface-space pins, comments/severity/status, decisions, design gates, evidence browsing, screenshot/export, and atomic review persistence under ignored `qa_runs/asset_reviews/`. Animated GLBs add clip/timeline/speed/loop controls, CPU skin evaluation, animated-bounds framing, deterministic eight-frame evidence capture, persisted criterion-level neural audit reports, and a hard block on human approval until `neuralMotion.status=pass`. GPU joint-uniform and float-texture paths were falsified by measured WebGL `INVALID_OPERATION`; both were removed rather than hidden, and CPU skinning plus explicit model-VAO attribute rebinding eliminated the grid-buffer alias failure. Five server/security tests pass; `node --check`, Python compilation, and `git diff --check` pass; `/`, catalog, review, JS, and CSS return HTTP 200. Browser QA found zero JS-console errors, preserved a live human scale comment across reload, and proved failed-neural approval is blocked. Neural contact sheet `qa_runs/asset_reviews/evidence/8931d004d4e498b7bbdd/7b549437a1a588f72925.png` shows the full armored duelist in all eight frames with intact static skin/materials; neural review correctly fails motion acceptance because the 0.300 s source clip is visually static. Files: `tools/asset_review.py`, `tools/asset_review/{README.md,index.html,styles.css,app.js}`, `tools/qa/test_asset_review.py`, and this ledger only; no Cargo/runtime/truth/asset source changed. Rollback: remove only these files and ledger entry.
Fresh system-guard evidence: OS-tempfile verifier `/tmp/hermes-verify-orwzbr39.py` passed artifact, Python compile, 5/5 focused tests, JS syntax, diff, static-contract, live loopback HTTP, catalog-metric, and security-negative gates, emitted `AD_HOC_VERIFY=PASS`, and removed itself. Log: `qa_runs/asset_reviews/verification/20260714T230636Z/ad_hoc_verify.log`; SHA-256 `20b127ce048110d100a04283529a2dfb3ab2d8bda3b8e704b55fc34c9e60bcfa`. Human-review policy: launch ForgeLens in the user's regular browser; `--no-open` is automation-only.

Prior PVP-005 visual/motion units remained paused and unchanged during this explicitly requested developer-tool unit.

PVP-005-VISUAL-CONTRACT-FOUNDATION — PASSED with no repair. A versioned, hash-bound contract now makes the required 16-view/layered harness falsifiable before render implementation. It fixes exact 2048x2048 4x4/512-tile orbit sheets with 16 azimuths from 0 through 337.5 degrees in 22.5-degree steps, pinned 60 Hz first-eight-frame sampling, first-six-frame human subset, live first-person camera constants, ten required output layers, explicit physical/visual/timing/perceptual thresholds, regression/baseline policy, candidate/admitted/live scopes, and hashes for the candidate packet, carrier, weapon, and current shaders. The maximin OKLab plus WCAG contrast selector proved no palette color meets 4.5:1 against both declared bronze actor and dark weapon; it therefore selected the required action-set-locked complementary pair `charcoal+offwhite`. Five focused tests pass for the complete committed contract, exact angle rejection, paired selection, single-background preference when possible, and color-metric symmetry. Config SHA-256 is `fa233a1ae2157ed4704d613088b2d048c205407b2c58211caf30f166612bc615`; repository-verifier and CI wiring pass syntax/lint checks. This is configuration/CI foundation only, not render or admission evidence. Rollback: remove only this config/validator/test wiring. Next: implement one-pass pinned WGPU beauty/object-ID/normals/depth tiles and deterministic sheet assembly.

PVP-005-GATE0-REACHABLE-BASELINE — PASSED after one branch-tracking repair with no source/runtime change. Published the intact five-commit chain as `pvp-005-readable-live-motion` at feature baseline `4e481ccd59602c1cb4eda97183c32dec48f9a801`, then committed and pushed reconciliation revision `99de72c87a64fdc994c7b044b5fb17cb4295e36c`; public `main` intentionally remains at ancestor `2677b4a7dd050e7f4c5ee03881aa16035e413a8b`. Historical PVP-002/003/004 reports retain their evaluated ancestor revisions and are not relabeled or combined with PVP-005 evidence. Repair 1 corrected only local upstream bookkeeping after `push -u` initially attached local `main` to the feature remote; `main` again tracks `origin/main`, while the matching local feature branch tracks its remote. `verify_pvp005_revision_baseline.py --remote` passes ancestry, remote reachability, status reconciliation, and candidate-manifest checks. The exact published reconciliation commit passed `tools/verify_playable_repo.sh`: 119 library + 116 binary + 2 live integrations, two byte-identical package assemblies, package manifest SHA-256 `6ffd6c3eca8d90e07622dd50617817dcc83f2ae1fb0964fc66435414588ece02`, and replay hash `d1a3cc1bfb9c2f67`. Rollback: revert the reconciliation commits only; do not delete published history. Next: build the pinned 16-view/layered visual harness before any live-pose change.

PVP-005-ADMITTED-THREE-ACTION-MOTION — IN PROGRESS. Hypothesis: one provenance-recorded Strike/Block/Grab source set can satisfy ordered raw-source continuity/readability gates and deterministic 24-bone retarget receipts before any runtime promotion. Goal: admit all three action sources with first-eight-frame semantic tells, finite/connected retarget output, weapon-hand intent where applicable, and exact source/model receipts. Expected files: bounded motion-source tooling, ignored candidate evidence, an admission report/manifest, tests, docs, and ledger closure; `App::current_pose()` remains bind pose until admission passes. Baseline: local package and OS input path pass, but both actors remain in bind pose and prior navigation/Dodge evidence cannot prove Strike/Block/Grab. Validation: source provenance/hashes, ordered strips, continuity/topology thresholds, deterministic retarget receipts, independent visual review, warning-denying tests, and unchanged M3 replay hash. Rollback: revert only PVP-005 admission tooling/reports; no live runtime pose mutation is permitted in this unit. Strike count: 2. Initial fresh-candidate generation failed before model load because the CLI registry rejects the Hugging Face repository name `ARDY-G1-RP-25FPS-Horizon52`; Repair 1 changed only the model argument to `Kimodo-G1-SEED-v1` and preserved the failure log. Repair 1 succeeded for all actions: 24 fresh 90-frame, 25 Hz candidates were generated from fixed prompts/seeds. All 24 passed finite/topology/continuity gates with explicit thresholds: segment drift `<=0.0001 m`, joint step `<=0.20 m/frame`, angular step `<=0.70 rad/frame`, contacted-foot drift `<=0.05 m`, rotation error `<=0.001`, root height `0.45..1.20 m`, and maximum position magnitude `<=3.0 m`; screen report SHA-256 `0c98725d63e01ba9dad693fbb5d810ac6fd4ae008330755dd2d83b270ff1c1a5`. Full source overviews and C0 retarget reveal strips selected `strike_02` frames 54–61, `block_07` frames 6–13, and `grab_07` frames 10–17. On the armored carrier, Strike descends from overhead, Block rises from chest guard, and Grab reaches forward from a crouch. The QA-only weapon exposed a separate Block socket crossing defect, so socket parity remains explicitly unpassed. The self-contained candidate packet `assets/motion/pvp005_candidates/manifest.json` hashes to `0943680512230b50366366bb69182131595311a5823f281e5ba035dec9a64755`; it binds generated NPZ/F413 bytes, C0 strips, carrier, retargeter, render harness, ARDY code/model license evidence, model snapshot, prompts, seeds, and measured metrics. Its status is `pending_blinded_human_trials`, `runtime_promoted=false`, and `public_distribution_ready=false`. `verify_pvp005_candidate_packet.py` passes and a deliberate one-byte F413 mutation fails closed on size drift before exact restoration. `run_pvp005_visual_harness.sh` regenerates all 96 C0 action/view frames and ordered front/side strips; its first direct launch failed because the new file lacked its executable bit, and Repair 2 set only that mode before the same command passed. The fresh mechanical visual report hashes to `6d4166e6d065560e1519791b2e9d500826dbb7d4d3fe9b1c1e944356e71ba3f2`. `analyze_pvp005_c0_visuals.py` measures carrier occupancy, adjacent-frame motion, exact dimensions, image hashes, and unique pose receipts while explicitly reporting semantic-human and socket/contact gates as pending. `blinded_motion_trial.py` prepared 20 balanced participant packets (60 labels total), rejects all 60 missing responses, and its scorer passes a synthetic plumbing fixture only; no synthetic result is admission evidence. Human response collection remains required before promotion. Next: commit this hash-bound pending packet after warning-clean validation, then collect genuine blinded responses; do not wire live poses yet.

B15J-B15C-REVIEW-HARDENING-001 — PASSED with no behavioral repair after one initial no-op V4A edit-path failure. Applied the delayed independent review against current code: packet validation now requires root tick 0; active admission requires all 34 tick-0 joint poses instead of silently using identity; root PD is expressed as integer millinewton force divided by the 70,000 g root mass; impulse and kinetic-energy units are explicit; cross products saturate through i128; offset conversion is checked; isotropic gains are documented. Added exact receipts for 70 kg at 1 m/s = 35,000 mJ and 0.05 kg*m^2 at 1 rad/s = 25 mJ, plus missing-root/joint, before-validity, expiry, plan-mismatch, tick-mismatch, non-anticipation, and saturation tests. Focused tests passed motion plan 5/5 and tracker 9/9. Trace-visible OS-tempfile verification passed `AD_HOC_B15J=PASS` and removed itself. Warning-clean all-target tests passed library 116/116 and binary 115/115 plus both live MotionBricks integrations; 100-reconstruction truth-hash gate passed. Review findings already satisfied without edits: `JOINT_COUNT` derives from `MAX_MOTOR_TARGETS`, and module scope explicitly says independent joint/root dynamics rather than coupled articulation.

B15H-INTEGER-HINGE-PROJECTION-CORE-001 — PASSED with no repair. Added `src/hinge_projection.rs` and crate exports. Integer-only CORDIC projects validated Q15 6D local bases onto one signed one-hot Q30 hinge axis; malformed bases/axes fail closed. Serial focused tests passed 4/4 for +/-0.5-rad X/Y/Z recovery within 100 microrad, pi quadrant determinism, identity/off-axis zero, and invalid-input rejection. Distinct OS-tempfile verification passed `AD_HOC_B15H=PASS tests=4 serial=true integer_only=true` and removed itself. Warning-clean all-target check/test passed library 113/113 and binary 112/112 plus both live MotionBricks integrations; 100-reconstruction truth-hash gate passed.

B15G-G1-HINGE-TARGET-ADAPTER-001 — HALTED and fully rolled back at Strike 2 on the temporary-verifier approach. Project implementation itself had passed focused adapter tests 4/4, model tests 4/4, tracking tests 6/6, warning-clean all-target tests (library 113/113, binary 112/112, two live integrations), and 100-reconstruction truth-hash. However, ad-hoc verifier creation first failed with an unterminated f-string; after Repair 1, its second repeated test run interleaved parallel test-harness stderr/stdout (`targets=ok29`) and failed receipt parsing. Per atomic discipline, no second verifier repair was attempted. Removed `src/g1_hinge_adapter.rs` and restored `src/lib.rs`/`src/main.rs`; `git status` returned clean. Next falsifier B15H isolates only CORDIC projection and uses serial assertion-based verification without output parsing.

B15F-G1-ARTICULATION-MODEL-001 — PASSED on Repair 1. Added `src/g1_articulation.rs` and crate exports with 34 topology nodes, 30 physical bodies, 29 actuated hinges, and four explicit zero-mass virtual markers. Stored integer body/frame offsets, body and inertia quaternions, COM, mass, diagonal inertia, axis, limits, rotor armature, friction loss, and torque bounds from official commit `021df739f0b36e514399f0030e3a195683a46383`; total physical mass is `33,341,142 mg`. First focused gate exposed genuine left/right knee inertia asymmetry (`<= 6,500 nano-kg*m^2`, about 0.058%); Repair 1 replaced exact bilateral inertia equality with a strict 0.1% relative bound while preserving exact mass/torque/topology checks. Focused model tests passed 4/4; existing tracking tests passed 6/6; warning-clean all-target test passed library 109/109 and binary 108/108 plus both live MotionBricks integrations; 100-reconstruction truth-hash gate passed. Independent OS-tempfile parser compared every quantized field of all 34 Rust records against the hash-verified Python/MJCF sources and passed `AD_HOC_B15F=PASS`; temp file removed.

B15E-MOTIONBRICKS-PROVENANCE-RECOVERY-001 — PASSED. Repository receipts named upstream but did not pin a commit; GitHub MCP failed with `401` then circuit-open, so the public Git transport/raw path was used. Official `NVlabs/GR00T-WholeBodyControl` `refs/heads/main` resolved to immutable commit `021df739f0b36e514399f0030e3a195683a46383`. Local and raw-upstream bytes matched exactly: `motionbricks/motionbricks/motionlib/core/skeletons/g1.py` SHA-256 `6f5bc54dfafed7fb8ccd6133ed6a5114ad5c260bbfb01354ab2bd121ae641eac` (8,097 bytes); `motionbricks/assets/skeletons/g1/g1.xml` SHA-256 `5d76cf92f00dd49d6eb9fae38d7d38e46886848b602ac691051e886c3bcccfb1` (31,881 bytes). No runtime source changed.

B15D-G1-ARTICULATION-MODEL-001 — HALTED at Strike 2 before project source modification. Provenance probe 1 found `/run/media/vdubrov/NVMe-Storage1/gr00t/motionbricks` is not a Git worktree (`git rev-parse HEAD`, exit 128). Probe 2 found its parent `/run/media/vdubrov/NVMe-Storage1/gr00t` is also not a Git worktree (exit 128 at filesystem boundary). Local topology/XML bytes exist, but no revision may be asserted from unversioned directories. Next falsifier: B15E searches durable receipts or exact official GitHub history and requires local byte-hash equality before restarting the model import.

B15C-ACTIVE-RAGDOLL-TRACKING-SOLVER-001 — PASSED with no failed repair. Added isolated `src/active_ragdoll.rs` and crate exports. It compiles sealed plan pose/root targets into 34 bounded spherical-joint motors, advances 34 independent integer rotational states plus root servo dynamics at 120 Hz, supports explicit root/joint impulses, preserves accepted plan bytes/hash, rejects future-target anticipation and missing tick-0 root state, and emits a deterministic state hash/kinetic-energy receipt. It is explicitly not yet parent-child rigid-body coupling, gravity, joint limits, contacts, or balance constraints. Measured gate: orientation error `16384 -> 31 q15`; peak kinetic energy `1260 mJ`; root `[213, 1000, -118] mm` with zero vertical drift; impulse displacement `[20, 0, 0] mm`; deterministic final state hash `10931853318179883807`. Focused tests passed 6/6; warning-clean all-target test passed library 105/105 and binary 104/104 plus two live MotionBricks integrations; 100-reconstruction truth-hash gate passed. Direct OS-tempfile ad-hoc verifier `/tmp/hermes-verify-zz1v90bp.py` passed `AD_HOC_B15C=PASS` and was removed. Files: `src/active_ragdoll.rs`, `src/lib.rs`, `src/main.rs`.

B15B-SPHERICAL-MOTOR-SCHEMA-001 — PASSED on Repair 1. Replaced the invalid one-scalar-per-joint controller packet with quantized 6D spherical-joint orientation and three-axis angular-velocity targets while retaining gains and bounded torque. Pose and motor packets now reject non-unit/non-orthogonal 6D bases. Initial compile failed because slice `.map` was not an iterator; Repair 1 used fixed borrowed iterators. Focused motion-plan tests passed 5/5, warning-clean all-target check passed, and `git diff --check` passed. File: `src/motion_plan.rs`; no solver/runtime wiring.

B15A-ACTIVE-RAGDOLL-TRACKING-SLICE-001 — HALTED at Strike 2 before source modification. Two V4A multi-hunk attempts to correct the motor schema failed patch-context disambiguation (`96 matches`, then `3 matches`) and applied no source hunks. Per atomic discipline, no third V4A attempt was made. Baseline remained green: motion-plan 5/5 and warning-clean all-target check. Next falsifier: B15B isolates the schema correction and uses unique replace-mode edits; the articulated solver moves to a separate B15C only after B15B passes.

B14Z-ARDY-G1-FEASIBILITY-SERVICE-001 — PASSED on final Repair 2. Cloned official Apache-2.0 source at `693f74d13b3d04a0a22ce127ee79c929dd89756b`; Hugging Face identity and actual gated Llama config download passed. Loaded `nvidia/ARDY-G1-RP-25FPS-Horizon52` revision `059b8007df0ba194a006a877b59a563955ac7b70` on RTX 5090 (G1Skeleton34, 25 Hz, 52-frame horizon, 4 frames/token, 10 denoising steps). Model manifest SHA-256 `b1e200fd4c8c30f0eee2bf0b957a47f6e6297c068f6f741ac6548cdd1a1a4708`; license-file SHA-256 `11ccdcb47db87ac30b63765b5e5154403026c4fbb52c19919d99d912ec454e19`. Added isolated JSON-lines `ardy_service/generate.py`: constraint-only, post-Reveal phases, bounded root targets, integer mm/q15/q16 output, model/source/license receipts, canonical proposal SHA-256, no outcome fields. Same-seed requests produced byte-identical quantized proposals; Commit-phase input was rejected. Three seeds produced distinct plans with endpoint errors `16.492`, `3.000`, and `21.840 mm`; warm generation `55.515..57.790 ms` after a `249.227 ms` first request. Initial 8-frame probe failed a 0.292 m target; Repair 1 showed the mask was populated but short-horizon control remained weak. Final Repair 2 used the intended 52-frame model and same-seed free/constrained comparison: error `0.604618 -> 0.016349 m`, causal endpoint delta `0.619394 m`, error reduction `0.588269 m`. Evidence: `qa_runs/m3_contact_truth_001/b14z_ardy_service/`.

B14Y-ATOMIC-MOTION-PLAN-SCHEMA-001 — PASSED on final Repair 2. Added integer-only `MotionPlanPacketV1`, intent grammar, ARDy/MotionBricks/normalization receipts, root/end-effector/desired-contact/pose/balance targets, parent-linked replans, canonical postcard bytes plus FNV-1a hash validation, `MotorTargetBatchV1`, and solver-derived `ImpactEventV1`. Replay now rejects unsealed plans and invalid impacts and records validated packet/event payloads. Focused tests passed 5/5. Full pinned CI passed warning-clean: library 99/99, binary 98/98, both live MotionBricks integrations 2/2, and the 100-reconstruction truth-hash gate. Repair 1 exported existing replay through the library crate; Repair 2 declared `motion_plan` in the binary crate's separate module list. Files: `src/motion_plan.rs`, `src/replay.rs`, `src/lib.rs`, `src/main.rs`. No runtime behavior is connected.

B14X-MOTION-PLAN-ACTIVE-RAGDOLL-SCHEMA-001 — HALTED at Strike 2 and fully rolled back. The complete integer-only packet/controller/impact schema compiled and its six focused tests passed, but the subsequent replay-acceptance test insertion failed twice in the targeted patch mechanism even after re-reading rustfmt-updated context. This was an edit-path failure, not a schema/test failure; no third patch was attempted. `src/motion_plan.rs`, its `lib.rs` export, and replay envelope were removed/restored. `git status` returned clean on `main`; no B14X code remains. Next falsifier: B14Y writes the complete tested new file atomically before touching existing modules, avoiding incremental test-module patching.

B14W-ONLINE-ATTACK-CONDITIONED-DEFENSE-SOLVER-001 — PASSED as a design/source-falsification unit. `docs/design/ONLINE_MOTIONBRICKS_INTERACTION_SOLVER.md` defines quantized 120 Hz inputs, versioned neural conditioning, 30 Hz transient receding-horizon solves, deterministic seeds, authority separation, latency/continuity gates, and perturbation cross-product tests; `IMPLEMENTATION_PLAN_3ACTION.md` no longer specifies `generate_action_clip` or marks clip generation complete. Direct source inspection established that released inference retains only first-four plus final-four pose targets (`motion_inference.py:266-285`) and the root model has fixed four-start/four-end embeddings (`root_backbone.py:91-97`). Live probe proved pose/root text channels disabled, no interaction parameter, 4 frames/token, 6..16 tokens, and adapter 4+4 concatenation. Evidence: `tools/qa/probe_motionbricks_online_contract.py`, `qa_runs/m3_contact_truth_001/b14w_online_solver_contract/metrics.json`; `B14W_RELEASED_CHECKPOINT_REJECTED_FOR_ONLINE_CONDITIONING=PASS`. Conclusion: current checkpoint can interpolate endpoints but cannot be the required live solver without a trained root+pose interaction-conditioning extension.

B14V-MOTIONBRICKS-WRAPPER-GENERALIZATION-001 — PASSED on final Repair 2. Initial 1e-5 decoder training improved held-out Strike/Grab but regressed Block by 130.04%. Repair 1 at 1e-6 reduced Block regression to 2.54%. Final Repair 2 added 2x Block replay on non-Block updates and passed every held-out action: Strike `6.079595 -> 5.033248` (+17.2108%), Block `0.103348 -> 0.086924` (+15.8919%), Grab `5.915506 -> 4.889575` (+17.3431%). Train sources `[B_Fence1, block_04, grab_02, grab_03, strike_00]` and validation sources `[B_Fence2, grab_06, strike_01]` are disjoint. Decoded max coordinate `1.723412 m`; root height `[0.572004, 0.986804] m`. Checkpoint: `qa_runs/m3_contact_truth_001/b14v_wrapper_generalization/motionbricks_pose_vqvae_combat_corpus.pt`, SHA-256 `d236c68dc4bfd67481c9297eab96154e17e07843d200f2d238c484df3b3e5ee2`; `B14V_WRAPPER_GENERALIZATION=PASS`.

B14U-MOTIONBRICKS-DECODER-GENERALIZATION-001 — HALTED at Strike 2 and rolled back. Initial run failed before model load due a wrong helper import. Repair 1 reached validation but addressed the inner `VQVAE` as the wrapper (`local_motion_rep` absent). Final Repair 2 used the inner `_motion_rep`, but the inner object's decoder mode is unsupported by `extract_feature_from_motion_rep`, raising `NotImplementedError` before optimization. No third repair was made. Trainer removed; logs preserved under `qa_runs/m3_contact_truth_001/b14u_decoder_generalization/`; `B14U_ROLLBACK=PASS`, QA script compilation, and `git diff --check` passed. Next falsifier must use the official wrapper object rather than manually reconstructing inner conditioning.

B14T-MOTIONBRICKS-COMBAT-DATASET-001 — PASSED. Generated 8 Block and 8 Grab ARDY candidates; structural screening passed all, visual admission selected `block_04`, `grab_02`, `grab_03`, and `grab_06`. Independent CC-BY-4.0 G1 sources `B_Fence1` and `B_Fence2` passed defensive visual gates; `B_HandsUp` failed and was excluded. `build_motionbricks_combat_dataset.py` resampled admitted sources to 30 Hz, converted them through canonical normalized 414-feature MotionBricks representation, detected action-specific phase windows, and wrote source/feature hashes. Corpus contains 8 trajectories with train+validation for each action: Strike 2, Block 3, Grab 3. Manifest: `qa_runs/m3_contact_truth_001/b14t_combat_dataset/corpus/manifest.json`, SHA-256 `14accbe8efca3fc1066fc26a0c918f4d0efa80a7c9423b2cb8c389f249d6d760`; `B14T_COMBAT_CORPUS=PASS` and `git diff --check` passed.

B14S-MOTIONBRICKS-COMBINED-COMBAT-001 — HALTED at Strike 2 and rolled back. Initial combined generation was coherent in vertical root but inherited locomotion horizontal drift (`6.963006 m`). Repair 1 added a stationary horizontal-root policy and passed structural gates (`max_abs=1.29556 m`, root height `0.70780..0.82034 m`, downward hand travel `0.20416 m`), but visual QA failed at 96% confidence for tangled/ambiguous limbs and no coherent descent. Final Repair 2 trained the decoder 240 steps (`5.96162 -> 1.16544`, 80.451% reduction), yielding bounded deterministic output and `0.39450 m` downward hand travel, but visual QA again failed at 97% confidence: deformed/crossed limbs, unstable torso, no impact/follow-through. No third repair was made. The B14S probe script was removed; logs, weights, metrics, and storyboard remain in `qa_runs/m3_contact_truth_001/b14s_combined_combat/`; `B14S_ROLLBACK=PASS` and `git diff --check` passed. Mechanism: a single locomotion-code sequence plus one teacher trajectory is insufficient to learn a coherent combat manifold.

B14R-MOTIONBRICKS-COMBAT-VQVAE-001 — PASSED on final Repair 2. Full VQVAE adaptation was unstable: commitment loss began at `54,012,932`, reconstruction worsened `5.96211 -> 54.21009`, and decoded root escaped `-8.15..12.52 m`. The final repair froze the released encoder/codebook, encoded the teacher once into native MotionBricks tokens, and adapted only `12,434,717` MotionBricks decoder parameters. Reconstruction loss decreased `5.961620 -> 4.728476` (20.6847%) with finite gradient norm `[3.50425, 34.28867]`; decoded coordinates remained within `1.455412 m` and root height `[0.756881, 0.884546] m`. QA checkpoint SHA-256 `b459d107fffaf178096598fb2ed464741ed308486f3cfb878190d85a7e933d1d` at `qa_runs/m3_contact_truth_001/b14r_combat_vqvae/motionbricks_pose_vqvae_combat_overfit.pt`.

B14Q-MOTIONBRICKS-FINETUNED-GENERATION-001 — HALTED at Strike 2 and rolled back. Initial native inference was deterministic but malformed: generated hands were `-39.41..-51.03 m`, target RMSE `10.7277 m`, and visual QA showed a blank/off-frame bottom row. Repair 1 limited training to the final transformer block/output head (`8,481,872` parameters, `1e-6`, 12 steps; loss `5.40331 -> 4.85408`) but reproduced the same failure (`10.6665 m` RMSE). Diagnostic inference with the untouched released backbone produced the same RMSE, localizing the dominant failure to the frozen representation/decoder path. Repair 2 used MotionBricks-native neutral context with teacher endpoint conditioning; it improved maximum coordinate magnitude to `7.215359 m` but still violated the 5 m sanity bound. No third repair was made. `probe_motionbricks_finetuned_generation.py` and its pycache were removed; logs remain in `qa_runs/m3_contact_truth_001/b14q_finetuned_generation/`; `B14Q_ROLLBACK=PASS` and `git diff --check` passed.

B14P-MOTIONBRICKS-COMBAT-FINETUNE-001 — PASSED. The official service's restored `PoseMotionModel` converted the 50-frame teacher through the canonical normalized 414-feature representation, froze VQVAE/all non-backbone parameters, and updated 136,588,272 pose-backbone parameters on one fixed 8-token batch. After adding the minimal Lightning `global_step` context, 24 AdamW steps reduced identical-batch cross-entropy from `5.403310776` to `0.001938970` (99.9641%) with finite gradient norm `[0.324743, 1056.231567]`. QA checkpoint: `qa_runs/m3_contact_truth_001/b14p_combat_finetune/pose_backbone_one_batch_overfit.pt`, SHA-256 `bfd70ab2979a4f5bd3cb69020b3199ec88a5121a4d53b635257c907da6c5614c`; teacher SHA-256 `f68026e856b3aea790dc7a5e0a0eed9ad6b37868468723fecd7d912a5c3f0a4b`. This proves trainability, not generated-motion semantics.

B14O-MOTIONBRICKS-SPECIFIC-TARGET-001 — FALSIFIED by upstream source inspection without code changes. `full_agent._override_target_transforms()` applies `specific_target_positions` only to 2D root positions and headings (`full_agent.py:298–318`); `_generate_target_joint_transforms()` still samples all target poses from the locomotion clip holder (`full_agent.py:321–344`). Live checkpoint inspection also reports `ACCEPT_TEXT_EMB_INPUT=False`, no text projection head, and no text embedding dimension. The released runtime surface cannot express hand/weapon combat targets.

B14N-MOTIONBRICKS-TEACHER-TRACKING-001 — HALTED at Strike 2 and rolled back. The first run failed because a QA script launched from `tools/qa` lacked the repository root on `sys.path`; repair 1 reached MotionBricks but fed a 64-row dense condition tensor into the root backbone's 8-row contract. Repair 2 separated root and pose contracts but its replacement method still validated the upstream 8-row batch instead of the separate dense pose tensor. Per fail-fast policy, no third repair was made. The QA script and `__pycache__` were removed; failure evidence remains at `qa_runs/m3_contact_truth_001/b14n_motionbricks_tracking/run.log` and `run_repair2.log`; `B14N_TRACKING_ROLLBACK=PASS` and `git diff --check` passed.

B14M-G1-POSITION-RETARGET-001 remains completed evidence: source-position arm-chain retarget for the 24-bone armored C0 carrier. G1 upper-arm and forearm world directions drive C0 `LeftArm/LeftForeArm/RightArm/RightForeArm` under C0's own hierarchy and lengths. Zero-length source segments fail closed. Production `src/main.rs`, truth, replay, cleanbox, physics, world, MotionRuntime data, and player rendering remain unchanged; the retarget bridge is exercised by QA and tests only.

Authoritative source evidence: NVIDIA ARDY G1-52 generated two deterministic constrained samples from a Bones-SEED native-G1 `sledgehammering_wall_R_003__A296` sequence. Source CSV SHA-256 `9fecdc553a594358c78947ca27ab266fe89d2834bbf2a13e6deced78a2670f14`; constrained F413 SHA-256 is recorded in `qa_runs/m3_contact_truth_001/b14k_ardy_constraints/generated/SHA256SUMS.txt`. Native source frame 305 is the overhead apex and frame 325 the strongest measured downward-hand velocity. Source front/side gates show overhead wind-up → forward/down drive → follow-through.

Falsifiers: two prompt-only ARDY batches generated valid G1 but failed the C0 semantic gate. Two rotation-basis retarget repairs (terminal DOF mapping, then bind-basis conjugation) improved motion but remained asymmetric/unclear and were rolled back at Strike 2. Relative source-direction B14M attempt 1 remained lateral. Direct observed source-segment directions produced coherent overhead C0 arms and descent without inversion.

Visual evidence: `qa_runs/m3_contact_truth_001/b14k_ardy_constraints/generated/c0_retarget_position_direct/position_direct_reveal_front.png`; W0-attached descent gate `qa_runs/m3_contact_truth_001/b14k_ardy_constraints/generated/c0_position_direct_w0/c0_w0_descent_reveal_front.png`, SHA-256 `06641315d226eaea2e4df88c2fde13d78c353d2c6a420830afd18b1773f33826`. Frames 16–23 show a coherent two-hand overhead sword apex entering descent. W0 attachment is QA-only and derives the hilt from posed `RightHand` and blade direction from `RightForeArm→RightHand`.

Mechanical verification: `cargo test --locked` passed 86 library, 93 main, and 2 integration tests; `cargo check --locked --all-targets` and `git diff --check` passed. New test `source_arm_directions_place_armored_hands_overhead` proves both posed C0 hands rise above shoulders from vertical source segments. Existing deterministic receipt test now uses physically valid nonzero G1 segments.

Pending B14I: CMU Subject 02 clip 07 remains a provenance-backed alternate source, not the active retarget path.

Observed numeric bridge: `0e5a29a0cc99b7e29e259a4192ef6be3e8c8eb60` maps each cached G1 source frame into the 24-bone armored hierarchy with finite, positive-determinant skin matrices and deterministic receipts. All-target validation passed. The bridge is intentionally not wired to `App::current_pose()`.

Falsified sources: Kimodo is unavailable without the gated Llama encoder. Public AnimationGPT/CombatMotion produced finite SMPL-22 clips after pinning `transformers==4.30.2`, but 15 seed candidates failed the first-eight-frame semantic tell gate and are not G1-cache compatible. The native G1 MotionBricks probe produced finite `[1,T,414]` features, but endpoint-only arm constraints yielded indistinguishable upright reveal frames. Evidence: `qa_runs/m3_contact_truth_001/b14_readability/B14_ALTERNATE_MODEL_GATE_20260713.md` plus the retained fresh ad-hoc verification logs.

Admitted source candidate, not runtime-promoted: `exptech/g1-moves` is CC-BY-4.0, explicitly supplies Unitree G1 mode-15 29-DOF qpos at 60 Hz, and contains martial/fencing clips. Exact B_Fence1 source bytes were hashed, converted through the official MotionBricks qpos converter into finite `[1620,414]` features, normalized into a temporary Strike primitive, and inversed through the runtime service into finite `[4,413]` source frames. Its source eight-frame panel shows a readable fencing-like one-arm posture but is not a final two-handed sword tell or a complete Strike/Block/Grab set. Preserve it as a provenance-backed Strike candidate only.

Temporal-trajectory falsifier result: action-specific four-frame G1 target qpos trajectories generated finite `[1,24,414]` streams. Source-frame 0→7 maximum joint displacement was only Strike 1.95 cm, Block 3.37 cm, Grab 2.35 cm. The kinetic `[16,24)` window showed a late unilateral Strike extension; Block and Grab remained upright and mutually ambiguous. All three fail the visual semantic gate and remain rejected.

Validation: compile the QA scripts; regenerate deterministic MotionBricks `[1,T,414]` / qpos source artifacts; assert finite, non-empty output; render first-eight-frame G1 panels; inspect every panel with vision. Do not promote to C0 or `App::current_pose()` before the semantic gate passes.

Current C0-target evidence (2026-07-13): v1 ANM1 (`[9,24]` parent-local matrices) passed structural encoding but its root offset used the carrier's large local units and pushed the actor out of standard framing; its selected axes preserved a T-like arm spread. A 15-case front C0 axis atlas identified `RightArm local X < 0` as a clean high weapon-arm direction and `LeftArm local X > 0` as the low guide-arm direction. v2 removed root translation, used those measured axes, and remained coherent across all eight rendered frames. An opt-in W0 socket (`JUSTDODGE_QA_OPPONENT_W0=1`) recovered `RightHand` world from `skin * inverse_bind^-1`; it stays connected and follows the hand. The resulting panel `qa_runs/m3_contact_truth_001/b14_readability/c0_target_strike/c0_v2_w0_renders/c0_target_strike_v2_w0_front_1_8.png` still fails the semantic tell gate: it is flat-footed with a lateral guide arm and reads as a posed weapon raise rather than a committed attack. This is the initial C0-target candidate failure, not a Strike. Next measured prerequisite: calibrate C0 upper-leg/hips local axes for a stable stagger/weight-transfer silhouette before altering the arm/W0 solution.

Strike count for B14F-C0-TARGET-STRIKE-001: 2. HALTED. Repair 1 combined measured leg-wide support with the high-arm W0 v2 configuration; the panel stayed a broad raised weapon with no committed torso/action read. Repair 2 used the full measured shoulder-plus-arm chain (right shoulder/arm high, left shoulder/arm low), widened legs, and persistent W0. Its final front panel `qa_runs/m3_contact_truth_001/b14_readability/c0_target_strike/c0_v4_w0_renders/c0_target_strike_v4_w0_front_1_8.png` (SHA-256 `b5e706e2859fb6f648ef2307feea3c61523698d58329d4b8e4bd6357f6d8d958`) remained a static high-sword elevation without visible trunk drive, step, or attack path: unclear / weak Strike about 55%, below 80%. Rollback required: discard the C0-local ANM loader, W0 QA socket path, and author script; preserve the pre-existing external-F413 harness and all runtime/truth/replay/cleanbox code. Next falsifier (not started): a constrained pose-trajectory/IK authoring method with hand goals, foot contacts, and root trajectory solved together; simple independent C0-local rotations are rejected.

No runtime, truth, replay, cleanbox, or production renderer path changed.

Procedural branch closure (2026-07-13, Strike-2 halt): direct `mujoco.MjModel` metadata loading failed because `g1.xml` mesh paths resolve only from the MotionBricks checkout. The replacement used an explicit 29-DOF schema and validated it exactly against the initialized live converter. Procedural Strike v1 was mechanically valid (`[48,36]` qpos → finite `[48,413]`) but its C0 front panel was a lateral T-like silhouette. A measured C0 single-DOF atlas established that negative shoulder roll raises the corresponding arm. Repair 1, Strike v2, used a measured high-right/low-left tell and passed mechanics but read as an unarmed raised-arm reach/wave, not Strike. Repair 2 added a QA-only W0 rigid-mesh hand socket recovered from `skin * inverse_bind^-1`; fresh `cargo build --locked --bin shot` passed and the sword attached visibly to the raised hand. The final eight-frame front panel `qa_runs/m3_contact_truth_001/b14_readability/author_poses/c0_strike_v2_w0/strike_v2_w0_front_reveal_1_8.png` (SHA-256 `69b899eb51da31b021efb5872dc6ff7fd7cc836e584cd8550bfa934861c2e382) still failed the semantic gate: high diagonal weapon but near-static temporal evolution, weak full-body weapon contrast, and no decisive travel/weight transfer; blind Strike confidence about 65%, below the 80% bar. No runtime/truth path was changed.

Rollback: completed for this failed approach — discard the procedural authoring tool and only the new W0/socket and camera-filter hunks in `shot.rs`; preserve pre-existing external-F413/timestamp harness changes, source cache, bind-pose runtime, truth, cleanbox, replay, and retarget bridge.

Strike count: 2 for procedural-qpos authoring. HALTED. Do not make further qpos-angle or QA-socket repairs.
Next falsifier (active by autonomous selection after the Strike-2 pause): author an opponent-visible C0 local-space combat trajectory with a persistent W0 grip/socket and measured root/stance travel, then determine whether this carrier can clear the first-eight-frame visual gate without treating G1 qpos as the presentation authority.
Current status: procedural G1-qpos and simple independent C0-local rotation routes are rejected for Strike. `b14g-constrained-strike` is active by autonomous selection after the Strike-2 pause; it must remain QA-only and preserve truth/runtime isolation while it probes coupled hand-goal, foot-contact, and root-path constraints.

### DODGE-SOURCE-EXTERNAL-001 closure record
- Probe: CMU Graphics Lab Motion Capture Database, subject 14 / trial 02. Official FAQ (`https://mocap.cs.cmu.edu/faqs.php`) verified 2026-07-12: motion data may be copied, modified, or redistributed without permission.
- Source: `data/cmu/14_02.bvh`, SHA-256 `ec705327a1442b3e5679bbbfa1aef31c8d845936b63c4c91462227af89cbd7f5`.
- Conversion/provenance receipt: `bones-seed/derived/external_dodge_qa/cmu_14_02/cmu_14_02.receipt.json`.
- Raw carrier: context `[100,220)`, 120×413 LE-f32, SHA-256 `1915ee77d1f7de779b67ea43fce4e16a3634c1410ef585fb31924211ea66f6f1`. Mechanical gates passed (finite; 3.527m total bone length; 0.450m max context XZ displacement; 0.548m within labeled `[130,160)`; unit heading norm).
- Raw human gate: FAIL. Human verdict: `FAIL` on the raw G1 side/top sheets. Evidence retained at `cmu_14_02.raw_source_sheet.png` and `cmu_14_02.raw_source_sheet_top.png` alongside their SHA-256 values in the receipt.
- Result: source rejected before C0 numeric/render gates. No `src/` files changed, no C0 work executed, no runtime wiring admitted.
### DODGE-SOURCE-SELECTION-LATERAL-001 closure record (corrected human gate)
- Source: `ib_dodge_270_R_003__A437`; active interval `[30,363)`; mirror `_M` rejected by family extension.
- Corrected gate artifact: `bones-seed/derived/lateral_dodge_qa/ib_dodge_270_R_003__A437/c0_lateral_dodge_human_gate_iso.png` (1920×720, source frames `[0,30,143,280,362]` = reference/onset/peak/recovery/terminal). Render script: `tools/qa/render_c0_human_gate.py` (ISO orthographic, lateral-displacement-preserving, labeled, deduplicated).
- Mechanical/C0 numeric gates passed: 163 bones, 14,517 vertices, skin determinant `[0.999996185, 1.000001431]`, target local-length error `0 m`, onset→terminal lateral centroid displacement `0.929 m` in Blender Y (= `-0.929 m` engine Z).
- Human visual gate: FAIL. User verdict: "visual failures completely." The corrected render was properly framed and displacement-preserving; the human semantic judgment is the final authority.
- Result: not admissible as a C0-presented lateral Dodge. No `src/` files changed; C0 fallback preserved. Causal attribution between source semantics and C0 presentation/retarget characteristics remains unresolved — the raw G1 interval was mechanically coherent, but the human-rejected artifact is the C0-presented result.
- QA evidence retained: `tools/qa/render_c0_human_gate.py`, `tools/qa/derive_lateral_dodge_active_window.py`, curated probe manifest/stream, `c0_lateral_dodge_human_gate_iso.png`.

### Prior automated closure record (retained for accuracy)
The lateral candidate was originally closed 2026-07-12 based on automated vision analysis of a render (`c0_lateral_dodge_calibration.png`) built by `render_c0_calibration_sheet.py`. That render: (a) uses a front-facing camera that compresses the Z/lateral axis, and (b) centers each imported OBJ independently at Y=0, stripping the measured ~0.93m lateral displacement. The automated vision conclusion ("dance/balance gesture") is therefore unreliable. The numeric retarget gates (determinant, axis scale, length invariants) passed. The candidate was reopened for a corrected human visual gate; that corrected gate has now failed by explicit human verdict.

### DODGE-SOURCE-SELECTION-001 closure record
- Scope: 12/12 indexed dodge IDs screened (11 in `--all` sweep + 1 negative control).
- Evidence:
  - `bones-seed/derived/screening/manifest_sweep_20260712.json` (11 entries)
  - `bones-seed/derived/screening/manifest_quick_validation.json` (1 negative control)
  - `bones-seed/derived/screening/backward_dodge_verdicts_20260712.json` (12 verdicts, real SHA-256 receipts)
  - Per-candidate receipts: `bones-seed/derived/screening/<motion_id>/<motion_id>.screening.json`
  - Screener: `tools/qa/screen_dodge_candidates.py`
- Result: 0 backward-dodge admissions.
- Per-class breakdown:
  - Raised-arm lateral gesture: `ib_dodge_back_L_001__A437`, `_M`, `ib_dodge_back_L_002__A437`, `_M` (4). Positive heading-relative backward displacement is a facing-direction artifact; pose semantics reject (upright torso, sustained one-arm-raised lateral gesture, no rearward pitch/crouch, weak/absent landing/recovery).
  - Forward/lateral evasion: `ib_dodge_270_R_001__A437`, `_M` (2). Negative heading-relative backward (forward motion), dominant lateral component.
  - Valid lateral dodge (NOT backward; flagged for future unit): `ib_dodge_270_R_003__A437`, `_M` (2). Staged prep/peak/recovery, 8:1 lateral dominance. Rejected for backward-only unit; candidates for future DODGE-SOURCE-SELECTION-LATERAL.
  - Crouch/duck: `ib_dodge_down_L_002__A437`, `_M` (2). Vertical compression, <4 cm net backward displacement.
  - Upward/jump dodge: `ib_dodge_up_R_001__A437`, `_M` (2). Vertical extension, negative heading-relative backward (forward drift).
- Runtime consequence: C0 fallback remains active; no C0 calibration or source interval derived; no runtime promotion.
- Strike count: 0 (no failed source was admitted; the 2 strikes belong to prior DODGE-PRESENTATION-RUNTIME-012).
- Frozen files: `src/main.rs`, `src/dodge_presentation.rs`, `src/truth.rs`, `src/cleanbox.rs`, `src/duel_physics.rs`, `src/duel_world.rs`, renderer, replay — no new diffs introduced during this unit. Broad dirty tree is from prior units.
- Screener note: `tools/qa/screen_dodge_candidates.py` uses `service["agent"]._converter` (private API), matching the reference validator `validate_bones_seed_g1.py` exactly. Pinned to that path; would break if the reference changes.

### 2026-07-10 approval
- User approved the MotionBricks combat direction with the non-negotiable condition that it supports the planned deep simulations. Therefore the primitive contract must preserve deterministic weapon/material/contact/anatomy information instead of encoding outcomes in an RPS label or letting generated animation supply truth.

### Prior active unit retained as pending
MB-GENERATION-SOURCE-001 — Replace the hand-reconstructed MotionBricks inference path with the official `full_navigation_agent` control cycle: official controller signal → `context_mujoco_qpos` → canonicalization/target generation → `generate_new_frames(..., force_generation=True)` → official MuJoCo conversion. Do not promote output until source-only G1 continuity/topology and visual gates pass.

### 2026-07-10 correction
- Falsified the manual direct-`inferencer.predict` integration: it bypassed the official `full_navigation_agent` canonicalization, target-transform generation, qpos filtering, and MuJoCo conversion. Its 32-frame output had max per-joint steps of `0.68–1.14 m/frame` and failed the G1 visual gate.
- Falsified measured primitive playback as a runtime substitute: C0 transfer stayed connected, but the selected CMU fragments did not yield readable Block/Grab/Thrust combat semantics. Rolled back the experimental runtime route; static C0 remains the only permitted runtime pose.
- Primary falsifier passed: NVIDIA's unmodified `scripts/interactive_demo_g1.py --has_viewer 0 --controller random --max_steps 90 --generate_dt 1.0 --pre_filter_qpos 1 --source_root_realignment 1 --target_root_realignment 1 --force_canonicalization 1 --skip_ending_target_cond 0 --random_seed 1234` exited `0` on the local checkpoint bundle. Next unit is an exact thin adapter around this known-good control path, not a new inference implementation.
- Adapter prerequisite reproduced: the process imported the wrong duplicate MotionBricks checkout first, then canonical import exposed the remaining official relative-path requirement (`out/motionbricks_pose/.../skeleton/joints.p`). The unmodified demo succeeds only when its current directory is the canonical checkout. The adapter must therefore construct `navigation_demo` under a scoped canonical checkout CWD and restore the game's CWD immediately afterward; do not alter model code or substitute checkpoint paths.
- Exact adapter now passes: `generate_official_navigation_clip(1234)` delegates to NVIDIA `navigation_demo` / `full_navigation_agent` with official controller qpos context and `force_generation=True`; the Rust integration test proved deterministic finite output with max G1 step `<0.2 m/frame`. `c0_official_motion` proved every official output frame calibrates into all 163 C0 local joints and finite positive-determinant skin matrices. This proves MotionBricks→C0 topology transport, not combat semantics: the released G1 checkpoint is a navigation model and has no Strike/Block/Thrust action conditioning.

- Files: `tools/qa/` gate script plus targeted runtime wiring/tests only after source evidence determines the valid playback path.
- Assumptions: source primitive clips are calibration evidence only; runtime must not promote generated source motion unless its G1 topology, segment-length, continuity, and C0 render gates pass.
- Baseline: primitive library contains Idle/Strike/Block/Thrust/Grab/Dodge only for `Longsword/Top`; AI currently selects all three stances; prior generated G1 contact sheet was visually invalid.
- Validation: all six source and generated clips, deterministic metric report, G1 contact sheets, C0 contact sheets for every candidate admitted to runtime, focused tests, then `cargo test --all-targets --locked`.
- Rollback: remove only MB-GENERATION-SOURCE-001 code/evidence; preserve C0 runtime closure and prior rejection evidence.
- Strike count: 0.

---

## Pending Units


- FORGELENS-HUMAN-REPORT-SUBMISSION-001: Pre-existing active-ledger entry is displaced, not silently closed. Its implementation is present in the untracked ForgeLens baseline; reconcile its separate live-browser/visual closure after FL-ID-001 rather than blending it into artifact identity.
- FL-REPLAY-003: Prove revision-bound replay/build/capture ReviewRun as the second concrete ForgeLens workflow.


- PVP-003-RUNTIME-FLOW: Add Menu, Establishing, Result/Replay, Rematch, Exit, and player instructions around the existing M3 authority.
- PVP-004-PACKAGED-INTERACTION: Prove active-M3 30/60/144 Hz equivalence, keyboard/mouse input, terminal result, replay, rematch, exit, and package launch outside the repository.
- PVP-005-PLUS: Continue the approved motion-plan, articulated-physics, pose-derived contact/socket, camera, truth-readable presentation, and packaged human-evidence units in dependency order.

- MB-GENERATION-SOURCE-001: Repair the full PyTorch MotionBricks generation path until source-only G1 renders pass skeleton topology, segment-length, context-continuity and anatomical visual gates. Generated output must fail closed before C0 skinning when these gates fail; measured primitive clips are calibration evidence, not a runtime fallback.


- ANAT-001: Build a measured deformable-anatomy spike on a provenance-clean subset (bone, joint, muscle/fascia, vessel, nerve, organ) and benchmark deterministic sparse-active 60/30/15 Hz truth against an all-structures reference. The delegated real-time trauma survey timed out without usable performance evidence; budgets remain hypotheses until this spike runs.
- JD-001A: Make `match_over` terminal inside `CombatTruth::tick()` and prove that catch-up frames record no post-terminal replay ticks. Independent review confirmed this behavior at `src/main.rs:451-465`; it predates JD-001 but remains incorrect.
- JD-002: Freeze authoritative combat/presentation schemas and ownership boundaries.
- JD-003: Wire 120 Hz shared-world swept geometry and bilateral physical packets into `PhysicalContactBatch`; synthetic `default_contact()` is removed.
- JD-004: Implement buffered, current-context MotionBricks playback for the three-action gate.
- JD-005: Complete the first player journey and replay verification.
- O1: Isolate/fix the Rust `ort` bridge using Python-saved encoder output and element-wise boundary comparison.
- R0: Replace direct G1-world→24-bone skin mapping with validated parent-local rest-frame retargeting.
- A0: Visual proof of stable humanoid deformation on the production runtime path.
- A1-A4: MotionBricks idle/strike/dodge and clip selection after R0/A0 pass.
- C1-C5: Combat loop on the accepted skeleton/retarget path.
- R1-R3: Readability and presentation polish after structural visual gates pass.
- EMERG-001: Define and probe emergent martial-arts mechanics from geometry, constraints, balance, capability, timing, and intent without technique-specific outcome scripting.
- MESHY-003: Retexture winners with Meshy-6 PBR/4K base color, remove baked lighting, then auto-rig humanoid candidates and validate skeleton/weights before MotionBricks retargeting.
- MESHY-004: Generate modular weapons, armor pieces, breakable shields, and arena assets as separate physical objects/components, not baked scene/character aggregates.
- ASSET-001: Generate or reconstruct anatomically correct layered fighter assets (Meshy only as candidate generation), then validate topology, registration, rigging, and runtime parity before acceptance.
- CANON-001: After ANAT-001/EMERG-001 contracts are accepted, amend `GAME_CANON.md` and affected PRDs with the explicit anatomy/emergence requirements and hardware budget.

---

## Blocked / Failed Units
- FORGELENS-MOTION-LAB-001 — IMPLEMENTATION AND NON-VISUAL GATES PASSED; VISUAL QA BLOCKED BY LOCAL CUA INFRASTRUCTURE. Added fail-closed repository-bound `--motion-lab` payload loading, synchronized four-view timeline/plots/candidate-difference UI, revision/frame/joint/object/world-bound append-only annotations, and CLI-only external-human outcomes that explicitly never transition a ReviewRun. Focused contract covers canonical path, strict finite data, source hash binding, annotation chain, external human outcome, HTTP snapshot, and rejection of an API `action=approved` field. Fresh evidence `qa_runs/asset_reviews/verification/20260716T171841Z-motion-lab/`: pycompile/node/diff gates zero exit; `test_asset_review.py` 40/40; log SHA-256 in `SHA256SUMS.txt`. Server launch loaded the browser and completed all UI/API requests including `/api/motion-lab`; however `computer_use` could not capture Firefox because cua-driver never reached MCP initialize (30 s timeout), so no vision inspection occurred. Do not claim visual acceptance; resume only after `hermes computer-use doctor` is supplied or capture is restored. No ReviewRun/pin/decision transition, `src/`, truth, replay, asset, dependency, commit, or push change. Strike count: 0.
- ARDY-ACTION-CONSTRAINTS-001: Failed after Strike 2 and fully rolled back; no v4 contract or validator from this method remains. The initial direction-plus-primary-grip design passed root/contact/support checks but the derived complete W0 tip for Strike frame 30 left the declared camera envelope. Repair 1 enlarged and revalidated the W0 envelope/cameras against their own corner preflight, but the same hidden-extent representation failed again at Strike frame 40 when the downward recovery tip crossed the envelope floor. Next falsifier: author explicit pommel/tip endpoints first, require exact W0 axis length and camera-envelope inclusion, then derive normalized blade direction, primary grip, and secondary grip mechanically. No candidate generation occurred.
- C0-WHOLEBODY-BIND-001: Failed after Strike 2; no repository output accepted. Initial actual-rest-delta LBS produced a coherent 1.669×1.767 m neutral body but simultaneous digit stress still tore hands/toes because Blender's stored rest matrices cannot represent the idealized cross-axis similarity scales used to move the mesh. Repair 1 attempted detached-armature probing plus Blender-evaluated modifier sampling; a zero-length shoulder bridge was discovered and corrected, but evaluated output became 2.80 m tall because absolute scaled pose matrices compounded through B-Bone/inherit-scale semantics. Repair 2 used Blender's pose-as-rest operator after splitting each shoulder bridge into nonzero spans; the operator preserved the original 0.986 m arm-down body width instead of baking the target T-pose into the mesh. Evidence: `/tmp/c0_wholebody_bind_qa/`, `/tmp/c0_wholebody_bind_r2_qa/`, `/tmp/c0_wholebody_bind_r3_qa/`. Conclusion: rewriting this 163-bone bind is the wrong integration boundary. Next falsifier preserves the validated bind exactly and stores C0 T-pose as a reference action consumed by retarget reference-pose alignment.
- C0-WHOLEBODY-TRANSFER-001: Failed after Strike 2; no repository tool/output accepted. Initial idealized similarity fit mapped 36 major bones to C0 landmarks with <0.00025 mm endpoint error but inherited target hand/foot/head bone-length scale into unmapped detail chains, producing a 3.55 m arm span and 2.11 m height. Repair 1 preserved source wrist/foot/head lengths at 1.08 global scale, yielding a coherent neutral 1.665×1.780 m body. Simultaneous 60-degree digit stress catastrophically tore hands/toes. Repair 2 wrote exact desired EditBone matrices instead of approximated roll; neutral remained coherent but stress still tore because the mesh had been deformed with idealized uniform-similarity deltas that Blender cannot store in X/Z rest-bone scale. Evidence: `/tmp/c0_wholebody_fit_qa/`, `/tmp/c0_wholebody_fit_r1_qa/`, `/tmp/c0_wholebody_fit_r2_qa/`. Next falsifier: derive mesh deformation from Blender's actual stored rest matrices after editing, so bind and mesh transforms are exactly consistent.
- C0-EXTREMITY-DCC-001: Failed after Strike 2; no repository output was created. Initial Blender Skin-modifier branch cage gave a coherent hand silhouette and visibly stable 60° finger curl, but topology measured 2 hand components and 10 foot components; high-degree graph junctions created disconnected surfaces. Repair 1 replaced star branches with lateral metacarpal/metatarsal chains: foot became one closed manifold component and hand one component, but hand retained 28 boundary/32 nonmanifold edges and both extremities had visible branch gaps. Repair 2 applied a 1.5 mm voxel union before subdivision: foot became one closed 180,002-vertex mesh but blocky, with non-anatomical vertical bar toes; hand fragmented into 187 closed remesh components and was visually destroyed. Evidence: `/tmp/extremity_skin_cage/{neutral,flex60}.png`, `/tmp/extremity_skin_cage_r1/{neutral,flex60}.png`, `/tmp/extremity_skin_cage_r2/{neutral,flex60}.png`. Conclusion: Blender Skin/voxel procedural cages cannot provide production anatomical topology or a safe Meshy-detail carrier. Next falsifier is a proven rig-ready base topology, not another generated/procedural surface.
- C0-DIGIT-COMP-001: Failed after Strike 2; all `digit_components_001` repo files were removed. Generated 12 isolated-reference multi-image Meshy-6 tasks (240 credits) plus single-image middle/ring repairs (40 credits). Multi-view generation often reconstructed multiple camera panels as duplicate disconnected digits or fused them into one base; measured examples included index 69,656+62,969-vertex components, pinky 45,656+43,244, thumb 111,292+108,650 and toe1 four separate shells. Largest-component extraction produced closed digit shells, but palm/forefoot tasks retained long digit stubs. Initial assembly had centered-core transform errors and floating digits. Repair 1 corrected core transforms, exposing duplicate core stubs and visible gaps. Repair 2 exact-clipped the cores and overlapped isolated bases; neutral still had gross seams/misaligned finger bases and duplicate toe forms, while 30°/50° flex exposed detached finger shells and a second row of toe stubs. Evidence: `/tmp/digit_components_geometry_mosaic.png`, `/tmp/isolated_digits_assembly/neutral.png`, `/tmp/isolated_digits_repair1/{neutral,flex}.png`, `/tmp/isolated_digits_repair2/{neutral,flex}.png`. Conclusion: Meshy isolated-component generation does not provide corresponded socket geometry, and overlapping/clipped shells cannot satisfy a continuous deforming body. Next falsifier is one hand-authored continuous DCC cage with explicit deformation loops and Meshy detail transfer after articulation.
- C0-DIGIT-TOPO-001: Failed after Strike 2; generated hand002/foot003/foot004 repo candidates were removed. New articulation-first references produced visually strong Meshy geometry tasks `019f4c84-b6fe-7744-992d-ae8810d0fcc8` (hand, 20 credits), `019f4c84-bf48-749e-8028-b9bb27ca427f` (first foot, 20 credits) and `019f4c8c-91f7-76ee-83b2-9cc5d1a59e5d` (corrected foot, 20 credits). Measured topology still joined the long fingers until 0.12/0.19 m and toes until roughly -0.08/0.27 m. Initial voxel-unioned explicit tube topology articulated independently but was visibly primitive. Unconstrained whole-surface shrinkwrap catastrophically cross-mapped digits in neutral pose. Repair 1 clamped BVH detail transfer to 4 mm, removing global spikes but retaining crude capped anatomy and unusable flex. Repair 2 directly rigged exact Meshy distal components; 35°/50° tests produced detached-looking fingertip hooks and vertically extruded/distorted toes because proximal web/forefoot topology remained shared and lacked deformation spans. Evidence: `/tmp/articulation_extremities_probe/flex.png`, `/tmp/articulation_extremities_conform2/neutral.png`, `/tmp/articulation_extremities_clamped/flex.png`, `/tmp/meshy_extremities_direct/flex.png`. Conclusion: neither whole-extremity Meshy remesh nor unconstrained surface transfer can satisfy full articulation. Next falsifier is isolated digit generation/rigging plus explicit palm/forefoot socket bridging.
- C0-FULL-RIG-001: Failed after Strike 2; all generated `rigged_full_001` files and the augmentation tool were removed. Initial 68-bone augmentation added 30 finger and 14 foot-detail bones with normalized weights and zero unbound vertices, but coordinate-band weights reached deep into palm/web topology: 42°/55° flex produced long geometry spikes and palm collapse; toe flex was visually negligible. Repair 1 moved hand roots distally (thumb x=0.790, fingers x=0.800), reducing assigned hand vertices 14,700→10,520, but spikes/web collapse remained. Repair 2 used exact connected distal components (four fingers beyond x=0.840, separate thumb beyond x=0.800), reducing assignments to 4,881 and eliminating spikes/palm collapse, but only fingertip hooks articulated; proximal phalanges and all toes remained effectively rigid. This proves the accepted Meshy remesh lacks safe articulation loops and preserves web connections too far distally for post-hoc bone weighting. Evidence: `/tmp/c0_digit_bend_contact.png`, `/tmp/c0_digit_bend_repair1_contact.png`, `/tmp/c0_digit_bend_repair2_contact.png`. Next falsifier: articulation-first hand/foot retopology and independent digit rigging before body graft/union.
- C0-RETOPO-002: Failed after Strike 2; no repo output accepted. Independent pre-simplification (body 80,694; each hand 55,000; each foot 55,228 faces) plus four exact booleans yielded one closed manifold component at 294,073 faces and preserved all digits. Initial output retained visible wrist/ankle diameter discontinuities. Repair 1 applied measured weighted Laplacian relaxation (8×0.22) only near junctions; lines remained. Repair 2 cleared sharp edges and forced smooth normals; lines remained, proving geometric cross-section mismatch rather than a shading seam. Evidence: `/tmp/c0_exact_union_extremities.png`, `/tmp/c0_exact_relaxed_extremities.png`, `/tmp/c0_exact_normals_extremities.png`. Next falsifier: matched boundary-loop trimming and explicit tapered loft, not voxelization, cuffs, or broader smoothing.
- C0-RETOPO-001: Failed after Strike 2; no repo output accepted. Initial 3.5 mm voxel union produced one closed 231,126-face mesh and preserved digits, but retained visible wrist/ankle graft rings. Repair 1 applied localized 5×0.58 smoothing; rings remained. Repair 2 added wrist/ankle bridge solids before voxel union; result was closed at 238,150 faces but created severe rectangular cuff bulges. Evidence: `/tmp/c0_voxel_0035_extremities.png`, `/tmp/c0_voxel_0035_smooth_extremities.png`, `/tmp/c0_voxel_bridge_extremities.png`. Accepted five-component C0 source remains untouched. Next falsifier: exact boolean union preserving source surfaces, then constrained relaxation only on the actual intersection loops.
- W0-ASM-001: Failed and rolled back after Strike 2. Initial implementation produced a nonmanifold three-component curve-bevel wrap; Repair 1 replaced it with a watertight explicit tube mesh but exposed wrong total length (0.93549 m); Repair 2 hardened Blender selection but reproduced identical blade collapse. Evidence `/tmp/w0_assemble.log`, `/tmp/w0_assemble_debug.log`. Root-cause hypothesis: setting `obj.scale` overwrote FBX importer unit/axis transform instead of composing/baking it; accepted blade collapsed from expected ~1.55 m assembly span to 0.20888 m. `tools/assemble_meshy_w0.py` and `assembled_001/` were removed; raw sources untouched.
- O1: Rust `ort` bridge decoder reshape error `{1,1024,2} → {1,10,-1}`. Prior attempts: named decoder inputs, owned-data extraction, direct encoder pass-through. Next falsifier: save Python encoder output, load it in Rust, compare element-by-element, then invoke decoder directly. Historical partial commit: `7bc5b3f`.
- VIS-001: Current animated character output is unusable. Evidence: `/tmp/jd001_assets_live.png`, `/tmp/jd001_assets_close.png`, `/tmp/jd001_assets_temporal.png`, `/tmp/jd001_bind_current.png`, and `/tmp/just_dodge_asset_visual_review_2026-07-09.md`. Bind mesh is intact; runtime skinning/retargeting explodes weighted geometry.

---

## Recently Completed
Older completed entries were archived to `.hermes/atomic_ledger_archive.md` on auto-compaction (oldest PVP-004-PACKAGED-INTERACTION-CADENCE).
- **B15I-G1-HINGE-TARGET-BATCH-001 — Passed after delayed read-only review repair, with no failed repair.** Added `src/g1_hinge_adapter.rs` and crate/binary exports. One validated spherical `MotorTargetBatchV1` now converts into a canonical node-ordered batch for the pinned 29 official G1 hinges: the physical root and four virtual markers are excluded; Q15 6D local rotations reuse the integer CORDIC projection core; three-axis milliradian/s velocity targets project onto the same signed one-hot Q30 axis without an uncontracted unit conversion; zero torque remains an explicit valid no-drive command; position and torque clamp to immutable model limits; gains, truth tick, and plan identity remain intact; and canonical FNV-1a receipts are invariant to source-target order. Malformed rotations and root-only output fail closed. This is representation conversion only—no runtime/App/renderer/truth/contact path changed. Focused adapter tests pass 6/6 serial; projection 4/4, articulation 4/4, source motor validation 1/1, and spherical-target compilation 1/1 remain green. Cargo fmt, clippy `-D warnings`, warning-denying all-target tests (prior gate: 129 library passed/1 provisioned ignore, 127 game, 2 capture; two provisioned integrations ignored), replay-100, and `git diff --check` pass. The delayed review identified and the repair removed microradian/s conversion overflow and zero-torque rejection. Sourced OS-tempfile verification emitted `AD_HOC_B15I_REVIEW=PASS adapter=6 projection=4 articulation=4 source=1 compiler=1 serial=true units=millirad_s zero_torque=preserved`; the verifier was removed. Strike count: 0. `PLAYABLE-PROOF=false`.
- **W0-001-DIRECT-CI-WIRING-007 — Passed without repair failure.** Clean CI now compiles the finalized standalone ReviewRun validator and its tests, parses `forgelens_review_run_v1.schema.json`, and executes the 16-test suite before the existing Phase-A verifier. Baseline inspection proved all three paths were absent from CI despite the local suite passing. Post-edit static assertions prove all commands are present; Python compile/tabnanny, JSON parse, ForgeLens 39/39, standalone schema 16/16, Phase-A `PASS_BLOCKED_PENDING_EVIDENCE`, Node syntax, Cargo fmt, clippy `-D warnings`, warning-denying all-target check/test (124 library + 122 game + 2 capture passed; three provisioned tests remain explicitly ignored), replay-100, and `git diff --check` all pass. No ForgeLens runtime/schema bytes changed, no dependency was added, and no commit/stage/promotion occurred. `PLAYABLE-PROOF=false`.
- **W0-001-DIRECT-TRACKED-CLEAN-PREFLIGHT-006 — Passed without repair failure.** Added `/validation_evidence/` beside the existing `qa_runs/` local-output rule in `.gitignore`; 219,398,339 bytes of generated captures/logs no longer contaminate ForgeLens live untracked identity, while `git ls-files validation_evidence` proves no tracked file is hidden. Commit-scope classification is now explicit: ForgeLens source/schema/contract/ledger, three historical baseline reports, and the standalone Blender research document; no generated evidence directory. Cargo lock metadata, format, clippy `-D warnings`, warning-denying all-target check/test, ForgeLens 39/39, schema 16/16, Phase-A blocked verifier, Node syntax, and diff check pass. Remaining tracked-clean transition requires user-authorized commits of the classified source/docs paths; no staging or commit occurred. `PLAYABLE-PROOF=false`.
- **W0-002-DIRECT-BASELINE-AUDIT-001 — Passed as a non-advancing planning audit; no report bytes changed.** All three W0-000 reports are canonical JSON with `PLAYABLE-PROOF=false`; exact report SHA-256 values are revision `6315152a3f40f9e965889ddecfb2f3374bcdc65cd3282f479d16159699f03faa`, provider `6edf874bfddbc96ac405ba685a780f48f19149e9c12115d9486caa9fe28714c1`, and failed visual `34b94947956280377b680364e5562a605ba1a70baa2038859b0aaf6e4bafcc52`. Parent/sibling report hashes cross-check and historical head `6f249b925f8025f9a1e72104c4e4fbfda7e859aa` remains reachable. The visual report has 674 current local path/hash matches and exactly three expected historical ForgeLens-contract mismatches after W0-001 hardening. The revision report's receipt-root manifests are absent, and six captured ForgeLens source hashes differ from current files, so it cannot advance admission or substitute for a fresh revision-bound receipt. Next dependency is a tracked-clean commit of the current W0-001 source followed by a fresh exact-revision baseline; committing remains user-owned. `PLAYABLE-PROOF=false`.
- **W0-001-DIRECT-DUPLICATE-CLEANUP-005 — Passed without repair failure.** Removed untracked `tools/qa/review_run.schema.json`, `review_run_validator.py`, and `test_review_run_validator.py`, a late parallel family referenced only by itself and offering a conflicting self-attested `pass_eligible` model. The verified ForgeLens v1 schema/runtime/promotion path remains singular. All three files are absent, no file-level references remain, no Kanban writer is active, standalone schema tests pass 16/16, Phase-A remains blocked-pending-evidence, and diff check passes. `PLAYABLE-PROOF=false`.
- **W0-001-DIRECT-INTEGRATION-004 — Passed after independent review found four blockers and direct repair closed all four without a failed repair strike.** External pass now receives server-derived pin statuses; pin context remains bound to the immutable ReviewRun revision while current geometry/plan bytes are remeasured, so stale pins block pass without making a later tracked decision-only commit invalidate unchanged pins. The standalone promotion validator remeasures the active ForgeLens profile and rejects forged profile hashes. Viewer recapture must byte-match one immutable produced artifact and records its evidence path, camera, AOV, and geometry identity; unrelated same-size PNGs fail. Server eligibility validates embedded/data-URI PNG bytes and reports exact `texture_decode_failure`; unvalidated formats fail closed, and browser spelling matches. External decision import also rechecks measured path bytes and committed bytes before writing terminal pass. Full focused tests pass 39/39 plus 16/16 standalone schema tests; Phase-A remains `PASS_BLOCKED_PENDING_EVIDENCE`; Python compile/tabnanny, Node syntax, and diff checks pass. `PLAYABLE-PROOF=false`.
- **W0-001-DIRECT-EXTERNAL-PASS-003 — Passed after one RED regression, no repair failure.** A temp-Git end-to-end test proved the submitted `reviewerId`/external `reviewerPseudonym` mismatch blocked every legitimate pass. Importer and reload scanner now compare those identities deliberately, and the pass writer emits exactly the seven scanner-required tracked-clean decision bindings. The external decision is committed after the immutable source revision, imported only when that source remains an ancestor and all run evidence revalidates, then reloads as terminal `pass`; browser/API pass remains rejected. Focused RED turned green, full ForgeLens tests passed 39/39, standalone schema tests 15/15, Python compile/tabnanny, Node syntax, Phase-A blocked verifier, and `git diff --check` passed. `PLAYABLE-PROOF=false`.
- **W0-001-DIRECT-CONTRACT-002 — Passed without repair failure.** Phase-A now binds the current seven tracked ForgeLens profile files, including its verifier, with exact SHA-256 values and aggregate tree hash. `verify_forgelens_phase_a.py` emits `FORGELENS_PHASE_A_CONTRACT=PASS_BLOCKED_PENDING_EVIDENCE` and `PLAYABLE_PROOF=false`; compile/tabnanny and `git diff --check` pass. The three standalone draft schema helpers remain outside the tracked Phase-A profile and do not weaken its tracked-source gate.
- **W0-001-DIRECT-SCHEMA-001 — Passed without repair failure after terminating two orphaned Kanban writers.** Standalone pass eligibility now takes a repository root and remeasures Git HEAD/reachability, working/staged/untracked identities, every bound file's bytes and tracked-clean state; non-finite contract values fail canonical validation. The final schema repair made `RepositoryPath` reject empty, dot, parent, trailing, backslash, and NUL segments. Focused tests passed 15/15; Python compile/tabnanny and `git diff --check` passed. `PLAYABLE-PROOF=false`.
- **PVP005-DELEGATED-AUDIT-REPAIR-001 — Passed without repair failure; no candidate generation or acceptance.** Reconciled all four full delegated audits. W0 sockets moved from collar-invalid -0.120/-0.280 m to GripCore/GripWrap-valid -0.105/-0.265 m while preserving 160 mm separation and at least 5 mm clearance; first-person envelope preflight now proves 60°/70°/85°. ARDY v4 now keys f0/f2/f3/f7 shipping-visible tells, uses immutable symbolic foot anchors, explicit swing origin/destination/apex/40 mm clearance, and permits root translation only in bounded single-support phases; dual support is limited to 1 mm. The repository generator exits before argument parsing, model/CUDA/output access unless a future content-addressed authorization gate replaces the hard disable; a subprocess negative test proves no output directory is created. ForgeLens Phase-A now validates tracked source paths, descendant evaluation, aggregate source-tree hash, current visual config, and the historical receipt's original config hash separately. CI now executes clippy rather than merely installing it. Focused config/contract validators pass; ForgeLens passes 22/22; cargo fmt, clippy `-D warnings`, warning-denying all-target check, and generic all-target tests pass (124 library + 122 game + 2 capture; three provisioned tests explicitly ignored here and retained by the canonical hydrated gate). Contract hashes: W0 `17b7117858ce40aeca41fff8f92b6b5352371df6dcb6db9e5c2ecd3a1808f779`; ARDY v4 `16c4f08dc7ae5bbb78f05baed583068a2a39513cafba55646028dbeef5c5d269`; visual config `fec6cfe8f19d5c353961b1d1fc10f5a9642c73a8d29481476a878874b98a40f9`. `PLAYABLE-PROOF=false`.
